      { id: 5, title: "Maintaining and Automating Data Workloads", weight: "18%", color: "#c5221f", questions: [{ q: "A nightly Cloud Composer DAG fails because a BigQuery table it reads from was not updated by the upstream team. How should you implement a robust wait condition?", opts: ["Use a TimeSensor to wait 2 hours and hope the data arrives", "Use BigQueryTableExistenceSensor or a custom sensor that checks data freshness before proceeding", "Add a try/except in the BigQuery operator to retry 10 times", "Split the DAG into two separate DAGs and trigger manually"], ans: 1, explain: "Airflow sensors poll until the expected partition or row count is met before downstream tasks run. This is the correct pattern for inter-team data dependencies — the DAG waits rather than failing or running on stale data.", concept: "Airflow sensors, BigQueryTablePartitionExistenceSensor, DAG data dependencies" }, { q: "You need to monitor BigQuery slot utilization and alert when average slot consumption exceeds 80% for 10 consecutive minutes. How do you implement this?", opts: ["Check BigQuery job history manually in the console every morning", "Create a Cloud Monitoring alert policy on the bigquery.googleapis.com/storage/table_count metric", "Use INFORMATION_SCHEMA.JOBS_BY_PROJECT to write a scheduled query that sends alerts via email", "Create a Cloud Monitoring alert policy on the bigquery.googleapis.com/reservation/slot_utilization metric with a threshold condition and notification channel"], ans: 3, explain: "BigQuery exports slot utilization metrics to Cloud Monitoring. Create an alert policy on the slot_utilization metric with a condition: average > 0.8 for 10 consecutive minutes, firing to a notification channel.", concept: "Cloud Monitoring, BigQuery metrics, reservation slot alerts" }, { q: "A Dataflow streaming job has been running for 30 days. You need to update the pipeline code (add a new transform) with zero downtime. What is the correct procedure?", opts: ["Stop the current job and immediately start a new job with updated code; brief downtime is acceptable", "Use Dataflow job update (--update flag with the same job name and compatible graph) to replace the running job in-place with no data loss", "Create a new job alongside the old one; route 50% of Pub/Sub traffic to each", "Deploy updated code as a new Cloud Function and retire the Dataflow job"], ans: 1, explain: "Dataflow supports in-place job updates via the --update flag. The runner drains the current job state to the new graph, maintaining checkpoints and Pub/Sub offsets. This achieves zero-downtime pipeline updates.", concept: "Dataflow job update, zero-downtime deployment, in-place graph replacement" }, { q: "How should you implement Infrastructure as Code (IaC) for BigQuery datasets, tables, and IAM bindings to ensure reproducible environments?", opts: ["Use bq CLI scripts in bash — they are idempotent", "Use Terraform with the google_bigquery_dataset and google_bigquery_table resources; store state in Cloud Storage; use CI/CD for automated deployment", "Manually create resources in the console and document the steps in a wiki", "Use Cloud Deployment Manager YAML files — preferred over Terraform for GCP resources"], ans: 1, explain: "Terraform is the industry standard for GCP IaC. The Google Terraform provider has full BigQuery support (datasets, tables, views, IAM). CI/CD pipelines (Cloud Build) apply changes on merge.", concept: "Terraform, IaC, BigQuery provisioning, CI/CD for data infrastructure" }, { q: "A data pipeline's SLA requires BigQuery tables to be refreshed by 6 AM UTC daily. The pipeline occasionally misses this due to variable upstream delays. What monitoring and alerting setup detects SLA breaches?", opts: ["Use Cloud Monitoring log-based metric on Composer DAG success; create an alert if success is not observed by 6:05 AM UTC", "Manually review BigQuery table metadata every morning for freshness", "Set up Cloud Scheduler to query the table at 6 AM and email results", "Use Datastream to track table modification time and alert via Cloud Functions"], ans: 0, explain: "Create a Cloud Monitoring log-based metric that counts DAG success events. An alert policy checks: if success count = 0 in the window [5:55 AM, 6:05 AM UTC], fire a PagerDuty alert. This is a tight, SLA-aware monitoring pattern.", concept: "SLA monitoring, Cloud Monitoring log-based metrics, DAG alerting, Composer" }, { q: "Your team needs to manage BigQuery table access for 200 analysts. Access changes frequently. Using individual IAM bindings at the table level is unmanageable. What is the scalable approach?", opts: ["Grant roles/bigquery.dataViewer to all analysts on the project level", "Use Google Groups: create analyst groups; grant IAM bindings to groups; add/remove individuals from groups for access changes", "Create a service account for each analyst with pre-generated keys", "Use BigQuery authorized views for each analyst with their email in the WHERE clause"], ans: 1, explain: "Managing IAM at the group level is Google's best practice. Individual-level IAM bindings create sprawl. Google Groups allow centralized membership management: add/remove an analyst from a group and their BigQuery access changes automatically.", concept: "IAM best practices, Google Groups, scalable access management" }, { q: "A Dataproc job fails intermittently with 'Out of Memory' errors on the executor nodes when processing a particularly large partition. What is the correct tuning approach?", opts: ["Double the number of Dataproc workers to spread memory pressure", "Increase executor memory (spark.executor.memory) and/or reduce partition size using repartition() to prevent single large partitions; also increase spark.memory.fraction", "Switch to Dataflow which handles memory automatically", "Reduce the number of executors per node to give each more memory"], ans: 1, explain: "OOM on executors is typically caused by data skew or insufficient memory. Fix: repartition() to break up large partitions; increase spark.executor.memory; tune spark.memory.fraction. Simply adding workers won't help if one worker still processes a massive partition.", concept: "Spark executor memory tuning, data skew, repartition, OOM debugging" }, { q: "You need to implement automated data quality checks in a CI/CD pipeline that runs before any BigQuery table goes to production. Which approach is most robust?", opts: ["Visual inspection of data by a data analyst before each deployment", "Use Great Expectations or dbt tests integrated into Cloud Build; fail the CI/CD pipeline if data quality assertions fail", "Schedule a Cloud Monitoring alert to check data quality after deployment", "Trust BigQuery schema validation to catch data quality issues"], ans: 1, explain: "dbt tests or Great Expectations define data quality expectations as code. Integrating into Cloud Build CI/CD means BigQuery tables failing quality checks block deployment — shifting quality left and catching issues before production.", concept: "dbt tests, Great Expectations, data quality in CI/CD, Cloud Build" }, { q: "A Dataflow job is consistently slow. Profiling shows 70% of time is spent in a ParDo reading from Cloud Spanner inside the hot loop. What optimization should you apply?", opts: ["Increase Dataflow worker memory — Spanner reads are CPU-bound", "Move the Cloud Spanner lookup out of the ParDo into a Dataflow side input or batch multiple keys per Spanner read call to reduce RPC overhead", "Switch from Dataflow to Dataproc — Spark handles external reads more efficiently", "Disable Dataflow autoscaling to reduce Spanner connection churn"], ans: 1, explain: "Per-element RPCs in a hot loop create massive overhead. Fix: group elements by key and batch Spanner reads (batchRead API); or load reference dataset as a side input at startup. This reduces RPC count from N calls to N/batch_size calls.", concept: "Dataflow performance, batch RPC calls, side inputs, Spanner integration" }, { q: "Your organization uses Cloud Build for CI/CD of data pipelines. You want to ensure that a Dataflow template deployed to production has been tested in staging first. Which Cloud Build + Dataflow pattern enforces this?", opts: ["Deploy to production first, then backfill staging from production data", "Use Cloud Build steps: (1) deploy Flex Template to staging bucket; (2) run integration test job against staging; (3) on success, promote the same template image to production bucket", "Manually copy the template from staging to production after developer review", "Use Dataflow's built-in staging promotion feature"], ans: 1, explain: "Proper CI/CD gate: build the Flex Template image → deploy to staging → launch test Dataflow job → validate output → promote the identical template image to production. The exact tested artifact reaches production.", concept: "Dataflow Flex Templates, Cloud Build CI/CD, staging promotion patterns" }, { q: "How does BigQuery handle concurrent DML statements (INSERT, UPDATE, DELETE) on the same table?", opts: ["BigQuery DML uses pessimistic locking — concurrent DML statements queue and execute serially", "BigQuery DML uses optimistic concurrency — statements read a snapshot at start time; if conflicts are detected at commit, the statement is retried automatically up to 5 times", "BigQuery does not support concurrent DML; only one DML statement may run per table at a time", "BigQuery DML has no isolation guarantees — concurrent statements may produce undefined results"], ans: 1, explain: "BigQuery uses snapshot isolation for DML. Each DML statement reads data at a consistent snapshot. Concurrent statements proceed; at commit time, if conflicting write is detected, BigQuery retries automatically.", concept: "BigQuery DML concurrency, snapshot isolation, ACID transactions" }, { q: "A data pipeline uses Cloud Composer to trigger a BigQuery job daily. The Composer environment is in version 1 (Airflow 1.x) which reaches end-of-life. What is the migration path?", opts: ["Upgrade Composer 1 environment in-place to Composer 2 using gcloud composer upgrade", "Create a new Composer 2 environment; export DAGs, variables, and connections from Composer 1; import to Composer 2; validate; redirect traffic; delete Composer 1", "Migrate DAGs to Cloud Workflows which is the replacement for Cloud Composer", "Keep using Composer 1; GCP does not deprecate services with active customers"], ans: 1, explain: "Cloud Composer 1 and 2 environments cannot be upgraded in-place. Create a new Composer 2 environment → export DAGs (already in Cloud Storage) → export Airflow variables/connections → import → test → delete old environment.", concept: "Cloud Composer migration, Composer 1 to 2, Airflow version upgrade" }, { q: "You want to set a budget alert for BigQuery on-demand query costs per project and automatically pause workloads when budget is exceeded. How do you implement this?", opts: ["BigQuery has no built-in cost controls — use manual invoice reviews", "Cloud Billing Budget: create a budget with threshold alerts; use Pub/Sub notification on budget alert → Cloud Function to set custom project quota to 0 for BigQuery API, effectively pausing queries", "Create an IAM policy that revokes BigQuery access at month-end", "Use BigQuery Reservations to hard-cap costs at a fixed monthly amount"], ans: 1, explain: "Cloud Billing budgets send Pub/Sub notifications when thresholds are hit. A Cloud Function sets the bigquery.googleapis.com daily bytes processed quota on the project to 0, blocking all on-demand queries.", concept: "Cloud Billing budgets, cost controls, BigQuery quota management" }, { q: "Which BigQuery feature allows you to schedule recurring SQL queries and write results to a destination table on a cadence?", opts: ["BigQuery Materialized Views with auto-refresh", "BigQuery Scheduled Queries using Cloud Scheduler integration (via the BigQuery Data Transfer Service scheduler)", "Cloud Composer DAG with BigQueryOperator scheduled by Airflow", "BigQuery Jobs API polled every hour by Cloud Functions"], ans: 1, explain: "BigQuery Scheduled Queries allows SQL queries to be scheduled at recurring intervals. Results can be written to a destination table with WRITE_TRUNCATE or WRITE_APPEND. Parameterized queries support @run_date substitution for incremental processing.", concept: "BigQuery Scheduled Queries, Data Transfer Service, recurring SQL" }, { q: "A data team wants to track the cost of individual pipelines in a large GCP project with many BigQuery jobs. How should they attribute costs per pipeline?", opts: ["Separate each pipeline into its own GCP project for clean billing separation", "Add consistent labels to BigQuery jobs (job_labels in JobConfiguration); use Cloud Billing export to BigQuery and filter/group by labels for cost attribution", "Use BigQuery INFORMATION_SCHEMA.JOBS to manually estimate cost by scanning bytes per job", "Cost attribution is only possible at the project level in GCP"], ans: 1, explain: "BigQuery job labels propagate to Cloud Billing export. Label jobs with pipeline=customer-churn, team=data-engineering. Exporting billing data to BigQuery and GROUP BY labels gives granular cost breakdowns per pipeline.", concept: "BigQuery job labels, cost attribution, Cloud Billing export" }, { q: "A Dataflow streaming pipeline is running in production. You observe that the pipeline's data staleness metric is steadily increasing over 6 hours. What does this indicate and what should you do?", opts: ["Data staleness increase is normal for streaming pipelines; ignore it", "Increasing data staleness means the pipeline is falling behind — the watermark is not advancing as fast as real-time. This indicates the pipeline cannot keep up with input throughput. Increase maxNumWorkers, investigate slow transforms, or check for Pub/Sub backlog growth", "Data staleness metric is informational only — it doesn't indicate a problem", "Restart the Dataflow job to reset the staleness metric"], ans: 1, explain: "In Dataflow, data staleness = current processing time minus watermark. Steadily increasing staleness means the pipeline is accumulating a backlog — it cannot process messages as fast as they arrive. This will eventually cause unbounded lag. Actions: increase workers, optimize slow transforms, check for hot keys or resource contention.", concept: "Dataflow data staleness, streaming backlog, pipeline monitoring, watermark lag" }, { q: "You need to set up automated alerting for Dataproc job failures across all clusters in a project. What is the most scalable approach?", opts: ["SSH into each Dataproc master node to check job logs every hour", "Create a Cloud Monitoring alert policy on the Dataproc job metric (dataproc.googleapis.com/job/state with filter state=FAILED); configure notification channels (PagerDuty, email, Pub/Sub)", "Use Cloud Scheduler to query Dataproc job status API every 5 minutes", "Use Cloud Composer to monitor all Dataproc jobs and send alerts"], ans: 1, explain: "Cloud Monitoring natively monitors Dataproc metrics. A metric alert on job/state=FAILED fires whenever any Dataproc job in the project fails. Notification channels (PagerDuty, Slack, email via Cloud Monitoring) deliver the alert immediately — more scalable than polling-based approaches.", concept: "Dataproc monitoring, Cloud Monitoring alerts, job failure detection" }, { q: "A data engineering team uses git for version control. How should they manage Airflow DAGs in Cloud Composer to ensure DAGs are deployed via CI/CD and not manually edited in GCS?", opts: ["Grant all engineers direct access to the Composer GCS bucket for manual DAG edits", "Store DAGs in a git repository; set up a Cloud Build trigger on the main branch that syncs changed DAG files to the Composer GCS bucket automatically via gsutil rsync; restrict direct GCS bucket access to the CI/CD service account only", "Use Composer's built-in git sync feature — it automatically pulls from git every minute", "Commit DAG files directly to GCS using git-gcs plugin"], ans: 1, explain: "CI/CD for DAGs: git push → Cloud Build trigger → gsutil rsync DAG files to Composer's GCS bucket. Restrict direct bucket write access to only the CI/CD service account. This enforces code review via PRs before any DAG change reaches Composer.", concept: "Cloud Composer CI/CD, DAG deployment, git integration, Cloud Build" }, { q: "A Dataflow job reads from BigQuery, performs complex transformations, and writes results to BigQuery. The job runs daily for 4 hours and costs $200/run. How do you reduce cost?", opts: ["Run the job weekly instead of daily — 7x cost reduction", "Investigate Dataflow FlexRS: use --flexrs_goal=COST_OPTIMIZED to use Spot VMs during non-peak hours, reducing batch job costs by up to 40%", "Switch to Dataproc — it's always cheaper than Dataflow for batch jobs", "Reduce the number of BigQuery columns read — Dataflow charges per column"], ans: 1, explain: "Dataflow FlexRS (Flexible Resource Scheduling) reduces batch job cost by 30-40% by using Spot (preemptible) VMs and scheduling when GCP has spare capacity. Suitable for daily batch jobs that can tolerate a flexible start time (up to 6-hour delay). Dataproc isn't always cheaper — depends on job characteristics.", concept: "Dataflow FlexRS, batch job cost reduction, Spot VMs" }, { q: "How does Cloud Composer handle failed tasks when a DAG has complex dependencies?", opts: ["All downstream tasks automatically succeed when an upstream task fails", "Failed tasks are marked as failed; downstream tasks with dependencies on the failed task are also marked as upstream_failed — they don't run. After the root cause is fixed, you can clear (reset) only the failed task and its downstream tasks for re-execution without rerunning the entire DAG", "Cloud Composer automatically retries all failed tasks indefinitely", "The entire DAG restarts from the beginning when any task fails"], ans: 1, explain: "Airflow's dependency model: when Task A fails, all tasks with depends_on_past=True or direct dependency on A are marked upstream_failed. Fix the issue, then use 'Clear' in the Airflow UI to selectively re-run only the failed task and its downstream chain — preserving already-succeeded task results.", concept: "Airflow task failure propagation, upstream_failed, selective DAG clearing" }, { q: "You need to set up monitoring for data freshness — alerting when a BigQuery table hasn't been updated in more than 2 hours. What is the most automated approach?", opts: ["Manually check BigQuery table metadata every 2 hours", "Use INFORMATION_SCHEMA.PARTITIONS to check last_modified_time in a scheduled query; write a 0/1 flag to a monitoring table; Cloud Monitoring alert on the flag", "Create a Cloud Monitoring custom metric: a Cloud Scheduler job every 30 minutes queries INFORMATION_SCHEMA.TABLE_METADATA_TIMELINE and writes a metric via the Cloud Monitoring API; alert when freshness_hours > 2", "Query BigQuery directly from Cloud Monitoring — it has native BigQuery freshness monitors"], ans: 2, explain: "Cloud Monitoring custom metrics: Cloud Scheduler triggers a Cloud Function every 30 minutes → Cloud Function queries INFORMATION_SCHEMA for last_modified_time → computes hours since last update → writes a custom metric (custom.googleapis.com/bigquery/table_freshness_hours) → Cloud Monitoring alert fires when > 2.", concept: "Data freshness monitoring, Cloud Monitoring custom metrics, INFORMATION_SCHEMA" }, { q: "A production Dataflow streaming pipeline has a memory leak — worker memory grows by 1 GB per hour. After 24 hours, workers run out of memory. How do you mitigate while debugging?", opts: ["Ignore it — Dataflow automatically handles memory leaks", "Configure Dataflow to automatically restart workers after N hours (using max_idle_time) as a temporary mitigation. Also enable worker profiling (Google Cloud Profiler integration) to identify the memory leak source in the pipeline code", "Add more memory to workers — a leak on a large machine will take longer to cause failure", "Switch to Dataproc — Spark has better memory management than Dataflow"], ans: 1, explain: "Temporary mitigation: use the --max_worker_restart_count and worker drain triggers, or configure autoscaling to retire workers after extended use. The proper fix requires Cloud Profiler integration with Dataflow workers to identify which DoFn or state cell is accumulating memory — then fix the leak in code.", concept: "Dataflow memory leak mitigation, worker profiling, Cloud Profiler" }, { q: "Which tool provides the most detailed view of BigQuery query execution — showing individual query stages, worker utilization, and data shuffle between stages?", opts: ["Cloud Monitoring BigQuery dashboards", "BigQuery Query Plan in the Query Details UI (or via API: jobs.get → statistics.queryPlan) — shows each execution stage, worker count, input/output rows, elapsed time, and shuffle data volume between stages", "Cloud Logging BigQuery logs — show query text and duration", "INFORMATION_SCHEMA.JOBS — shows aggregate statistics but not stage-level breakdown"], ans: 1, explain: "BigQuery Query Plan (Execution Details tab): each stage shows read/write operators, assigned workers, elapsed time, input bytes, and shuffle output. Hot stages (high wait time, high shuffle) indicate bottlenecks. This is the primary tool for query-level performance diagnosis.", concept: "BigQuery query plan, execution stages, performance diagnosis, query optimization" }, { q: "Your data team runs hundreds of dbt models on BigQuery. Models take 4 hours to run daily. How do you reduce dbt run time?", opts: ["Increase BigQuery slot reservations — more slots make dbt faster", "Use dbt incremental models (config type='incremental') for large tables that only need to process new records; parallelize dbt with --threads N to run independent models concurrently; use dbt's --select tag:daily to run only relevant models", "Run dbt once per week instead of daily", "Switch from BigQuery to Cloud Spanner for dbt transformations"], ans: 1, explain: "dbt run optimization: (1) incremental models process only new rows instead of full refresh — dramatically reduces processing time for large tables; (2) increase --threads (parallelism within a single dbt run); (3) tag-based selection runs only relevant models. These can reduce 4-hour runs to under 1 hour.", concept: "dbt optimization, incremental models, dbt threads, selective execution" }, { q: "A Dataproc cluster running PySpark jobs is experiencing slow job startup (5-10 minutes) due to Python package installation on each job. How do you fix this?", opts: ["Pre-install packages on the Dataproc cluster master node only", "Create a Dataproc custom image with all required Python packages pre-installed; or use Dataproc's conda/pip initialization action that caches packages in Cloud Storage. The custom image approach eliminates all per-job installation time", "Install packages at job submission time using --py-files for each job", "Use Dataproc Serverless — it handles Python package management automatically"], ans: 1, explain: "Python package installation on every Dataproc job is a major startup overhead. Solutions: (1) Custom images: pre-bake all packages into the machine image — zero installation time at job start; (2) Initialization actions with caching: scripts install packages but cache to a shared location. Custom images provide the fastest startup.", concept: "Dataproc Python packages, custom images, initialization actions, job startup optimization" }, { q: "You need to implement a data pipeline that is automatically retried on failure with exponential backoff. The pipeline runs as a Cloud Function. What is the recommended implementation?", opts: ["Cloud Functions automatically retry failed executions with exponential backoff by default", "Configure the Cloud Function with retry-on-failure enabled (--retry flag); Pub/Sub triggers re-deliver the message with exponential backoff (up to 600 seconds between retries) until the function succeeds or the message retention period expires", "Use Cloud Scheduler to retry the function every 5 minutes after failure", "Write custom retry logic in the function code using time.sleep()"], ans: 1, explain: "Pub/Sub-triggered Cloud Functions with retry enabled: if a function throws an exception (or returns non-2xx), Pub/Sub redelivers the message with exponential backoff (30s → 60s → 120s → ... up to 600s). This continues until success or the 7-day message retention expires.", concept: "Cloud Functions retry, Pub/Sub exponential backoff, event-driven retries" }, { q: "How should you implement automated testing for a BigQuery data pipeline before deploying to production?", opts: ["Run the pipeline directly in production with a small subset of data", "Unit test pipeline logic locally (Beam DirectRunner); integration test with a dedicated BigQuery test dataset using CI/CD (Cloud Build); run dbt tests against test data; validate row counts and schema in the test environment before production deployment", "Trust the pipeline code reviews — testing is redundant for data pipelines", "Use BigQuery time travel to rollback production if tests would fail"], ans: 1, explain: "Multi-layer testing: (1) unit tests using DirectRunner with TestStream for Beam pipelines; (2) integration tests in Cloud Build with a dedicated test BigQuery dataset; (3) dbt tests (not_null, unique, referential integrity) run in CI. Only promote to production when all tests pass.", concept: "Data pipeline testing, DirectRunner, Cloud Build CI/CD, dbt tests" }, { q: "A data team uses Airflow on Cloud Composer. They want to prevent a single slow DAG from monopolizing all Airflow workers. How do you implement resource isolation in Airflow?", opts: ["Airflow does not support resource isolation between DAGs", "Use Airflow pools: create named pools with a limited number of slots; assign tasks to pools; slow DAGs are limited to their pool's slot count, preventing monopolization of all workers", "Use Cloud Composer environment scaling to add workers dynamically", "Separate slow DAGs into a second Cloud Composer environment"], ans: 1, explain: "Airflow Pools: create a pool (Pool with N slots). Assign tasks: task = BigQueryOperator(..., pool='slow_dag_pool', pool_slots=1). Slow tasks can only occupy N pool slots total — ensuring fast DAG tasks always have worker availability by keeping them in the default_pool.", concept: "Airflow pools, resource isolation, DAG concurrency control" }, { q: "You need to monitor Cloud Composer (Airflow) for SLA misses — DAGs that fail to complete within their expected duration. Which Airflow feature handles this natively?", opts: ["Configure Cloud Monitoring alerts on Cloud Composer scheduler metrics", "Use Airflow SLA miss callbacks: set sla=timedelta(hours=2) on a DAG or task; Airflow calls the sla_miss_callback function when the SLA is exceeded; configure the callback to send an alert via email or Pub/Sub", "Use Cloud Scheduler to trigger an alert if the DAG is still running after 2 hours", "Monitor Cloud Composer logs in Cloud Logging for slow execution patterns"], ans: 1, explain: "Airflow SLA misses: set sla=timedelta(hours=2) on DAG or task. When execution time exceeds 2 hours, Airflow calls sla_miss_callback with details of the missed SLA. The callback can send Pub/Sub notifications, emails, or PagerDuty alerts. This is Airflow's native SLA enforcement mechanism.", concept: "Airflow SLA misses, sla_miss_callback, pipeline SLA enforcement" }, { q: "A BigQuery table grows by 100 GB per day. At this rate, the table will cost more than budgeted in 6 months. What automated mechanism controls storage costs?", opts: ["Manually delete old partitions when storage gets too large", "Configure BigQuery partition expiration (partition_expiration_days on the table or at partition creation) to automatically delete partitions older than N days. Set up a Cloud Monitoring alert on bigquery.googleapis.com/storage/stored_bytes to track growth", "Export old data to Cloud Storage manually every month", "Set a BigQuery table size quota — BigQuery rejects inserts when the table exceeds the limit"], ans: 1, explain: "Partition expiration: set partition_expiration_days=90 on the table; BigQuery automatically deletes partitions older than 90 days. This caps the maximum table size at 90 × 100 GB = 9 TB regardless of data age. Combined with a Cloud Monitoring alert on stored_bytes, you get automated cost control with visibility.", concept: "BigQuery partition expiration, automated storage management, cost cap" }, { q: "A Dataflow job reads from 1,000 small Cloud Storage files. The job is slow at startup due to file listing overhead. What is the most effective optimization?", opts: ["Increase Dataflow worker count — more workers list files faster", "Pre-generate a manifest file (list of GCS URIs) at pipeline launch and pass it to FileIO.matchAll() rather than using wildcard globs. Also compact small files into larger Parquet/Avro files — smaller file count reduces parallelism overhead", "Switch to BigQuery as the data source — BigQuery avoids file listing overhead", "Use Cloud Storage requester pays to prioritize listing operations"], ans: 1, explain: "File listing overhead: GCS glob patterns require listing all objects matching the pattern — slow for 1,000 files. Pass a pre-computed list of URIs via FileIO.matchAll(). Long-term fix: compact small files into fewer large files to reduce parallelism overhead and improve throughput.", concept: "Dataflow file listing optimization, FileIO, GCS glob performance, small files" }, { q: "You need to implement canary deployment for a Dataflow pipeline — gradually shift traffic from old to new pipeline version while monitoring for errors. What is the pattern?", opts: ["Dataflow has a built-in canary deployment mode", "Run both old and new Dataflow pipelines simultaneously; route a small fraction of Pub/Sub messages to the new pipeline using Pub/Sub message filters or a routing pipeline; monitor error rates on both; gradually increase new pipeline share until 100%", "Deploy the new pipeline and immediately stop the old one", "Use Dataflow job update (--update) for all version changes"], ans: 1, explain: "Dataflow canary: publish messages to two Pub/Sub subscriptions (old and new) in split ratios using a routing Dataflow pipeline or Pub/Sub topic filters. Monitor new pipeline error rates (Cloud Monitoring). Increase new pipeline traffic share as confidence grows. Once at 100%, drain and stop the old pipeline.", concept: "Canary deployment, Dataflow, Pub/Sub traffic splitting, gradual rollout" }, { q: "How does Terraform manage BigQuery Scheduled Query configurations to ensure they are version-controlled and reproducible?", opts: ["Terraform cannot manage BigQuery Scheduled Queries — only datasets and tables", "Use the google_bigquery_data_transfer_config Terraform resource to define scheduled query parameters (query, destination table, schedule, run_time_offset_duration, params). This allows version-controlling scheduled query definitions in git alongside table definitions", "Use bq CLI commands in Terraform null_resource for scheduled queries", "BigQuery Scheduled Queries are managed only through the BigQuery UI"], ans: 1, explain: "Terraform's google_bigquery_data_transfer_config resource manages scheduled queries (via BigQuery Data Transfer Service). Define schedule, query_string, destination_table, and schedule in HCL. This enables version-controlled, reproducible scheduled query deployments via CI/CD.", concept: "Terraform BigQuery Data Transfer, IaC scheduled queries, version control" }, { q: "A Dataproc Spark job produces incorrect results intermittently. You suspect a data race condition in the Spark accumulator. How do you investigate?", opts: ["Accumulators in Spark are perfectly thread-safe; race conditions are impossible", "Spark accumulators are write-only on workers (add-only semantics) — they cannot be read from worker tasks. However, accumulator values may be double-counted if tasks are re-executed (retries). Check spark.task.maxFailures and examine whether task retries are occurring by reviewing Spark history server logs", "Increase Spark worker count to reduce task contention", "Disable Spark shuffle to eliminate race conditions"], ans: 1, explain: "Spark accumulator semantics: accumulators are correctly updated when tasks complete successfully. However, if a task is retried (due to speculation or failure), the accumulator is incremented multiple times. Check Spark History Server for task retry counts. Fix: disable speculation (spark.speculation=false) or use idempotent accumulation logic.", concept: "Spark accumulators, task retries, speculation, Spark debugging" }, { q: "Your organization has a Shared VPC setup. Dataflow jobs run in the service project but need to access Cloud Storage in the host project. What configuration is needed?", opts: ["Dataflow cannot access resources in other projects in a Shared VPC setup", "The Dataflow service account in the service project needs Cloud Storage permissions on the host project bucket. The Shared VPC subnet used by Dataflow workers must be configured in the service project. Grant roles/compute.networkUser to the Dataflow service agent in the host project for VPC access", "Move the Cloud Storage bucket to the service project", "Use VPC peering instead of Shared VPC for Dataflow cross-project access"], ans: 1, explain: "Shared VPC Dataflow setup: (1) Dataflow workers use the Shared VPC subnet (configured via --subnetwork flag pointing to host project's subnet); (2) Dataflow service agent needs roles/compute.networkUser on the host project for network access; (3) Dataflow service account needs Storage permissions on the target bucket regardless of project.", concept: "Shared VPC, Dataflow networking, cross-project access, service agent permissions" }, { q: "You need to implement a data catalog that automatically documents all BigQuery tables created by Dataform. How do you achieve this?", opts: ["Manually document each table in a wiki after Dataform deployment", "Configure Dataform to publish table descriptions and column documentation from SQLX config blocks to BigQuery table descriptions via Dataform's built-in documentation publishing; set up Dataplex auto-discovery to catalog BigQuery assets automatically including published descriptions", "Use Cloud Data Fusion for all transformations — it has built-in documentation", "BigQuery INFORMATION_SCHEMA provides all documentation automatically"], ans: 1, explain: "Dataform SQLX config blocks support table and column descriptions: config { type: 'table', description: 'Daily sales summary', columns: { revenue: 'Total net revenue' } }. Dataform publishes these to BigQuery table/column descriptions. Dataplex auto-discovery picks up these descriptions in the unified catalog.", concept: "Dataform documentation, BigQuery table descriptions, Dataplex auto-discovery, data catalog" }, { q: "A Cloud Composer DAG has 50 tasks with complex dependencies. Some task failures should NOT fail the entire DAG (optional tasks). How do you implement this?", opts: ["All tasks in Airflow must succeed for the DAG to succeed — optional tasks are not supported", "Set trigger_rule='all_done' on tasks downstream of optional tasks — they run whether the upstream task succeeded or failed. For the optional task itself, set on_failure_callback to log the failure and continue without marking the DAG as failed", "Use try/except inside the task to prevent failure propagation", "Create a separate DAG for optional tasks and trigger it independently"], ans: 1, explain: "Airflow trigger rules handle optional task chains: trigger_rule='all_done' means 'run when all upstream tasks are done, regardless of their state' — unlike the default 'all_success'. Combine with on_failure_callback for alerting without DAG failure. This enables optional task graphs within a single DAG.", concept: "Airflow trigger rules, all_done, optional tasks, DAG flow control" }, { q: "You need to version-control and deploy Cloud Spanner database schemas alongside application code. What is the recommended approach?", opts: ["Apply DDL changes manually via Cloud Console before deploying application code", "Use Spanner's schema versioning via migration tool (Liquibase, Flyway with Spanner JDBC driver, or Wrench CLI); store DDL migration scripts in git; CI/CD pipeline applies pending migrations to Spanner before or after application deployment", "Store schema in BigQuery INFORMATION_SCHEMA and sync to Spanner", "Use Terraform for all Spanner schema changes including ALTER TABLE"], ans: 1, explain: "Spanner schema migrations: use Wrench (Google's Spanner migration CLI) or Flyway/Liquibase with Spanner JDBC. Migration SQL scripts are versioned in git (V1__add_column.sql, V2__create_index.sql). CI/CD pipeline runs 'wrench migrate up' before application deployment. Online schema changes in Spanner ensure zero downtime.", concept: "Cloud Spanner schema migrations, Wrench, Flyway, version-controlled DDL" }, { q: "What is the purpose of Dataflow's 'drain' operation and when would you use it instead of 'cancel'?", opts: ["Drain and cancel are identical — both immediately stop the job", "Drain gracefully stops a Dataflow job: it stops accepting new input from sources (Pub/Sub) but completes processing of all in-flight elements and finalizes all open windows before stopping. Cancel immediately terminates the job, potentially losing in-flight data. Use drain when data loss is unacceptable", "Cancel is safe; drain is for debugging purposes only", "Drain is only available for batch Dataflow jobs; cancel is for streaming"], ans: 1, explain: "Dataflow drain vs cancel: Drain = graceful shutdown (stops reading new messages, processes in-flight data, commits all state, terminates cleanly). Cancel = immediate termination (in-flight data is lost, windows are not finalized). Always drain streaming jobs when possible; use cancel only for immediate shutdown when data loss is acceptable.", concept: "Dataflow drain vs cancel, graceful shutdown, in-flight data" }, { q: "A data team needs to monitor the lag between when events occur and when they appear in BigQuery (end-to-end pipeline latency). Which approach provides this measurement?", opts: ["Compare BigQuery table update times with the current clock — this measures pipeline latency", "Embed event_created_at in each event; compare event_created_at with bigquery_inserted_at (ingestion time) per event in BigQuery: SELECT AVG(TIMESTAMP_DIFF(ingquery_inserted_at, event_created_at, SECOND)) as avg_latency FROM events WHERE DATE = CURRENT_DATE", "Use Cloud Monitoring Dataflow metrics — they show end-to-end latency automatically", "Use Pub/Sub message age metric — it shows latency from Pub/Sub to BigQuery"], ans: 1, explain: "End-to-end latency measurement: include event_timestamp (when the event occurred) in each message. Record the BigQuery insertion time (CURRENT_TIMESTAMP() in a Dataflow transform or TIMESTAMP at insert). The difference is end-to-end pipeline latency. Query this in BigQuery daily to track latency SLA compliance.", concept: "End-to-end pipeline latency measurement, event timestamps, BigQuery monitoring" }, { q: "You need to implement automated schema comparison between two BigQuery tables (source and destination) to detect drift. Which approach works?", opts: ["Manually compare schemas in the BigQuery UI after every deployment", "Query INFORMATION_SCHEMA.COLUMNS for both tables; JOIN on column_name and compare data_type, is_nullable, mode. Use a Cloud Function triggered by Pub/Sub schema change notifications or scheduled via Cloud Scheduler. Alert via Cloud Monitoring when drift is detected", "Use BigQuery table clone and compare — clones have identical schemas", "Use dbt source freshness checks to detect schema drift"], ans: 1, explain: "Schema drift detection: SELECT s.column_name, s.data_type as src_type, d.data_type as dest_type FROM src.INFORMATION_SCHEMA.COLUMNS s FULL OUTER JOIN dest.INFORMATION_SCHEMA.COLUMNS d ON s.column_name = d.column_name WHERE s.data_type != d.data_type OR s.column_name IS NULL OR d.column_name IS NULL. Run this as a scheduled query with alerting.", concept: "Schema drift detection, INFORMATION_SCHEMA, automated schema comparison" }, { q: "A Spark job on Dataproc uses broadcast joins for a 500 MB lookup table. After a schema change, the table grew to 2 GB. The job is now failing with broadcast timeout errors. What should you fix?", opts: ["Increase spark.broadcast.timeout to 600 seconds", "Increase spark.sql.autoBroadcastJoinThreshold from default (10 MB) to above 2 GB (2147483648 bytes); or increase spark.broadcast.timeout. Alternatively, if 2 GB is too large for worker memory, convert the broadcast join to a sort-merge join by setting threshold below 2 GB to force sort-merge", "Reduce the lookup table back to 500 MB by removing recent additions", "Switch to Cloud Spanner for the lookup table"], ans: 1, explain: "Spark broadcast join threshold: the table must fit within spark.sql.autoBroadcastJoinThreshold (default 10 MB). For 2 GB, either: (1) increase threshold and ensure workers have sufficient memory (2+ GB available for broadcast); (2) if memory is insufficient, set threshold < 2 GB to force sort-merge join (slower but memory-safe).", concept: "Spark broadcast join, autoBroadcastJoinThreshold, sort-merge join, join strategy" }, { q: "How do you implement a daily SLA report that shows which BigQuery pipelines completed on time vs. late over the past 30 days?", opts: ["Manually check each pipeline's logs daily", "Query INFORMATION_SCHEMA.JOBS_BY_PROJECT filtering on job_type='QUERY' and job_id patterns matching your pipelines; join with expected completion times from a configuration table; calculate on-time vs late rates. Schedule this as a BigQuery scheduled query writing to a reporting table; build a Looker Studio dashboard on top", "Create a separate monitoring Cloud Function that emails SLA status daily", "Use Cloud Monitoring dashboards — they natively show pipeline SLA compliance"], ans: 1, explain: "SLA reporting from INFORMATION_SCHEMA.JOBS: SELECT job_id, creation_time, end_time, TIMESTAMP_DIFF(end_time, DATE_TRUNC(creation_time, DAY) + INTERVAL expected_hour HOUR, MINUTE) AS minutes_late FROM INFORMATION_SCHEMA.JOBS WHERE DATE(creation_time) >= DATE_SUB(CURRENT_DATE, INTERVAL 30 DAY). Report shows on-time vs. late trends.", concept: "SLA reporting, INFORMATION_SCHEMA.JOBS, pipeline monitoring, compliance dashboard" }, { q: "A Dataflow pipeline reads from a Cloud Storage bucket that is in a VPC Service Controls perimeter. The Dataflow workers are outside the perimeter. What error will they get and how do you fix it?", opts: ["No error — VPC Service Controls does not affect Dataflow Cloud Storage reads", "Workers get 'VPC Service Controls violation' error. Fix: add the Dataflow worker project/service account to the VPC Service Controls perimeter; or configure an ingress rule allowing the Dataflow service agent to access Cloud Storage from outside the perimeter", "Switch to Private Google Access for Dataflow workers — this bypasses VPC Service Controls", "Move the Cloud Storage bucket outside the perimeter"], ans: 1, explain: "VPC Service Controls blocks API access from outside the perimeter. Dataflow workers outside the perimeter cannot access a Cloud Storage bucket inside. Solutions: (1) add the Dataflow project to the same perimeter; (2) configure an ingress policy allowing access from the Dataflow service agent identity; (3) deploy Dataflow workers inside the perimeter.", concept: "VPC Service Controls, Dataflow perimeter access, ingress rules, troubleshooting" }, { q: "You need to ensure that a Cloud Composer DAG is paused automatically outside of business hours (8 AM - 6 PM UTC) to avoid triggering on stale data. How do you implement this?", opts: ["Manually pause and unpause the DAG every morning and evening", "Use two additional DAGs: one scheduled at 8 AM to unpause the main DAG (using the Airflow API or DagTrigger), and one at 6 PM to pause it. Or use a BranchPythonOperator at the start of the main DAG to check the current time and skip all downstream tasks if outside business hours", "Cloud Composer has a built-in business hours schedule setting", "Use Cloud Scheduler to trigger DAG pause/unpause via Airflow REST API at the correct times"], ans: 3, explain: "Cloud Scheduler → Airflow REST API: Cloud Scheduler at 8 AM UTC sends a POST request to Airflow REST API endpoint PATCH /api/v1/dags/{dag_id} with is_paused=false. Cloud Scheduler at 6 PM sends is_paused=true. This automates pause/unpause without additional DAGs or complex branching logic.", concept: "Airflow DAG scheduling, business hours automation, Cloud Scheduler + Airflow REST API" }, { q: "A BigQuery pipeline that runs hourly needs to be rolled back to yesterday's state due to a data quality issue in today's runs. What is the most efficient rollback mechanism?", opts: ["Manually re-insert yesterday's data from a backup export", "Use BigQuery time travel: SELECT * FROM table FOR SYSTEM_TIME AS OF TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 24 HOUR) to query yesterday's state; use CREATE OR REPLACE TABLE AS SELECT ... to restore yesterday's data in a single atomic operation", "Contact Google support to restore the table to yesterday's state", "Re-run the pipeline from yesterday's raw data in Cloud Storage"], ans: 1, explain: "BigQuery time travel rollback: SELECT * FROM table FOR SYSTEM_TIME AS OF '2024-01-14 23:59:59' queries the exact table state. CREATE TABLE restored_table AS SELECT ... writes it to a new table. Then CREATE OR REPLACE TABLE production_table AS SELECT * FROM restored_table performs the atomic rollback.", concept: "BigQuery time travel rollback, data recovery, FOR SYSTEM_TIME AS OF" }, { q: "What is the purpose of Cloud Monitoring's 'Synthetic Monitor' (formerly Uptime Checks) for data pipelines?", opts: ["Synthetic monitors are only for web applications and APIs, not data pipelines", "Synthetic monitors make HTTP/TCP requests to check endpoint availability. For data pipelines, use them to check: (1) the Airflow webserver is accessible; (2) a health-check Cloud Function endpoint that validates key pipeline components; (3) BigQuery API availability. They complement log-based and metric-based monitoring", "Synthetic monitors replace Cloud Logging for data pipeline observability", "Synthetic monitors automatically detect pipeline data quality issues"], ans: 1, explain: "Synthetic monitors (uptime checks) verify external endpoint availability. For pipelines: (1) check Airflow/Composer webserver URL; (2) implement a health-check endpoint (Cloud Function returning 200 if data is fresh, 503 if stale) — the synthetic monitor calls this and alerts on 503. This provides user-experience-level monitoring beyond metric thresholds.", concept: "Cloud Monitoring Synthetic Monitor, uptime checks, pipeline health monitoring" }, { q: "A data team wants to implement blue/green deployment for their dbt transformation pipeline in BigQuery. How should they structure this?", opts: ["dbt does not support blue/green — all models overwrite production tables directly", "Use dbt's target configuration: maintain two dbt targets (blue and green) pointing to separate BigQuery datasets (prod_blue, prod_green). Run dbt against the inactive color; validate results; update a 'current' BigQuery view to point to the validated dataset atomically. The view swap is the cutover", "Create separate BigQuery projects for blue and green environments", "Use BigQuery table clones for blue/green — clones are free until modified"], ans: 1, explain: "dbt blue/green: two target datasets (prod_blue, prod_green). Run dbt against the inactive one; run dbt tests; validate freshness. Cutover: UPDATE a BigQuery authorized view (prod_current) to SELECT from the now-validated dataset. The view swap is instant — no downtime, instant rollback by reverting the view.", concept: "dbt blue/green deployment, BigQuery view swap, zero-downtime promotion, authorized views" }, { q: "You need to implement real-time alerting for Dataflow pipeline errors. A new exception type was added to the pipeline code that is not yet in Cloud Monitoring. How do you create an alert for it?", opts: ["Wait for GCP to add the exception metric to Cloud Monitoring", "Create a Cloud Logging log-based metric: define a filter that matches the exception message in Dataflow worker logs (resource.type='dataflow_step', textPayload=~'MyNewException'); create a Cloud Monitoring alert on this log-based metric", "Use Cloud Debugger to intercept the exception and trigger an alert", "Modify the exception handler to write to a BigQuery error table and alert via scheduled query"], ans: 1, explain: "Log-based metrics: define a metric in Cloud Logging that counts log entries matching a filter (Dataflow worker logs + exception string). Cloud Monitoring creates an alert on this metric. This is the standard pattern for alerting on custom application-level errors not tracked by built-in service metrics.", concept: "Cloud Logging log-based metrics, custom error alerting, Dataflow log monitoring" }, { q: "A data engineering team uses feature flags to enable/disable new pipeline features without code deployment. How do you implement feature flags for BigQuery pipelines?", opts: ["Hardcode feature flags in BigQuery SQL — change them with a new deployment", "Store feature flags in a BigQuery config table or Cloud Firestore; read flags at pipeline startup via a BigQuery scalar function or Cloud Function; conditionally apply pipeline logic based on flag values. Update flags without pipeline redeployment", "Use Airflow Variables in Cloud Composer — store feature flags in Airflow's variable store, accessible from tasks without code changes", "Cloud Composer has built-in feature flag support via the Airflow UI"], ans: 2, explain: "Airflow Variables for pipeline feature flags: store flags in Airflow Variables (Airflow UI → Admin → Variables). In DAG code: flag = Variable.get('enable_new_feature', default_var='false'). The flag can be changed in the Airflow UI without any code deployment, and all subsequent DAG runs pick up the new value.", concept: "Feature flags for data pipelines, Airflow Variables, configuration management" }, { q: "What are the key differences between Cloud Composer's sequential task execution and Airflow's parallel task execution, and how do you configure parallelism?", opts: ["Airflow is always sequential — it executes one task at a time", "Airflow executes independent tasks in parallel (tasks without dependencies on each other). Configure parallelism at three levels: (1) parallelism (global concurrent tasks, default 32); (2) dag_concurrency (concurrent tasks per DAG); (3) max_active_runs_per_dag (concurrent DAG runs). Cloud Composer sets these based on environment size", "Airflow DAGs run tasks in parallel only when explicitly configured with parallel=True on each task", "Cloud Composer requires paid upgrade for parallel task execution"], ans: 1, explain: "Airflow parallelism: tasks with no dependency relationships run simultaneously, limited by: parallelism (global), max_active_tasks_per_dag (per DAG), and worker count. In Cloud Composer 2, configure parallelism in Airflow's configuration section. More workers = more tasks run simultaneously.", concept: "Airflow parallelism, concurrent task execution, Cloud Composer configuration" }, { q: "You need to implement automated BigQuery table documentation where column descriptions are automatically populated from a data dictionary spreadsheet. How do you automate this?", opts: ["Manually copy column descriptions from the spreadsheet to BigQuery table descriptions", "Write a Python script that reads the Google Sheets data dictionary (via Sheets API), then uses the BigQuery client library (bigquery.SchemaField.description or tableservice.patch() to update table schema with descriptions); run via Cloud Function or Cloud Build CI/CD on spreadsheet changes", "Use dbt to read from Google Sheets and populate BigQuery descriptions", "BigQuery auto-generates column descriptions from data patterns"], ans: 1, explain: "Automated documentation pipeline: Google Sheets (data dictionary) → Cloud Function (triggered by Google Sheets webhook or Cloud Scheduler) → BigQuery client library patches table schema with descriptions. This keeps BigQuery column documentation in sync with the business data dictionary without manual copy-paste.", concept: "BigQuery automated documentation, Google Sheets API, schema description management" }, { q: "A Dataproc cluster is underperforming because the default configuration parameters are not optimized for the workload (large-scale SQL analytics). What Dataproc component helps configure Spark parameters automatically?", opts: ["Dataproc Autoscaling tunes Spark parameters automatically", "Dataproc's optional component 'Spark Thrift Server' enables JDBC/ODBC SQL access to Spark but doesn't tune parameters. Use Dataproc's initialization actions to set custom Spark properties in spark-defaults.conf, or use the Dataproc properties parameter (spark:spark.executor.memory, spark:spark.sql.shuffle.partitions, etc.) at cluster creation time", "Dataproc automatic tuning optimizes all parameters — no manual configuration needed", "Use BigQuery instead of Dataproc for SQL analytics — it has automatic optimization"], ans: 1, explain: "Dataproc Spark property configuration: at cluster creation, set spark-defaults.conf properties via gcloud dataproc clusters create --properties spark:spark.sql.shuffle.partitions=2000,spark:spark.executor.memory=8g. For analytics workloads: increase shuffle.partitions (default 200 is often too low); tune executor memory; set dynamic resource allocation.", concept: "Dataproc Spark configuration, spark-defaults.conf, performance tuning at cluster creation" }, { q: "A pipeline writes to multiple Cloud Storage buckets across regions. How do you monitor the total data written per region for cost attribution and compliance?", opts: ["Cloud Storage has no per-region write metrics", "Use Cloud Logging data access logs for Cloud Storage (enabled at project level); create log-based metrics filtered by resource.location to count bytes written per region; export billing data to BigQuery and filter by location for cost attribution", "Cloud Billing automatically provides per-region Cloud Storage breakdowns", "Use gsutil du periodically to measure bucket sizes per region"], ans: 1, explain: "Cloud Storage audit logs capture all write operations with location. Log-based metrics on method_name=storage.objects.create filtered by resource.location provide per-region write counts. For cost attribution, export Cloud Billing to BigQuery and filter by sku.description containing the region name.", concept: "Cloud Storage audit logs, log-based metrics, regional cost attribution" }, { q: "You have a BigQuery dataset with 500 tables. You need to identify which tables haven't been queried in the last 90 days for potential cleanup. Which query provides this?", opts: ["There is no way to track per-table query history in BigQuery", "Query INFORMATION_SCHEMA.TABLE_STORAGE to find table sizes; tables with size > 0 that are not in INFORMATION_SCHEMA.JOBS (last 90 days) are unused", "SELECT table_id, last_modified_time FROM dataset.__TABLES__ — last_modified_time shows last write, not last query time", "Query INFORMATION_SCHEMA.JOBS_BY_PROJECT filtering on referenced_tables and aggregate to find tables with no queries in 90 days: SELECT table_id, MAX(creation_time) FROM INFORMATION_SCHEMA.JOBS_BY_PROJECT, UNNEST(referenced_tables) WHERE DATE(creation_time) >= DATE_SUB(CURRENT_DATE, INTERVAL 90 DAY) GROUP BY table_id — tables absent from results are unqueried"], ans: 3, explain: "INFORMATION_SCHEMA.JOBS_BY_PROJECT logs all queries with referenced_tables (the list of tables each job accessed). UNNEST(referenced_tables) expands them. Tables with no entries in the last 90 days have not been queried — candidates for archival or deletion.", concept: "BigQuery INFORMATION_SCHEMA.JOBS, table usage tracking, data cleanup" }, { q: "What is Dataplex's role in automated data quality management and how do you define quality rules?", opts: ["Dataplex only catalogs data assets — it has no quality rule execution capability", "Dataplex Data Quality provides: (1) auto data profiling (statistical analysis of columns); (2) data quality rules defined declaratively (not_null, value_in_set, min/max range, regex, SQL row conditions); (3) scheduled scans that evaluate rules and write results to BigQuery; (4) Cloud Monitoring metrics and alerts on quality scores", "Dataplex Data Quality requires Spark code to define and run quality checks", "Dataplex Data Quality only works on Cloud Storage files, not BigQuery tables"], ans: 1, explain: "Dataplex Data Quality: define rules in the console or API (row_condition: 'amount > 0', non_null: 'customer_id', regex: 'email LIKE pattern'). Scheduled scans run these rules on BigQuery tables; results (pass/fail rate per rule) appear in a BigQuery results table and Cloud Monitoring. Alert when quality score drops below threshold.", concept: "Dataplex Data Quality, declarative quality rules, scheduled scans, quality monitoring" }, { q: "A Cloud Composer environment fails to start DAG runs due to database saturation (too many metadata operations). How do you diagnose and fix this?", opts: ["Increase Cloud SQL storage for the Airflow metadata database", "Check Airflow's metadata database (Cloud SQL for PostgreSQL) for slow queries and table bloat. Fix: (1) reduce dag_run_conf and xcom sizes; (2) increase xcom_backend to use cloud storage instead of DB; (3) set max_dagruns_per_loop_to_schedule to limit per-loop runs; (4) archive/delete old DagRun and TaskInstance records via db clean command", "Restart the Cloud Composer environment to clear metadata cache", "Reduce the number of DAGs to reduce metadata database load"], ans: 1, explain: "Airflow metadata DB saturation: symptoms include slow scheduling, high Cloud SQL CPU. Fix: (1) archival — airflow db clean command purges old run history; (2) XCom storage — move large XComs to GCS (use custom XCom backend); (3) reduce dag_run history retention; (4) increase Cloud SQL compute tier; (5) reduce parallelism to reduce concurrent DB connections.", concept: "Airflow metadata database, DB saturation, db clean, XCom optimization" }, { q: "How should you implement cross-project BigQuery queries when each project has its own VPC Service Controls perimeter?", opts: ["Cross-perimeter BigQuery queries are not supported — all resources must be in the same perimeter", "Create a unified VPC Service Controls perimeter that includes all projects involved in the cross-project queries; or configure bridge perimeters with ingress/egress access policies between perimeters to allow specific service accounts to cross the perimeter boundary", "Disable VPC Service Controls for queries that need to cross projects", "Use BigQuery Data Transfer Service to copy data between projects instead of cross-project queries"], ans: 1, explain: "VPC Service Controls cross-perimeter access: option 1 is merging perimeters (simpler but broader scope). Option 2 uses ingress/egress rules: create an ingress rule on Project B's perimeter allowing access from Project A's service account via the BigQuery API. This is more fine-grained — only the specific cross-perimeter access is allowed.", concept: "VPC Service Controls cross-perimeter, bridge perimeters, ingress/egress rules" }, { q: "You need to automate the rotation of Cloud SQL database credentials used by data pipelines. What is the recommended approach?", opts: ["Manually update credentials in all pipeline configurations when rotating", "Store credentials in Secret Manager; configure automatic rotation (Cloud Functions triggered by Secret Manager rotation events); pipelines read credentials from Secret Manager at runtime (not hardcoded). On rotation, Secret Manager calls the rotation Cloud Function which updates Cloud SQL and creates a new secret version", "Store credentials in Airflow connections — they auto-rotate", "Rotate credentials manually quarterly and update all pipeline configurations"], ans: 1, explain: "Secret Manager automatic rotation: (1) store Cloud SQL password in Secret Manager; (2) configure rotation schedule (30/90 days); (3) rotation Pub/Sub notification triggers a Cloud Function that: creates new Cloud SQL password, stores it as new Secret Manager version, marks old version for destruction; (4) pipelines always call Secret Manager at startup to get current password.", concept: "Secret Manager, credential rotation, Cloud SQL, automated security" }, { q: "A data pipeline uses a Cloud Function that runs for 9 minutes. You observe it's approaching the maximum timeout. How do you refactor for long-running operations?", opts: ["Cloud Functions supports unlimited execution time — increase the timeout in settings to 60 minutes", "Cloud Functions has a maximum timeout of 9 minutes (2nd gen: 60 minutes for HTTP functions). For operations > 60 minutes: trigger a Cloud Run job (no timeout limit) or a Dataflow job from the Cloud Function. The Cloud Function initiates the long-running operation and returns immediately; the job continues independently", "Increase Cloud Function memory — memory increases automatically extend timeout", "Use Cloud Tasks to break the work into sub-9-minute chunks"], ans: 1, explain: "Cloud Functions timeout limits: 1st gen max 9 minutes; 2nd gen HTTP max 60 minutes. For operations requiring more time: the Cloud Function starts a Cloud Run job, Dataflow job, or Dataproc job and returns immediately (async). The triggered job runs until completion independently. This is the standard pattern for long-running pipeline initiation.", concept: "Cloud Functions timeout, long-running operations, Cloud Run Jobs, async pipeline trigger" }, { q: "You need to implement data masking for development environments so that developers can work with realistic data structures but no real PII. What is the comprehensive approach?", opts: ["Share production data with developers — they have security clearance", "Use Cloud DLP to de-identify a copy of production data: apply tokenization (format-preserving encryption) to PII fields, generalize dates (year only), suppress rare values. Store the de-identified copy in a development BigQuery dataset; developers use this without accessing production", "Create fake data using a data generation tool for all development needs", "Grant developers read-only access to production and rely on code reviews to prevent PII misuse"], ans: 1, explain: "Dev environment data masking: Cloud DLP de-identification on a production snapshot — FPE (format-preserving encryption) for IDs and emails (maintains format, scrambles values), date shifting for dates, bucketing for ages. The result looks realistic for testing but contains no real PII. Store in dev BigQuery dataset; refresh monthly from new production snapshots.", concept: "Dev environment data masking, Cloud DLP de-identification, FPE, realistic test data" }, { q: "What is Vertex AI Pipelines' caching mechanism and how does it reduce pipeline execution time?", opts: ["Vertex AI Pipelines has no caching — all steps re-execute on every run", "Vertex AI Pipelines caches component outputs: if a component's inputs (code, parameters, input artifacts) haven't changed since the last run, it returns the cached output instead of re-executing. This dramatically reduces re-run time for pipelines where early steps (data preprocessing) rarely change but later steps (model training) always change", "Pipeline caching is only available for Python function components, not container components", "Cached results are stored for 24 hours then deleted automatically"], ans: 1, explain: "Vertex AI Pipelines caching: each component execution is fingerprinted (code hash + input hash). On re-run, if the fingerprint matches a recent execution, the cached output artifacts are reused. This allows rapid iteration on later pipeline stages without re-running expensive earlier stages (data preprocessing, feature engineering).", concept: "Vertex AI Pipelines caching, component fingerprinting, pipeline re-run optimization" }, { q: "A production Cloud Composer environment experiences scheduler failures due to too many concurrent task instances. How do you tune this?", opts: ["Add more Composer environments to distribute DAG scheduling", "Reduce Airflow scheduler configuration: decrease parallelism (global concurrent tasks), max_active_tasks_per_dag, and max_active_runs_per_dag to match available worker capacity. Also increase worker count or worker concurrency to match demand. In Composer 2, set these in the Airflow configuration override section", "Delete all historical DAG runs to free scheduler resources", "Reduce the number of DAGs to fewer than 10"], ans: 1, explain: "Composer scheduler tuning: parallelism (total concurrent tasks across all DAGs), max_active_tasks_per_dag (concurrent tasks within a DAG), worker_concurrency (tasks a single worker executes simultaneously). Balance these against worker count. In Composer 2 (GKE Autopilot), workers scale per task — set worker_concurrency appropriately.", concept: "Airflow scheduler tuning, parallelism configuration, worker concurrency, Composer 2" }, { q: "You need to implement an end-to-end observability stack for your data platform covering pipelines, storage, and data quality. Which combination of GCP services provides comprehensive coverage?", opts: ["Cloud Monitoring alone covers all observability needs for GCP data services", "Multi-layer observability: (1) Infrastructure metrics: Cloud Monitoring (Dataflow, Dataproc, Composer metrics); (2) Pipeline logs: Cloud Logging (Airflow task logs, Dataflow worker logs); (3) Data quality: Dataplex Data Quality scans with results in BigQuery; (4) Data freshness: custom Cloud Monitoring metrics from INFORMATION_SCHEMA queries; (5) Cost: Cloud Billing export to BigQuery with pipeline label filtering", "Use a third-party tool (Grafana, Datadog) — GCP's native tools are insufficient for data platforms", "BigQuery INFORMATION_SCHEMA provides all observability data needed"], ans: 1, explain: "Comprehensive data platform observability requires layering: infrastructure metrics (Cloud Monitoring), log analytics (Cloud Logging), data quality (Dataplex), lineage (Dataplex Lineage API), freshness (custom metrics), and cost attribution (Cloud Billing labels). No single service covers all dimensions — they complement each other.", concept: "Data platform observability, Cloud Monitoring, Cloud Logging, Dataplex, multi-layer monitoring" }, { q: "A data pipeline produces results that need to be approved by a business user before being published to a production table. How do you implement a human-in-the-loop approval step in Cloud Composer?", opts: ["Cloud Composer does not support human approval steps — automate all approvals", "Use an Airflow ExternalTaskSensor that waits for a trigger from an external system, or use the Airflow API sensor that polls a BigQuery approval table. Business user approves in a UI that marks the record in BigQuery; sensor detects the approval and continues the DAG", "Use Cloud Composer's built-in human approval task type", "Pause the entire Composer environment during the approval period"], ans: 1, explain: "Human-in-the-loop in Airflow: implement a custom sensor that polls an approval table in BigQuery or Firestore every N minutes. Business user reviews results in a dashboard and clicks Approve → writes a record to the approval table. The sensor detects the approval record and returns True, allowing downstream tasks to run.", concept: "Airflow human-in-the-loop, custom sensors, approval workflows, ExternalTaskSensor" }, { q: "How do you ensure that sensitive BigQuery data is never written to Cloud Logging (Airflow logs may contain query results)?", opts: ["Sensitive data never appears in Airflow logs — logs only contain task metadata", "Configure Airflow to minimize log verbosity (log_level=WARNING); avoid logging query results in BigQuery operators (set log_output=False); use Cloud Logging exclusions to filter out log entries from Composer tasks that may contain sensitive data; apply DLP scan on Cloud Logging to detect and redact PII in logs", "Disable Cloud Logging for Cloud Composer entirely", "Use VPC Service Controls to prevent log data from leaving the perimeter"], ans: 1, explain: "Preventing sensitive data in logs: (1) set Airflow log level to WARNING or ERROR — INFO logs often include SQL results; (2) configure BigQueryExecuteQueryOperator with log_output=False; (3) add Cloud Logging exclusion filters for specific Composer namespaces; (4) implement a Cloud Logging sink with DLP inline inspection to redact PII before logs are stored.", concept: "Sensitive data in logs, Airflow log configuration, Cloud Logging DLP, log redaction" }, { q: "What is the recommended approach for managing secrets (API keys, database passwords) used by Cloud Composer DAGs?", opts: ["Store secrets in Airflow Variables — they are stored in the Airflow metadata database", "Store secrets in Secret Manager; in Airflow DAGs, use the Secret Manager backend for Airflow Connections and Variables (configure airflow.cfg: [secrets] backend = airflow.providers.google.cloud.secrets.secret_manager.CloudSecretManagerBackend). Secrets are fetched from Secret Manager at runtime — never stored in the metadata DB or code", "Hardcode secrets in DAG code files stored in Cloud Storage", "Store secrets in environment variables on Dataproc clusters"], ans: 1, explain: "Cloud Composer + Secret Manager: configure Composer to use Secret Manager as the Airflow secrets backend. Airflow Connections and Variables prefixed with airflow-variables- and airflow-connections- in Secret Manager are automatically fetched at runtime. This keeps secrets out of the metadata DB, encrypted at rest, with full audit logging and rotation support.", concept: "Secret Manager, Airflow secrets backend, Cloud Composer secret management" }, { q: "A data team needs to ensure that no Dataflow job in their organization can read from or write to resources outside of a specific GCP project list. How do you enforce this policy?", opts: ["Add Firewall rules to all Dataflow worker VPCs", "Configure an Organization Policy constraint (gcp.resourceLocations or a custom OPA/Forseti policy) restricting Dataflow job network configuration to specific VPCs within approved projects; combine with VPC Service Controls perimeters that include only the approved projects", "Review all Dataflow job configurations manually before deployment", "Use Workload Identity Federation to restrict Dataflow service account access to specific projects"], ans: 1, explain: "Organization Policy + VPC Service Controls for Dataflow resource restriction: VPC SC perimeters define which projects can communicate via GCP APIs. Dataflow workers within the perimeter can only access services also in the perimeter. Organization Policies (constraints/compute.restrictVpnPeerIPs, etc.) further restrict network access.", concept: "Organization Policy, VPC Service Controls, Dataflow resource restrictions, enterprise governance" }, { q: "You need to monitor the health of a Pub/Sub subscription to ensure messages are being consumed in a timely manner. Which metric indicates consumption health?", opts: ["pubsub.googleapis.com/topic/send_message_operation_count — counts published messages", "pubsub.googleapis.com/subscription/num_undelivered_messages (backlog) and subscription/oldest_unacked_message_age — a growing backlog or increasing age indicates the subscriber is falling behind. Alert when oldest_unacked_message_age exceeds your processing SLA", "pubsub.googleapis.com/subscription/pull_request_count — counts consumer requests", "pubsub.googleapis.com/topic/byte_cost — total bytes consumed"], ans: 1, explain: "Pub/Sub subscription health: num_undelivered_messages (backlog) shows unprocessed messages; oldest_unacked_message_age shows how old the oldest unprocessed message is. A growing backlog with increasing message age means the consumer is behind. Alert when message age > your max acceptable lag (e.g., 5 minutes).", concept: "Pub/Sub subscription monitoring, backlog metrics, oldest_unacked_message_age, consumer health" }, { q: "A Dataflow pipeline needs to be tested before deployment. How do you run a subset of production data through the pipeline locally without incurring Dataflow costs?", opts: ["Dataflow jobs must always run in the cloud — local testing is not supported", "Use the Apache Beam DirectRunner: run the pipeline locally with a small sample of data from Cloud Storage or BigQuery. The DirectRunner executes all Beam transforms locally on your machine (or CI/CD agent) without Dataflow infrastructure. Use pytest with the DirectRunner for automated unit and integration tests", "Use Dataflow's simulation mode (--simulate_shuffle flag)", "Test only the individual DoFn functions in isolation — no full pipeline testing needed"], ans: 1, explain: "DirectRunner for local testing: PipelineOptions.fromArgs(['--runner=DirectRunner']). The pipeline runs locally on JVM/Python interpreter. Use a sample BigQuery table (TABLESAMPLE or LIMIT 1000) or small Cloud Storage files as test input. DirectRunner supports all Beam features and produces identical logical results to DataflowRunner.", concept: "Apache Beam DirectRunner, local pipeline testing, CI/CD pipeline validation" }, { q: "You need to implement automatic remediation when a Dataplex data quality check fails (e.g., rows with NULL customer_id should be moved to a quarantine table). How do you automate this?", opts: ["Manually review Dataplex scan results and move bad rows each time a scan fails", "Configure Dataplex Data Quality scan to write results to a BigQuery table; create a Cloud Monitoring alert on the Dataplex data quality metric; alert triggers a Pub/Sub notification → Cloud Function that queries the failed rows (using the scan result partition info) from the source table → inserts them into the quarantine table → optionally deletes from source", "Dataplex automatically moves bad rows to quarantine — no configuration needed", "Use a Cloud Composer DAG to run Dataplex scans and handle remediation"], ans: 1, explain: "Automated DQ remediation: Dataplex scan → writes results to BigQuery → Cloud Monitoring alert on failing rules → Pub/Sub → Cloud Function reads the scan result row filter (which rows failed which rules) → queries source BigQuery table for those rows → writes to quarantine_table → optionally DELETEs from source. Full automation from scan to remediation.", concept: "Dataplex data quality remediation, automated quarantine, Cloud Monitoring alerts, Cloud Functions" }, { q: "A data team runs 500 BigQuery jobs per day. They want to identify the top 10 most expensive jobs by slot consumption in the last 7 days. Which query achieves this?", opts: ["Query the Cloud Billing export in BigQuery — it has per-job cost data", "SELECT job_id, total_slot_ms, total_bytes_processed, user_email FROM `project`.`region-us`.INFORMATION_SCHEMA.JOBS WHERE DATE(creation_time) >= DATE_SUB(CURRENT_DATE, INTERVAL 7 DAY) AND state = 'DONE' ORDER BY total_slot_ms DESC LIMIT 10", "Query Cloud Monitoring for BigQuery slot metrics — it tracks per-job consumption", "Use the BigQuery UI's Recent Jobs view — sort by duration to find expensive jobs"], ans: 1, explain: "INFORMATION_SCHEMA.JOBS contains per-job statistics: total_slot_ms (slot time = cost proxy for on-demand), total_bytes_processed, user_email, creation_time, job_id. Sort by total_slot_ms DESC LIMIT 10 gives the 10 most resource-intensive jobs. Use this for cost attribution, optimization targeting, and identifying runaway queries.", concept: "BigQuery INFORMATION_SCHEMA.JOBS, job cost analysis, top expensive queries" }, { q: "How do you implement a rolling deployment for a Dataform project — deploying to 10% of users first, then 100% — in a BigQuery context?", opts: ["Dataform does not support partial deployment", "Use feature flags: maintain a user_cohort field; deploy Dataform models that JOIN to a feature_flags table to apply new logic only for cohort 1 (10% of users). After validation, expand the cohort to 100%. This is a SQL-level A/B deployment without infrastructure changes", "Deploy to a separate BigQuery project for 10% of users", "Use BigQuery TABLESAMPLE to randomly route 10% of queries to the new model"], ans: 1, explain: "SQL-level rolling deployment: add a user_cohort or ab_test_flag to your user dimension. Deploy new Dataform models with: CASE WHEN user_cohort = 'experiment' THEN new_logic ELSE old_logic END. Only users in the experiment cohort see new results. Gradually expand cohort size from 10% to 100% via a config table update — no new deployment needed.", concept: "Dataform rolling deployment, A/B testing in SQL, feature flags, progressive rollout" }] }

      { id: 1, title: "Designing Data Processing Systems", weight: "22%", color: "#1a73e8", questions: [{ q: "Your organization needs to process 10 TB of clickstream data daily. The analytics team wants sub-second query performance on aggregated data, while the ML team needs raw event data for feature engineering. Which architecture best meets both requirements?", opts: ["Stream data to BigQuery only; use materialized views for aggregations", "Use Pub/Sub → Dataflow → write to both BigQuery (processed) and Cloud Storage (raw parquet)", "Load raw data to Cloud SQL; export aggregates to BigQuery nightly", "Use Dataproc Spark to process and write to Bigtable for all use cases"], ans: 1, explain: "A Lambda-style architecture using Pub/Sub → Dataflow serves both needs: BigQuery receives transformed/aggregated data for sub-second analytics, while Cloud Storage stores raw parquet for ML feature engineering.", concept: "Lambda Architecture, Pub/Sub, Dataflow fan-out patterns" }, { q: "A retail company stores 500 million product catalog records that need millisecond point-lookup by product_id and range scans by category+price. Which storage solution is most appropriate?", opts: ["BigQuery — columnar storage with slot-based querying", "Cloud Bigtable — wide-column NoSQL with row-key design for point lookups", "Cloud Spanner — globally distributed relational database", "Firestore — document store with rich query support"], ans: 1, explain: "Bigtable is optimized for high-throughput, low-latency reads and writes at petabyte scale. By designing a composite row key (category#price_padded#product_id), you enable both point lookups and range scans.", concept: "Bigtable row key design, NoSQL storage selection criteria" }, { q: "Which component of a data processing architecture is responsible for ensuring exactly-once semantics when processing streaming data from Pub/Sub into BigQuery?", opts: ["Pub/Sub message acknowledgment with 7-day retention", "Dataflow's built-in idempotent writes with checkpointing", "BigQuery streaming inserts with deduplication window", "Cloud Composer DAG with retry logic"], ans: 1, explain: "Apache Beam on Dataflow provides exactly-once processing guarantees through checkpointing, state management, and idempotent sink writes. Pub/Sub guarantees at-least-once delivery; combining it with Dataflow's checkpointing achieves end-to-end exactly-once semantics.", concept: "Exactly-once semantics, Dataflow checkpointing, Beam windowing" }, { q: "Your company's data lake stores data in Cloud Storage. You need to design a system that automatically classifies PII data, applies column-level security, and prevents unauthorized exports. Which combination of services addresses all three requirements?", opts: ["Cloud DLP for classification, BigQuery column-level security, VPC Service Controls for export prevention", "IAM conditions for classification, Cloud Armor for security, Cloud Audit Logs for monitoring", "Data Catalog tags for classification, Cloud KMS for security, Cloud NAT for export prevention", "Dataproc for classification, Dataplex for security, Firewall rules for export prevention"], ans: 0, explain: "Cloud DLP scans and classifies PII automatically. BigQuery's column-level security restricts access to sensitive columns. VPC Service Controls creates a security perimeter preventing data exfiltration.", concept: "Cloud DLP, BigQuery column security, VPC Service Controls" }, { q: "A financial firm needs to join real-time transaction streams (from Pub/Sub) with historical reference data (200 GB customer dimension table in BigQuery) for fraud detection. Joins must complete within 500ms. What is the recommended approach?", opts: ["Dataflow pipeline with side inputs — load the dimension table as a side input at pipeline startup", "Join in BigQuery using streaming inserts + scheduled queries every minute", "Use Dataproc Structured Streaming with BigQuery connector for dynamic joins", "Cloud Functions triggered by Pub/Sub that query BigQuery per message"], ans: 0, explain: "Dataflow side inputs allow a bounded dataset to be broadcast to all workers and held in memory, enabling sub-millisecond in-pipeline lookups against the streaming data.", concept: "Dataflow side inputs, stream-to-batch joins, pipeline patterns" }, { q: "You are designing a data pipeline where upstream teams may change source schema by adding columns. Your pipeline must not break when new columns are added and must make new fields available downstream within 1 hour. Which schema management strategy is correct?", opts: ["Use a strict Avro schema registry; reject messages that don't match the registered schema", "Use BigQuery schema auto-detection with NULLABLE new columns; configure Dataflow to pass-through unknown fields", "Maintain a fixed schema in Cloud SQL and transform all data to match it", "Use JSON-only storage and never enforce schemas"], ans: 1, explain: "BigQuery supports schema evolution: new NULLABLE columns can be added without breaking existing queries. Configuring Dataflow to pass through unknown fields means new columns flow through automatically.", concept: "BigQuery schema evolution, Beam dynamic schemas, backward compatibility" }, { q: "Which statement accurately describes when to use Dataflow vs. Dataproc for a new data processing workload?", opts: ["Dataproc is preferred when the team uses Apache Spark/Hadoop libraries or has existing Spark code; Dataflow is preferred for new serverless streaming/batch pipelines using Apache Beam", "Dataflow only supports batch processing; Dataproc handles all streaming use cases", "Dataproc is always cheaper than Dataflow for large batch jobs", "Dataflow requires managing cluster autoscaling; Dataproc is fully serverless"], ans: 0, explain: "Dataproc is the right choice when leveraging the Spark/Hadoop ecosystem. Dataflow (Apache Beam) excels for unified stream+batch pipelines with managed autoscaling and exactly-once semantics.", concept: "Dataflow vs Dataproc decision criteria, Apache Beam" }, { q: "A pipeline must process 1 billion small IoT messages per day where each message is only 200 bytes. The team worries about Pub/Sub per-message costs. What is the most cost-effective ingestion design?", opts: ["Use Pub/Sub Lite with zonal delivery for lower cost per GB", "Batch messages client-side and publish as a single larger message", "Switch to Cloud Tasks for FIFO delivery at lower cost", "Use Dataflow DirectRunner to avoid Pub/Sub entirely"], ans: 0, explain: "Pub/Sub Lite offers significantly lower per-GB costs compared to standard Pub/Sub by using pre-provisioned throughput capacity with zonal delivery.", concept: "Pub/Sub Lite, Pub/Sub cost optimization, IoT ingestion patterns" }, { q: "Your organization is building a data mesh. Each domain team owns its data products. Which Google Cloud service provides a central catalog for discovering, governing, and tagging data across all domain-owned datasets?", opts: ["Cloud Data Catalog (now Dataplex Catalog)", "BigQuery Information Schema", "Cloud Asset Inventory", "Cloud Monitoring custom metrics"], ans: 0, explain: "Dataplex Catalog is Google Cloud's unified metadata management service. It enables data discovery across BigQuery, Cloud Storage, Pub/Sub; supports business glossaries, custom tags, and data lineage.", concept: "Dataplex, Data Catalog, Data Mesh, metadata management" }, { q: "A streaming pipeline using Dataflow processes late-arriving events. Events can arrive up to 30 minutes late. You are using 1-hour tumbling windows. Which Beam configuration correctly handles late data without losing it?", opts: ["Set allowedLateness(Duration.standardMinutes(30)) with ACCUMULATE_AND_RETRACT firing mode", "Use session windows instead of tumbling windows to auto-handle late data", "Set window size to 90 minutes to absorb all late arrivals", "Late data is automatically handled by Pub/Sub; no Beam configuration needed"], ans: 0, explain: "Beam's allowedLateness tells the pipeline to keep window state alive for 30 minutes past the watermark to accept late elements. ACCUMULATE_AND_RETRACT mode retracts previous results and emits new ones when late data arrives.", concept: "Dataflow windowing, allowedLateness, late data handling, watermarks" }, { q: "Which service should you use to orchestrate a multi-step data pipeline that includes a Dataproc job, a BigQuery transformation, and a Dataflow job, with retry logic and dependency management?", opts: ["Cloud Workflows", "Cloud Composer (Apache Airflow)", "Cloud Scheduler + Cloud Functions", "Dataflow composite transforms"], ans: 1, explain: "Cloud Composer (managed Apache Airflow) is purpose-built for orchestrating complex multi-step workflows with DAG-based dependency management, retries, SLA alerting, and rich operators for GCP services.", concept: "Cloud Composer, Airflow DAGs, pipeline orchestration" }, { q: "Your company must store data in the EU region only for GDPR compliance. A BigQuery dataset was accidentally created in the US. What is the correct remediation?", opts: ["Use BigQuery Data Transfer Service to copy data to a new EU dataset, then delete the US dataset", "Change the dataset region using ALTER DATASET statement in BigQuery", "Enable CMEK on the US dataset — it will satisfy GDPR residency requirements", "Replicate the dataset to EU using Datastream and keep both in sync"], ans: 0, explain: "BigQuery dataset locations cannot be changed after creation. The correct approach is to copy data to a new EU multi-region dataset then delete the incorrectly located dataset.", concept: "BigQuery data residency, GDPR compliance, dataset migration" }, { q: "A data engineer is designing a medallion architecture in BigQuery. Which layer descriptions are correct?", opts: ["Bronze = raw ingested data; Silver = cleaned/validated data; Gold = aggregated business-ready data", "Bronze = aggregated KPIs; Silver = raw data; Gold = ML training sets", "Bronze = real-time data; Silver = batch data; Gold = historical archives", "Bronze = external tables; Silver = materialized views; Gold = native tables"], ans: 0, explain: "The medallion architecture: Bronze holds raw data as received. Silver applies deduplication and validation. Gold contains curated, aggregated, business-logic-applied tables for BI.", concept: "Medallion architecture, data lakehouse, BigQuery best practices" }, { q: "You need to design a system that processes both real-time events and corrects historical data when source records are updated. Which pattern best addresses this requirement?", opts: ["Lambda architecture with separate speed layer (Dataflow streaming) and batch layer (Dataflow batch/BigQuery)", "Only use streaming; historical corrections are not supported in GCP", "Use Cloud Spanner for all data to leverage its MVCC for time-travel corrections", "Store all data in Bigtable and overwrite records as corrections arrive"], ans: 0, explain: "Lambda architecture separates concerns: a speed layer handles real-time events; a batch layer periodically reprocesses historical data from the source of truth, correcting errors.", concept: "Lambda architecture, speed/batch/serving layers, reprocessing" }, { q: "An organization wants to enable data scientists to query data across Cloud Storage (parquet), BigQuery, and Spanner from a single SQL interface without data movement. Which service enables this?", opts: ["BigQuery Omni", "BigQuery federated queries with external tables and Spanner federation", "Dataproc Metastore with Hive federation", "Cloud Data Fusion with a unified query endpoint"], ans: 1, explain: "BigQuery supports federated queries: external tables over Cloud Storage, and native federation with Cloud Spanner via EXTERNAL_QUERY. This allows SQL queries spanning all three sources with no data movement.", concept: "BigQuery external tables, federated queries, Spanner federation" }, { q: "You are designing a data processing system for a healthcare company. Patient data must never leave a specific GCP region, and data access must be logged for HIPAA auditing. Which combination of controls achieves both requirements?", opts: ["VPC Service Controls with regional access levels + Cloud Audit Logs data access logging enabled", "CMEK encryption + Cloud Monitoring dashboards", "Cloud Armor + BigQuery authorized views", "Private IP for all services + Cloud Storage bucket lifecycle policies"], ans: 0, explain: "VPC Service Controls enforces data residency by restricting API access to specific regions. Cloud Audit Logs (data access logs) records every read/write operation for HIPAA compliance audit trails.", concept: "VPC Service Controls, Cloud Audit Logs, HIPAA compliance, data residency" }, { q: "A Kappa architecture differs from Lambda architecture in what fundamental way?", opts: ["Kappa uses only batch processing; Lambda uses only streaming", "Kappa eliminates the batch layer — all processing (including reprocessing) uses a single streaming path; Lambda maintains separate batch and speed layers", "Kappa requires Apache Kafka; Lambda requires Apache Spark", "Kappa is a GCP-specific architecture; Lambda is open-source"], ans: 1, explain: "Kappa architecture simplifies Lambda by using a single stream-processing system for both real-time and reprocessing. To reprocess, you replay historical data through the same streaming pipeline. This reduces operational complexity at the cost of requiring a scalable streaming system for batch-size replays.", concept: "Kappa architecture, streaming reprocessing, Lambda vs Kappa comparison" }, { q: "Your data pipeline must handle both structured (CSV) and semi-structured (JSON with varying schemas) data from the same source. Which BigQuery feature accommodates semi-structured JSON without flattening?", opts: ["JSON string columns with application-level parsing", "BigQuery JSON data type (native JSON column) allowing dot-notation queries on nested keys", "STRUCT columns requiring schema declaration upfront", "External tables with JSON auto-detection"], ans: 1, explain: "BigQuery's native JSON data type stores variable-schema JSON and allows querying with dot-notation (json_col.key.subkey) and JSON functions without flattening. Unlike STRUCT, it doesn't require a fixed schema declaration, making it ideal for semi-structured sources.", concept: "BigQuery JSON type, semi-structured data, schema flexibility" }, { q: "An event-driven architecture uses Eventarc to trigger a Cloud Run service when a BigQuery job completes. What is the correct Eventarc trigger event type for BigQuery job completion?", opts: ["google.cloud.bigquery.job.v1.JobCompleted", "google.cloud.storage.object.v1.finalized", "google.cloud.pubsub.topic.v1.messagePublished", "google.cloud.dataflow.job.v1.statusChanged"], ans: 0, explain: "Eventarc supports BigQuery audit log events. The google.cloud.bigquery.job.v1.JobCompleted event fires when a BigQuery job finishes. This enables event-driven pipelines where downstream processing starts immediately when a BigQuery job completes, without polling.", concept: "Eventarc, event-driven pipelines, BigQuery audit log triggers" }, { q: "You need to process 50 GB files stored in Cloud Storage using Dataflow. The job takes 45 minutes with 10 workers. What is the most effective way to reduce processing time to under 15 minutes?", opts: ["Switch to a Dataproc cluster with more HDFS capacity", "Increase the number of Dataflow workers; Dataflow autoscaling will add workers up to maxNumWorkers", "Compress the input files to reduce I/O time", "Upgrade to n2-highmem machine type for all workers"], ans: 1, explain: "For CPU-bound or I/O-bound batch Dataflow jobs, increasing worker count is the most direct lever. Set maxNumWorkers to ~30 for 3x throughput. Dataflow automatically distributes work across workers. Compression helps I/O but doesn't reduce compute time proportionally.", concept: "Dataflow horizontal scaling, maxNumWorkers, batch throughput optimization" }, { q: "BigQuery Analytics Hub allows organizations to share datasets. What is the key difference between Analytics Hub and simply granting IAM access to a BigQuery dataset?", opts: ["Analytics Hub is for sharing within a single GCP project only; IAM is for cross-project", "Analytics Hub provides a subscription model where subscribers get a linked dataset that mirrors the publisher's data without copying it; the publisher controls data, subscribers see updates automatically", "Analytics Hub encrypts data differently than standard BigQuery", "Analytics Hub is only for public datasets; private sharing requires IAM"], ans: 1, explain: "Analytics Hub creates linked datasets: subscribers see a read-only view that always reflects the publisher's current data. No data copy occurs — storage costs stay with the publisher. The publisher can revoke access centrally. This is fundamentally different from IAM grants, which allow direct table access.", concept: "Analytics Hub, data sharing, linked datasets, no-copy architecture" }, { q: "A Dataflow pipeline writes to BigQuery using the BigQueryIO sink. You observe duplicate rows in the output table. The pipeline uses at-least-once mode. How do you eliminate duplicates?", opts: ["Add a DISTINCT query after pipeline completion", "Switch BigQueryIO to use the Storage Write API in committed mode (STORAGE_WRITE_API with withNumStorageWriteApiStreams) for exactly-once guarantees", "Add a deduplication ParDo using GroupByKey on a unique key", "Enable BigQuery streaming insert deduplication (best-effort, 1-minute window)"], ans: 1, explain: "The BigQuery Storage Write API in committed mode provides exactly-once semantics through atomic stream commits. Switching from legacy streaming inserts to STORAGE_WRITE_API eliminates duplicates at the write level without post-processing deduplication logic.", concept: "BigQuery Storage Write API, exactly-once writes, deduplication" }, { q: "Which design pattern should be used when a Dataflow pipeline needs to enrich each event with data from a constantly-updating reference dataset (updated every 5 minutes)?", opts: ["Load the reference data once as a side input at pipeline start; restart the pipeline every 5 minutes", "Use Dataflow's watchForNewFiles() to continuously reload the reference data as a side input using View.asSingleton() with periodic refresh", "Store reference data in BigQuery and query per-element (one BigQuery call per event)", "Cache reference data in Cloud Memorystore (Redis) and call from within the ParDo"], ans: 1, explain: "Dataflow supports updatable side inputs through the watchForNewFiles pattern with PCollectionView. When the reference file changes, the view refreshes automatically without restarting the pipeline. This is the correct pattern for slowly-changing reference data in long-running streaming pipelines.", concept: "Dataflow updatable side inputs, slowly-changing data, streaming pipeline patterns" }, { q: "Your organization processes financial transactions. Auditors require the ability to query the exact state of any BigQuery table at any point in the past 6 months. What BigQuery feature supports this and what configuration is needed?", opts: ["BigQuery snapshots — manually create a snapshot every day", "BigQuery time travel configured to max_time_travel_hours=4380 (6 months); use FOR SYSTEM_TIME AS OF for historical queries", "BigQuery table clones that retain full history", "Incremental BigQuery exports to Cloud Storage for historical reconstruction"], ans: 1, explain: "BigQuery time travel retains historical table data for up to 7 days by default, but can be extended up to 6 months (168–4380 hours) via table or dataset settings. FOR SYSTEM_TIME AS OF 'TIMESTAMP' queries the exact table state at that time — without manually maintaining snapshots.", concept: "BigQuery time travel, historical queries, extended retention" }, { q: "When would you choose Pub/Sub message ordering keys, and what constraint does enabling ordering impose?", opts: ["Ordering keys guarantee global ordering across all topics; no constraints", "Ordering keys guarantee in-order delivery of messages with the same key to a single subscriber; enabling ordering requires publishing through a single region endpoint and reduces throughput per key", "Ordering keys are only available for Pub/Sub Lite, not standard Pub/Sub", "Ordering keys enable exactly-once delivery; no ordering constraints apply"], ans: 1, explain: "Pub/Sub ordering keys guarantee that messages with the same key are delivered in the order published to a single subscription. The constraint: all messages with the same key must be published to the same region endpoint. Throughput per ordering key is limited (~1 MB/s). Plan carefully for high-volume keys.", concept: "Pub/Sub ordering keys, message ordering, throughput constraints" }, { q: "A data platform team needs to design storage for user activity events (billions per day). The access patterns are: (1) last 7 days with high frequency, (2) 7-30 days with low frequency, (3) > 30 days archive only. What is the correct multi-tier storage design?", opts: ["Store all data in BigQuery with PARTITION BY date; use partition expiration policies for automatic management", "Use Bigtable for 0-7 days, Cloud Storage Standard for 7-30 days, Cloud Storage Archive for 30+ days", "Store 0-7 days in BigQuery Standard Edition, 7-30 days in BigQuery native storage with long-term pricing, 30+ days in Cloud Storage Coldline as external tables", "Use Redis for 0-7 days, Cloud SQL for 7-30 days, BigQuery for 30+ days"], ans: 0, explain: "BigQuery partition expiration automatically manages data lifecycle. Configure partition expiration on the table; separately, set table-level time partitioning. For pure cold archival, BigQuery long-term pricing kicks in after 90 days of no modification. Single-tier BigQuery with partitioning is simpler to manage than three separate systems.", concept: "BigQuery partition expiration, data tiering, storage lifecycle management" }, { q: "You need to expose a BigQuery dataset to external researchers while ensuring they cannot combine it with other data to re-identify individuals. Which technique achieves this privacy guarantee at the data level?", opts: ["Row-level security with researcher email filters", "Apply k-anonymity or differential privacy using Cloud DLP de-identification transformations before sharing", "Use authorized views to show only aggregated results", "Encrypt PII columns with AEAD and share decryption keys only with approved researchers"], ans: 1, explain: "Cloud DLP supports de-identification transformations including k-anonymity (suppress or generalize quasi-identifiers until each combination appears in k rows) and differential privacy noise addition. These provide mathematical privacy guarantees that prevent re-identification, unlike access controls which only limit who sees data.", concept: "Cloud DLP, k-anonymity, differential privacy, de-identification" }, { q: "A streaming Dataflow job has a bottleneck: one ParDo step has 100x more elements to process than others. The step is not parallelizable by key. How do you address this pipeline bottleneck?", opts: ["Increase worker count — Dataflow will automatically redistribute work", "Rewrite the hot step as a batched operation using GroupIntoBatches before the slow ParDo to reduce call overhead", "Split the pipeline into two separate jobs connected via Pub/Sub", "Use Dataflow FlexRS to schedule the job during off-peak hours"], ans: 1, explain: "GroupIntoBatches collects N elements before passing them to the next step, reducing the number of function calls by N. If the bottleneck is per-element overhead (API calls, initialization), batching dramatically reduces that overhead. This is the canonical solution for high-fan-in ParDo bottlenecks.", concept: "Dataflow GroupIntoBatches, pipeline bottlenecks, batching optimization" }, { q: "Cloud Composer 2 uses GKE Autopilot for the Airflow workers. How does this differ from Cloud Composer 1 in terms of resource management?", opts: ["Composer 2 requires manual node pool configuration; Composer 1 is fully managed", "Composer 2 workers scale to zero between tasks and scale up per-task demand on GKE Autopilot; Composer 1 uses pre-allocated VMs", "Composer 2 only supports Airflow 1.x; Composer 1 supports Airflow 2.x", "Composer 2 does not support Kubernetes pod operators; Composer 1 does"], ans: 1, explain: "Composer 2 leverages GKE Autopilot — workers are ephemeral Kubernetes pods that spin up for each task and terminate when done. This means near-zero resource cost between tasks. Composer 1 used pre-allocated GKE nodes that ran continuously regardless of workload.", concept: "Cloud Composer 2, GKE Autopilot, autoscaling workers, resource efficiency" }, { q: "You need to build a real-time leaderboard that shows the top 100 players by score, updated within 1 second of each score change. Millions of players update scores concurrently. Which storage technology is most appropriate?", opts: ["BigQuery with streaming inserts and scheduled queries every second", "Cloud Bigtable with a row key of inverted_score#player_id to enable range scan for top 100", "Cloud Spanner with ORDER BY score DESC LIMIT 100 query", "Firestore with a collection ordered by score field"], ans: 1, explain: "Bigtable excels at this use case: design row key as zero_padded_inverted_score#player_id (e.g., MAX_LONG - score). A single range scan from the beginning of the table returns top players in order. Writes are single-row mutations — extremely fast. Spanner would work but is more expensive; Firestore has Firestore query limitations at millions of concurrent writes.", concept: "Bigtable row key design, leaderboard pattern, range scans, inverted scores" }, { q: "Dataform is used to manage SQL transformations in BigQuery. What are the key advantages of Dataform over raw BigQuery scheduled queries?", opts: ["Dataform supports Python transformations; BigQuery scheduled queries only support SQL", "Dataform provides dependency management between transformations, unit testing, git-based version control, incremental table management, and documentation — none of which BigQuery scheduled queries provide natively", "Dataform is faster than BigQuery scheduled queries for large transformations", "Dataform auto-generates schemas; BigQuery scheduled queries require manual schema definition"], ans: 1, explain: "Dataform (now integrated into BigQuery) provides a full data transformation framework: SQLX files define transformations with ref() dependencies, enabling a DAG of transformations. Built-in testing, incremental strategies (merge/insert_overwrite), documentation, and git integration make it vastly more manageable than raw scheduled queries.", concept: "Dataform, BigQuery transformations, dependency management, SQLX" }, { q: "What is the purpose of Dataproc Metastore and when should you use it?", opts: ["Dataproc Metastore stores Dataproc cluster configuration; use it to share configs between clusters", "Dataproc Metastore is a fully managed Apache Hive Metastore service; use it when multiple clusters (Dataproc, Dataflow, Spark) need to share table metadata, schemas, and partition information", "Dataproc Metastore is a NoSQL store for Dataproc job outputs", "Dataproc Metastore replaces Cloud Storage as the backing store for Dataproc jobs"], ans: 1, explain: "Dataproc Metastore (managed Hive Metastore) stores table schemas, partition metadata, and SerDes for distributed query engines. When multiple Dataproc clusters, Dataflow jobs, or other Spark environments need to share access to the same tables in Cloud Storage, they all point to the same Metastore.", concept: "Dataproc Metastore, Hive Metastore, shared metadata, data lake catalog" }, { q: "A multinational company needs BigQuery datasets to reside in specific regions based on the data subject's country for GDPR. How should datasets be organized?", opts: ["Create a single multi-region EU dataset for all European data", "Create separate datasets per country, each pinned to the closest single-region location (e.g., europe-west1 for Germany)", "GDPR only requires encryption, not specific regional storage — use a single US dataset", "Use BigQuery cross-region replication to maintain copies in all required regions"], ans: 1, explain: "For strict GDPR data residency, create datasets pinned to specific regional BigQuery locations (europe-west3 for Frankfurt/Germany, europe-west2 for London/UK, etc.). Multi-region EU guarantees data stays in Europe broadly but not necessarily in a specific country — insufficient for some member states' laws.", concept: "BigQuery regional datasets, GDPR data residency, single-region vs multi-region" }, { q: "You need to build a data catalog that classifies 10,000 BigQuery tables automatically based on content. Which GCP service provides automated discovery and sensitive data classification?", opts: ["Manual tagging by data stewards using Dataplex catalog UI", "Cloud DLP automatic discovery configured to scan BigQuery datasets; results appear as Data Catalog tags", "BigQuery INFORMATION_SCHEMA scanning with a classification stored procedure", "Cloud Asset Inventory with custom asset types for classification"], ans: 1, explain: "Cloud DLP's Auto Discovery feature can be configured to periodically scan entire BigQuery datasets and projects, detect sensitive data (PII, financial data, etc.), and automatically create Dataplex/Data Catalog annotations. At 10,000 tables, manual tagging is infeasible.", concept: "Cloud DLP Auto Discovery, automated classification, Data Catalog integration" }, { q: "Which pattern correctly implements a BigLake table to allow BigQuery and Dataproc to share access to the same Parquet files in Cloud Storage with unified governance?", opts: ["Create a BigQuery external table pointing to Cloud Storage; Dataproc reads directly from Cloud Storage separately", "Create a BigLake table with a connection resource; grant access through BigLake; both BigQuery SQL and Dataproc (via BigQuery Storage API) read through BigLake, enabling row/column policy enforcement", "Use Dataplex to create a lake zone; BigQuery and Dataproc auto-discover tables", "Create a Bigtable instance that mirrors Cloud Storage content for both engines"], ans: 1, explain: "BigLake decouples storage from compute: data stays in Cloud Storage as Parquet/ORC. Both BigQuery and Dataproc access data through the BigLake table, which enforces row access policies, column masking, and DLP inspection centrally — regardless of which engine is used.", concept: "BigLake, unified storage access, cross-engine governance, Cloud Storage" }, { q: "A streaming pipeline needs to aggregate user sessions. Sessions end when a user is inactive for more than 10 minutes. Which Beam window type is correct for this?", opts: ["Tumbling windows of 10 minutes", "Sliding windows of 10 minutes with 5-minute slides", "Session windows with a gap duration of 10 minutes", "Global window with AfterCount trigger every 10 events"], ans: 2, explain: "Beam session windows are variable-length windows that close when no elements arrive within the gap duration (10 minutes). Each user's session is tracked independently — active users have long windows, inactive users have short windows. This directly models user session behavior.", concept: "Beam session windows, gap duration, user session analysis" }, { q: "You want to migrate 500 TB of data from an on-premises data warehouse to BigQuery with minimal network bandwidth usage. What is the recommended GCP service?", opts: ["gsutil -m cp over a dedicated Cloud Interconnect", "Transfer Appliance — physical hardware shipped to your data center; data loaded offline, then shipped to Google for cloud upload", "BigQuery Data Transfer Service with on-premises connector", "Cloud Storage Transfer Service over public internet"], ans: 1, explain: "For 500 TB, Transfer Appliance is the recommended solution. Google ships a ruggedized NAS device to your data center; you load data locally, then ship it back to Google who uploads directly to Cloud Storage/BigQuery. At typical network speeds, 500 TB over the internet would take weeks.", concept: "Transfer Appliance, large-scale migration, offline data transfer" }, { q: "What is Workload Identity Federation and when would a data engineer use it?", opts: ["A GCP feature for federating BigQuery workloads across projects", "A mechanism allowing workloads running outside GCP (AWS, Azure, on-premises) to authenticate to GCP APIs using their native identity credentials, without creating or managing service account keys", "A way to share service accounts across GCP organizations", "An IAM feature for running multiple workloads under a single service account"], ans: 1, explain: "Workload Identity Federation allows external workloads (GitHub Actions CI/CD, AWS Lambda, Azure Functions, on-premises processes) to exchange their native credentials (AWS IAM role, Azure AD token, OIDC token) for short-lived GCP access tokens. Eliminates the need to store service account keys.", concept: "Workload Identity Federation, keyless authentication, cross-cloud identity" }, { q: "A Dataflow pipeline needs to read from a private Cloud SQL database. The Cloud SQL instance has no public IP. How should the Dataflow workers connect to it?", opts: ["Enable Public IP on Cloud SQL temporarily during pipeline execution", "Configure Dataflow workers to use the same VPC as Cloud SQL; use Private IP connection via Cloud SQL Auth Proxy or direct Private IP", "Use Cloud SQL Federated Queries from BigQuery as an intermediary", "Connect via Cloud VPN from Dataflow worker VPC to Cloud SQL VPC"], ans: 1, explain: "Dataflow workers can be deployed in a specific VPC. If Cloud SQL is in the same VPC (or peered VPC), workers connect via Private IP. The Cloud SQL Auth Proxy handles authentication and SSL. This avoids exposing Cloud SQL to the internet.", concept: "Dataflow VPC configuration, Cloud SQL private connectivity, Cloud SQL Auth Proxy" }, { q: "You need to design a multi-hop data pipeline: Cloud Storage → Dataflow transform → BigQuery staging → dbt transformation → BigQuery production. How should you handle pipeline failures at each hop?", opts: ["Use Cloud Composer to orchestrate all hops with task-level retry and alerting; write idempotent transformations that can be safely rerun", "Run all hops as a single script; restart from the beginning on any failure", "Accept data loss if any hop fails; downstream consumers will request a re-extract", "Use Cloud Scheduler to retry the entire pipeline hourly until it succeeds"], ans: 0, explain: "Cloud Composer orchestrates each step as a task with individual retry logic. Idempotent operations (WRITE_TRUNCATE in BigQuery, dbt idempotent models using incremental with unique keys) ensure that retrying any step doesn't produce duplicates or inconsistencies.", concept: "Pipeline fault tolerance, idempotency, Cloud Composer orchestration, multi-hop pipelines" }, { q: "A company has a hybrid architecture: some processing runs on-premises (Apache Spark) and some in GCP (Dataflow). They want a unified metadata store for both. What is the recommended approach?", opts: ["Manually synchronize table definitions between on-premises Hive and Dataproc Metastore weekly", "Use Dataproc Metastore as the central Hive Metastore; configure on-premises Spark to point to it via Cloud Interconnect/VPN", "Store all metadata in a Cloud SQL MySQL instance; both on-premises and GCP connect to it", "Use BigQuery as the metadata store with external tables representing on-premises data"], ans: 1, explain: "Dataproc Metastore exposes a standard Hive Metastore Thrift API. On-premises Spark clusters can be configured with the Metastore endpoint (accessible via Cloud Interconnect or VPN), enabling both environments to share table definitions, schemas, and partition metadata.", concept: "Dataproc Metastore, hybrid architecture, Hive Metastore federation" }, { q: "What is the primary use case for Dataflow FlexRS (Flexible Resource Scheduling)?", opts: ["Running streaming Dataflow jobs with autoscaling", "Running batch Dataflow jobs at reduced cost by mixing preemptible VMs and scheduling jobs during off-peak hours when GCP has spare capacity", "Scheduling Dataflow jobs to start at a specific time of day", "Running Dataflow jobs with higher priority to guarantee SLA completion time"], ans: 1, explain: "Dataflow FlexRS reduces batch job cost by up to 40% by using a mix of regular and preemptible (Spot) VMs, and scheduling the job when GCP has available capacity. Suitable for batch jobs that can tolerate a flexible start time (up to 6 hours delay) and occasional preemption.", concept: "Dataflow FlexRS, Spot VMs, batch cost optimization" }, { q: "A data pipeline reads from BigQuery and writes results back to BigQuery. During transformation, it processes personal data. How should you ensure DLP de-identification occurs before writing to the output table?", opts: ["Apply DLP de-identification in a post-processing step after BigQuery write", "Add a Dataflow ParDo step in the pipeline that calls Cloud DLP deidentifyContent API on each batch of rows before writing to the output BigQuery table", "Configure BigQuery to auto-apply DLP policies on write", "Use Cloud DLP findings from a scan to manually remove PII after pipeline completes"], ans: 1, explain: "The correct pattern is inline DLP transformation: a Dataflow ParDo calls the Cloud DLP deidentifyContent API with appropriate transformation configs (masking, tokenization, bucketing) before the BigQueryIO write sink. This ensures no PII ever reaches the output table.", concept: "Cloud DLP, Dataflow PII transformation, inline de-identification" }, { q: "You need to query data stored in AWS S3 from BigQuery without moving the data to GCP. Which feature enables this?", opts: ["BigQuery federated queries with the AWS S3 connector", "BigQuery Omni — BigQuery on AWS (BQ Omni) can query S3 data using BigQuery syntax", "Cloud Storage Transfer Service daily sync of S3 to GCS, then query", "Dataflow pipeline reading from S3 via the AWS SDK and writing to BigQuery"], ans: 1, explain: "BigQuery Omni extends BigQuery to run query processing on AWS infrastructure, querying data directly in S3 without moving it to GCP. This enables multi-cloud analytics with centralized BigQuery governance while data stays in AWS.", concept: "BigQuery Omni, multi-cloud analytics, S3 federation" }, { q: "Which Bigtable replication feature allows you to route read traffic to a specific cluster for geographic latency optimization?", opts: ["Bigtable autoscaling routes reads to the least loaded cluster automatically", "Bigtable app profiles — configure a single-cluster routing app profile pointing to the geographically closest cluster; write traffic uses multi-cluster routing", "Bigtable read replicas like Cloud SQL read replicas", "Bigtable zone distribution automatically routes reads to the nearest zone"], ans: 1, explain: "Bigtable app profiles control routing. For latency optimization, create app profiles with single-cluster routing pointing to the cluster nearest the application (e.g., US app → us-central1 cluster, EU app → europe-west1 cluster). This ensures reads are served from the closest cluster.", concept: "Bigtable app profiles, cluster routing, geo-distributed reads" }, { q: "A data pipeline produces output tables that downstream teams depend on. You need to signal when new data is ready for consumption without polling. Which pattern achieves this?", opts: ["Downstream teams schedule queries every 5 minutes to check for new data", "Write a completion record to a Pub/Sub topic or update a BigQuery 'ready' table when the pipeline finishes; downstream systems subscribe to the notification", "Share a shared calendar with expected pipeline completion times", "Use Cloud Scheduler to trigger downstream jobs 1 hour after expected completion"], ans: 1, explain: "Event-driven data contracts: when the pipeline completes, publish a message to Pub/Sub (or update a metadata table). Downstream teams subscribe and trigger their jobs immediately on notification. This eliminates polling overhead, reduces latency, and provides a clear data contract interface.", concept: "Event-driven pipelines, data contracts, Pub/Sub notifications, pipeline dependencies" }, { q: "You need to ingest 10,000 events/second from IoT devices to BigQuery with the lowest possible ingestion latency. Which approach is optimal?", opts: ["Device → Cloud Storage (batch every minute) → BigQuery load job", "Device → Pub/Sub → Dataflow → BigQuery Storage Write API (streaming mode)", "Device → Cloud SQL → BigQuery scheduled query every 5 minutes", "Device → BigQuery legacy streaming inserts directly from the device SDK"], ans: 1, explain: "The standard low-latency IoT ingestion pattern: Pub/Sub buffers device messages reliably (handles spikes), Dataflow processes and transforms, and BigQuery Storage Write API writes at high throughput with exactly-once guarantees. Legacy streaming inserts directly from devices lack buffering and have higher per-message overhead.", concept: "IoT ingestion pipeline, Pub/Sub buffering, Storage Write API, low-latency streaming" }, { q: "A data team needs to implement column-level encryption in BigQuery where certain columns are encrypted at rest and at query time using AEAD. Which approach correctly implements this?", opts: ["Enable CMEK on the BigQuery dataset — all columns are encrypted including individual columns", "Use BigQuery AEAD encryption functions (AEAD.ENCRYPT/AEAD.DECRYPT) with key material from Cloud KMS wrapped keys; store key URIs in a separate key table", "Use Cloud DLP to encrypt individual columns before inserting to BigQuery", "BigQuery does not support column-level encryption; encrypt the entire table with CSEK instead"], ans: 1, explain: "BigQuery's AEAD functions provide deterministic or probabilistic column-level encryption using KEYS.NEW_KEYSET() or KMS-wrapped keys. AEAD.ENCRYPT() stores ciphertext in a BYTES column; AEAD.DECRYPT_STRING() decrypts at query time. Only users with the decryption key can read plaintext.", concept: "BigQuery AEAD encryption, column-level encryption, Cloud KMS key wrapping" }, { q: "What does the BigQuery Editions pricing model (Standard, Enterprise, Enterprise Plus) change compared to the older flat-rate model?", opts: ["Editions only affect storage pricing; compute remains on-demand", "Editions replace the old flat-rate commitment model with tiered compute options using BaselineSlots + autoscaling; Enterprise includes CMEK and VPC Service Controls; Enterprise Plus adds cross-region failover and 1-year/3-year commitments", "Editions are identical to on-demand pricing with different names", "Editions eliminate slot-based pricing entirely — all workloads use serverless compute"], ans: 1, explain: "BigQuery Editions (2023+) restructure reservations: you purchase a baseline slot count at hourly rates and can autoscale up to a max. Standard has no commitments; Enterprise adds CMEK, BQML, and VPC SC; Enterprise Plus adds BI Engine, multi-region failover, and deeper commitment discounts.", concept: "BigQuery Editions, capacity pricing, autoscaling slots, commitment discounts" }, { q: "You need to run a BigQuery ML model for real-time scoring within a Dataflow streaming pipeline. What is the correct architectural pattern?", opts: ["Export the BQML model as a TensorFlow SavedModel and load it into the Dataflow pipeline as a side input", "Call BigQuery ML PREDICT SQL from Dataflow via the BigQueryIO source (not feasible in streaming)", "Export BQML model to Vertex AI; deploy as an online prediction endpoint; call from Dataflow via HTTP in a ParDo", "Train and serve the model directly in Dataflow using TFX"], ans: 2, explain: "For real-time scoring in Dataflow: export the BQML model to Vertex AI Model Registry (bqml export) and deploy as a Vertex AI online prediction endpoint. The Dataflow ParDo calls the Vertex AI endpoint via HTTP for each event. This enables sub-100ms scoring with managed scalability.", concept: "BQML model export, Vertex AI online prediction, Dataflow real-time scoring" }, { q: "A team uses Apache Airflow on Cloud Composer. They find that DAG parsing time is 45 seconds, causing scheduler lag. What is the most effective fix?", opts: ["Increase the Composer environment's CPU and memory — more resources reduce parsing time", "Reduce the number of Python imports at DAG file level; move heavy imports inside functions; use .airflowignore to exclude non-DAG files", "Reduce the number of DAGs from 200 to 50", "Upgrade to Cloud Composer 3"], ans: 1, explain: "DAG parsing runs every 30-300 seconds and imports every Python file in the dags/ folder. Heavy top-level imports (pandas, tensorflow, etc.) dramatically slow parsing. Fix: use lazy imports (move imports inside function bodies), add .airflowignore for non-DAG files, and use DAG factory patterns.", concept: "Airflow DAG parsing performance, lazy imports, .airflowignore, Composer optimization" }, { q: "You need to implement data versioning for a BigQuery table so analysts can reproduce past analyses using the exact dataset that existed on a specific date. Which approach is most scalable?", opts: ["Export the table to Cloud Storage as a CSV snapshot daily", "Use BigQuery table snapshots (CREATE SNAPSHOT TABLE) on a schedule; snapshots store only changed data (deltas) and are queryable directly with SQL", "Create a new table for each day's data with the date as suffix", "Use BigQuery time travel — it provides 7-day versioning without configuration"], ans: 1, explain: "BigQuery table snapshots create a point-in-time reference to a table without physically copying unchanged data (uses copy-on-write). Analysts query snapshots directly with SELECT * FROM snapshot_table. This is more storage-efficient than full copies and more permanent than time travel (which only goes back 7 days).", concept: "BigQuery table snapshots, copy-on-write, data versioning" }, { q: "A data pipeline must process files from Cloud Storage in strict FIFO order. Files arrive throughout the day. Which design achieves ordered processing?", opts: ["Cloud Storage doesn't guarantee file ordering; processing is always parallel", "Use a Cloud Storage notification → Pub/Sub → Cloud Functions that appends the filename to a Cloud Firestore ordered queue; a separate process dequeues and processes in order", "Dataflow can enforce strict per-file ordering automatically", "Use BigQuery load jobs which process files in the order they are specified"], ans: 1, explain: "Cloud Storage and Dataflow process files in parallel without strict ordering. For FIFO: Cloud Storage event → Pub/Sub → Cloud Function writes file metadata to an ordered queue (Firestore, Spanner, or ordering-key Pub/Sub). A single-threaded consumer processes the queue in order.", concept: "FIFO processing, ordered queues, Firestore ordered collections, event-driven design" }, { q: "Which statement about BigQuery slot autoscaling (in Editions) is correct?", opts: ["Autoscaling increases slots without any upper limit based on workload", "Autoscaling scales between the baseline slot count and a configured max slots limit; you are only billed for autoscaled slots when they are actively used", "Autoscaling only works for streaming queries, not batch", "Autoscaling requires manual approval before slots are added"], ans: 1, explain: "BigQuery Editions autoscaling: you define a baseline (always available) and a maximum. When workload exceeds baseline, GCP adds slots up to the maximum automatically. You're billed per-slot-second for autoscaled capacity only when used. This combines cost efficiency with burst capacity.", concept: "BigQuery autoscaling slots, Editions pricing, burst capacity" }, { q: "A financial reporting pipeline fails with a BigQuery error: 'Quota exceeded: Your project exceeded quota for concurrent interactive queries.' What is the correct resolution?", opts: ["Rerun the failed queries; the quota resets every minute", "Migrate reporting workloads to BigQuery Reservations to bypass on-demand quotas; or restructure queries to run sequentially with Cloud Composer", "Reduce query complexity to reduce concurrent quota usage", "Contact GCP support to permanently remove the quota"], ans: 1, explain: "The concurrent interactive query quota (default 100 concurrent) applies to on-demand usage. Solutions: (1) use BigQuery Reservations (slot-based), which have no concurrent query limits; (2) queue queries via Cloud Composer to control concurrency; (3) request a quota increase if the use case requires it.", concept: "BigQuery quotas, concurrent queries, Reservations vs on-demand" }, { q: "A Dataflow job processes sensor data and should trigger an alert when temperature readings exceed 100°C. The alert must fire within 10 seconds of the anomaly. Which Dataflow pattern handles this?", opts: ["Use AfterWatermark trigger in a 10-second tumbling window", "Use a stateful DoFn with a timer that fires after 10 seconds if the max temperature in the state exceeds 100°C", "Write all readings to BigQuery and run an alerting query every 10 seconds", "Use Cloud Monitoring custom metrics — Dataflow writes metrics; alerts fire after 10 seconds"], ans: 1, explain: "Stateful DoFn with timers: a state cell tracks the maximum temperature; a processing-time timer fires after 10 seconds. If the max state exceeds 100°C, fire the alert. This is in-pipeline, sub-second capable alerting without external dependencies.", concept: "Dataflow stateful processing, timer APIs, streaming alerting patterns" }, { q: "How does BigQuery handle query costs for partitioned tables when using wildcard queries like SELECT * FROM project.dataset.table_*?", opts: ["Wildcard queries bypass partition pruning; all partitions are scanned for all matching tables", "BigQuery applies partition pruning on wildcard table queries if you add a _TABLE_SUFFIX filter in the WHERE clause", "Wildcard queries are not supported on partitioned tables", "Wildcard queries cost nothing — they only scan metadata"], ans: 1, explain: "For sharded tables (table_20240101, table_20240102...), wildcard queries scan all matching tables unless you filter: WHERE _TABLE_SUFFIX BETWEEN '20240101' AND '20240131'. This is the partition-pruning equivalent for sharded table designs, limiting scan to only the relevant tables.", concept: "BigQuery wildcard tables, _TABLE_SUFFIX, shard pruning, cost optimization" }, { q: "A data team wants to build a real-time dashboard of streaming events with data guaranteed to be at most 30 seconds stale. They are using Dataflow + BigQuery. What architectural element ensures the freshness SLA?", opts: ["Set Dataflow's streaming parallelism to maximum to reduce processing time", "Use Dataflow streaming with the Streaming Engine enabled; write to BigQuery via Storage Write API streaming mode; dashboards query BigQuery directly — data is available seconds after Dataflow writes it", "Use Dataflow batch jobs triggered every 30 seconds via Cloud Scheduler", "Store data in Bigtable for 30-second freshness; BigQuery queries Bigtable via external tables"], ans: 1, explain: "Dataflow Streaming Engine + Storage Write API (streaming mode) provides end-to-end latency of 2-10 seconds from event ingestion to BigQuery availability. This comfortably meets a 30-second freshness SLA. Enabling Streaming Engine reduces latency and cost vs. the standard shuffle-based approach.", concept: "Dataflow Streaming Engine, Storage Write API, real-time dashboard freshness" }, { q: "You need to copy a BigQuery dataset (100 TB) from one GCP project to another in the same region for environment promotion (dev → prod). What is the fastest and cheapest method?", opts: ["Export to Cloud Storage in the same region, then load into the new project — costs egress + storage", "Use BigQuery dataset copy (bq cp --copy_all_tables or BigQuery Data Transfer Service cross-project copy); in-region copies are free of charge for data transfer", "Use Dataflow to read from source and write to destination BigQuery", "Replicate using Datastream change data capture"], ans: 1, explain: "BigQuery's native copy operation (within the same region) uses Google's internal network and is free for data transfer. The bq cp command or BigQuery Data Transfer Service handles all tables. In-region copies avoid both egress costs and the latency of export/import cycles.", concept: "BigQuery dataset copy, cross-project promotion, in-region copy, zero egress cost" }, { q: "What is the difference between a Dataflow template (Classic) and a Flex Template?", opts: ["Classic templates are faster; Flex Templates are for streaming only", "Classic templates compile pipeline code into a JSON specification at build time — parameters are limited to predefined options. Flex Templates package pipeline code in a Docker container — parameters can be dynamic and evaluated at launch time", "Classic templates support all Beam features; Flex Templates have a limited feature set", "Flex Templates require Java; Classic templates support Python and Java"], ans: 1, explain: "Classic templates freeze the execution graph at compile time. Parameters are passed as runtime values but the graph topology is fixed. Flex Templates run the pipeline code in a Docker container at launch — the full pipeline logic executes in the launcher, enabling dynamic graph construction, custom connectors, and complex parameterization.", concept: "Dataflow Flex Templates vs Classic Templates, dynamic pipeline construction" }, { q: "A pipeline joins two BigQuery tables in Dataflow: a 10 TB fact table and a 1 GB dimension table. The join is taking too long. What optimization should be applied?", opts: ["Run the join in BigQuery SQL instead of Dataflow for better performance", "Load the 1 GB dimension table as a Dataflow side input; all workers cache it in memory and perform local lookups against the fact table stream", "Increase the number of Dataflow workers to parallelize the join", "Partition both tables by the join key before processing"], ans: 1, explain: "Side inputs are ideal when one dataset is small enough to fit in worker memory (1 GB is fine). The dimension table is loaded once per worker at startup; each element of the 10 TB fact table is joined locally without any shuffle. This eliminates network I/O for the join entirely.", concept: "Dataflow side inputs, broadcast join, large-small table join optimization" }, { q: "Datastream is used for CDC from MySQL to BigQuery. After an upstream DBA truncates a table and reloads it, the BigQuery destination has duplicate rows. What causes this and how do you resolve it?", opts: ["Truncate operations are not captured by CDC binary logs; Datastream backfills the full table and merges with existing rows creating duplicates. Fix: configure Datastream with merge mode (not append) to upsert rows by primary key", "Datastream does not support MySQL TRUNCATE operations at all", "The duplicates are caused by BigQuery deduplication lag; wait 1 hour for BigQuery to resolve them", "Disable Datastream CDC and use full load daily instead"], ans: 0, explain: "TRUNCATE + reload creates a full table scan backfill in Datastream. In append mode, old rows + new rows both appear. Using Datastream's merge delivery mode (with primary key upsert), rows are upserted in BigQuery — duplicate primary keys are overwritten, not appended.", concept: "Datastream CDC, merge mode, TRUNCATE handling, upsert patterns" }, { q: "A company needs to implement a data hub where multiple teams publish and subscribe to datasets. Publishers should not know who the subscribers are. Which GCP pattern achieves this decoupled architecture?", opts: ["Use BigQuery authorized views shared directly between publisher and subscriber projects", "Analytics Hub — publishers list data exchanges; subscribers subscribe to listings as linked datasets without direct IAM grants from publisher to subscriber", "Use Cloud Storage public buckets for all shared data", "Create a central project with IAM bindings for every publisher-subscriber pair"], ans: 1, explain: "Analytics Hub creates a marketplace model: publishers create listings in an exchange; subscribers discover and subscribe without the publisher managing individual IAM grants. The publisher simply maintains the exchange listing; Analytics Hub handles the linked dataset provisioning for each subscriber.", concept: "Analytics Hub, data marketplace, decoupled data sharing, linked datasets" }, { q: "A streaming Dataflow pipeline has been running for weeks and is accumulating excessive state in timer/state cells. The job memory is growing steadily. What is the correct diagnosis and fix?", opts: ["State accumulation is normal; Dataflow automatically garbage-collects old state", "Timers or state cells are not being cleared when processing is complete. Fix: ensure state.clear() is called when a session ends; or set a GC timer that clears stale state entries", "The job needs more workers to handle the increased state", "Disable the Streaming Engine to revert to standard shuffle-based state management"], ans: 1, explain: "In stateful streaming processing, developers must explicitly clear state when it is no longer needed (e.g., when a session ends, clear all associated state cells). Without explicit cleanup, state grows indefinitely. Setting a GC timer that fires after a maximum session length and calls state.clear() prevents unbounded growth.", concept: "Dataflow stateful processing, state GC, timer cleanup, memory management" }, { q: "A pipeline must process CSV files from a vendor who uses a non-standard delimiter (pipe |) with no header row and with some rows having extra delimiters due to data quality issues. How should you handle this in Dataflow?", opts: ["Use BigQuery load job with CSV format — it handles all delimiter variations automatically", "Write a custom DoFn in Dataflow that reads each line as a string and applies custom parsing logic with error handling for malformed rows", "Use Cloud Data Fusion with a CSV Reader plugin configured for pipe delimiter", "Use gsutil to preprocess files and replace | with , before Dataflow reads them"], ans: 1, explain: "Custom parsing logic in a DoFn gives full control: parse the pipe-delimited line, handle extra delimiters, route malformed rows to a dead-letter output (using tagged outputs). This is the most robust approach for non-standard and dirty input files.", concept: "Dataflow custom DoFn, CSV parsing, dead-letter pattern, tagged outputs" }, { q: "What is the primary role of the Dataflow Shuffle Service?", opts: ["It manages Pub/Sub message routing for streaming pipelines", "It offloads GroupByKey shuffle operations to a managed, scalable GCP service — reducing worker memory pressure and enabling faster autoscaling during batch pipeline execution", "It monitors Dataflow job progress and triggers autoscaling events", "It handles encryption and compression of pipeline data in transit"], ans: 1, explain: "The Dataflow Shuffle Service moves GroupByKey (shuffle) data off workers into a managed GCP-side service. Benefits: workers use less memory (no local shuffle buffers), autoscaling is faster (no data to redistribute between workers), and jobs are more reliable. Enabled via --experiments=shuffle_mode=service.", concept: "Dataflow Shuffle Service, GroupByKey optimization, managed shuffle" }, { q: "A BigQuery ML model achieves 95% accuracy on training data but only 72% on new data. What is the likely issue and solution?", opts: ["The model is underfitting — increase model complexity", "The model is overfitting — apply L1/L2 regularization (l1_reg, l2_reg options in CREATE MODEL), or reduce model complexity, or use more training data", "The training data has schema errors — re-run with schema auto-detection", "BigQuery ML cannot generalize to new data; use Vertex AI instead"], ans: 1, explain: "95% train / 72% test accuracy is a classic overfitting signature. BQML supports regularization: SET OPTIONS(l1_reg=0.1, l2_reg=0.1) in CREATE MODEL. Also consider: increase training data size, use data_split_method='AUTO_SPLIT' to ensure proper train/test split, and reduce feature count if using many high-cardinality features.", concept: "BigQuery ML overfitting, regularization, L1/L2, model generalization" }, { q: "Which combination of IAM roles grants a service account the minimum permissions needed to run Dataflow jobs and read from Pub/Sub?", opts: ["roles/owner on the project", "roles/dataflow.worker + roles/pubsub.subscriber on the Pub/Sub subscription; the Dataflow service account also needs roles/dataflow.worker and roles/storage.objectAdmin on the GCS temp bucket", "roles/editor on the project", "roles/dataflow.admin + roles/bigquery.admin"], ans: 1, explain: "Principle of least privilege for Dataflow: the worker service account needs roles/dataflow.worker (to execute jobs), roles/pubsub.subscriber on the specific subscription (to consume messages), and roles/storage.objectAdmin on the temp/staging bucket. Never use roles/editor or roles/owner for service accounts.", concept: "Dataflow IAM, least privilege, service account permissions" }, { q: "How does Dataplex organize data governance? What are the three levels of the hierarchy?", opts: ["Projects → Datasets → Tables", "Lakes → Zones → Assets — where a Lake is a top-level data management domain, Zones are data quality tiers (Raw/Curated), and Assets are the actual storage resources (GCS buckets, BigQuery datasets)", "Organizations → Folders → Projects", "Catalogs → Databases → Tables"], ans: 1, explain: "Dataplex hierarchy: Lake (domain/business area, e.g., 'Marketing') → Zone (data quality tier: Raw Zone for unvalidated data, Curated Zone for validated data) → Asset (Cloud Storage bucket or BigQuery dataset). This maps organizational data domains to technical governance policies.", concept: "Dataplex hierarchy, lakes, zones, assets, data governance" }, { q: "A data pipeline must comply with a regulatory requirement that all data transformations be logged with before/after values for audit purposes. Which design implements this in GCP?", opts: ["Enable BigQuery Admin Activity audit logs only", "Implement custom audit logging in each transformation step: write before/after records to a dedicated BigQuery audit table as part of each transformation transaction; enable Data Access audit logs at the GCP project level", "Use Cloud Monitoring to capture all BigQuery DML operations", "GCP provides automatic transformation logging; no custom implementation needed"], ans: 1, explain: "For regulatory transformation auditing: (1) enable Data Access audit logs in GCP (captures who ran which SQL and when); (2) implement custom before/after logging in transformation code (write to an audit BigQuery table). This two-layer approach provides both automatic API-level logging and business-level data change logging.", concept: "Audit logging, Cloud Audit Logs, custom transformation logging, compliance" }, { q: "Your team needs to run exploratory data analysis (EDA) on a 50 TB BigQuery dataset cost-effectively. Analysts run many ad-hoc queries that often scan large portions of the table. What is the most effective cost-control measure?", opts: ["Create a smaller 1 TB sample table and restrict analysts to it", "Enable maximum bytes billed per query on each analyst's connection; set project-level maximum bytes billed; require analysts to use LIMIT clauses and preview before executing", "Switch all analysts to BigQuery Reservations with a shared slot pool", "Use BigQuery views instead of tables — views are free to query"], ans: 1, explain: "The maximum bytes billed setting (set via connection property or project quota) prevents runaway queries from scanning the full 50 TB. Analysts must preview estimated scan size before executing. Combining this with proper partitioning/clustering on the table ensures cost-conscious EDA.", concept: "BigQuery cost controls, maximum bytes billed, EDA cost management" }, { q: "A Dataflow pipeline uses Pub/Sub as source and BigQuery as sink. You need to test this pipeline without consuming real production Pub/Sub messages or writing to the production BigQuery table. How do you structure the test?", opts: ["Deploy to production with a --dry-run flag", "Use TestStream in the Beam DirectRunner: TestStream simulates a Pub/Sub-like source with pre-defined events and timing; use a BigQuery test dataset or BigQueryIO.write().to(testTable) in the test", "Use a separate GCP project as a staging environment and replay production messages", "Mock the Beam sources and sinks at the Java/Python class level"], ans: 1, explain: "Beam's TestStream creates a synthetic event stream with controlled event-time and processing-time advancement. Running with DirectRunner + TestStream + a test BigQuery table enables full pipeline testing without touching production resources. This is the standard Beam integration testing pattern.", concept: "Beam TestStream, DirectRunner testing, pipeline unit testing" }, { q: "A BigQuery table uses integer range partitioning on customer_id (range: 0–10,000,000, interval: 100,000). A query filters WHERE customer_id = 42. How many partitions will BigQuery scan?", opts: ["All 100 partitions — integer range partitioning doesn't support point lookups", "1 partition — customer_id 42 falls in the [0, 100000) partition; BigQuery prunes all other partitions", "2 partitions — BigQuery always scans the adjacent partition for safety", "10 partitions — BigQuery scans 10% of partitions for any point lookup"], ans: 1, explain: "Integer range partitioning in BigQuery maps each row to a partition based on the range interval. customer_id 42 maps to the [0, 100000) partition. BigQuery prunes all other partitions at query planning time — only 1 partition is scanned. This is analogous to date partition pruning but for numeric keys.", concept: "BigQuery integer range partitioning, partition pruning, numeric partitioning" }, { q: "You need to build a pipeline that processes genomics VCF files stored in Cloud Storage. Processing requires complex bioinformatics tools (bwa, samtools) not available in standard Dataflow transforms. What is the best approach?", opts: ["Rewrite the bioinformatics tools in Python and run in Dataflow ParDo", "Use Dataflow with custom Docker containers (Flex Templates) containing the bioinformatics tools; or use Life Sciences API (Cloud Batch) which natively supports custom container-based genomic workflows", "Use Dataproc with a custom initialization action that installs the tools", "Install the tools on Dataflow workers using pip in the pipeline setup"], ans: 1, explain: "For specialized scientific tools, Dataflow Flex Templates with custom Docker containers (FROM apache/beam_python3.10_sdk; RUN apt-get install bwa samtools) allows arbitrary tool execution. Alternatively, Cloud Batch (formerly Life Sciences API) is purpose-built for container-based scientific batch workloads.", concept: "Dataflow custom containers, Flex Templates, scientific computing, Cloud Batch" }, { q: "What is the significance of the 'committed' vs 'buffered' write modes in the BigQuery Storage Write API?", opts: ["Buffered mode is for batch jobs; committed mode is for streaming only", "Committed mode provides exactly-once guarantees with atomic row-level commits; buffered mode provides lower latency with rows visible within seconds but possible duplicates on failure. Default stream (legacy) provides no ordering guarantee", "Both modes are identical in guarantees; buffered is just newer", "Committed mode is only available in Enterprise BigQuery Edition"], ans: 1, explain: "Storage Write API stream types: Default stream (legacy, at-least-once, rows visible immediately). Committed mode: rows are appended to committed streams with offset-based exactly-once guarantee; rows visible on commit. Buffered mode: rows held in a buffer, committed in batches — balances latency and exactly-once-ness.", concept: "BigQuery Storage Write API, committed vs buffered streams, exactly-once guarantees" }, { q: "A company's data team has 5 data engineers, 10 data analysts, and 3 data scientists, all needing different levels of BigQuery access. What is the recommended IAM structure?", opts: ["Grant each person a custom role with exactly the permissions they need, configured individually", "Create three Google Groups (data-engineers, data-analysts, data-scientists); assign BigQuery IAM roles to groups at appropriate levels (project/dataset/table); manage individual membership in groups", "Assign roles/bigquery.admin to all users for simplicity", "Create a shared service account that all users impersonate"], ans: 1, explain: "Google recommends managing BigQuery access via Google Groups. Groups provide scalable, auditable access management. Typical bindings: data-engineers → roles/bigquery.dataEditor at project level; data-analysts → roles/bigquery.dataViewer on specific datasets; data-scientists → roles/bigquery.jobUser + viewer on ML datasets.", concept: "BigQuery IAM best practices, Google Groups, role-based access" }, { q: "You need to monitor data quality in a production BigQuery pipeline and alert when more than 5% of records fail validation rules. Which approach automates this?", opts: ["Manually sample the BigQuery table daily and count invalid records", "Use Dataplex Data Quality rules with expectation metrics (PERCENT_NULL, REGEX_MATCH, etc.); configure the quality scan to write results to BigQuery; create a Cloud Monitoring alert on the Dataplex data quality metric when failure rate exceeds 5%", "Write a Cloud Function that runs a BigQuery query nightly to check data quality", "Add a Dataflow ParDo that counts invalid records and logs them to Stackdriver"], ans: 1, explain: "Dataplex Data Quality scans define rules (not_null, regex_match, value_in_set, custom SQL) and execute on a schedule. Results (including pass/fail percentages) are logged to Cloud Monitoring. An alerting policy on the dataplex.googleapis.com/data_quality/... metric fires when failure rate > 5%.", concept: "Dataplex Data Quality, automated data quality monitoring, Cloud Monitoring alerts" }, { q: "Which Google Cloud product allows you to create a complete Apache Iceberg table format on top of Cloud Storage, accessible from BigQuery, Dataproc, and Spark?", opts: ["BigQuery managed tables with Iceberg compatibility mode", "BigLake Managed Tables (Iceberg on BigLake) — data stored in Cloud Storage as Iceberg format; accessible from BigQuery, Dataproc Spark, and any Iceberg-compatible engine", "Dataproc Metastore with Iceberg catalog support only", "Cloud Spanner with Iceberg export enabled"], ans: 1, explain: "BigLake Managed Tables implement the Apache Iceberg open table format on Cloud Storage. They support ACID transactions, time travel, schema evolution, and partition evolution. Both BigQuery (via BigLake external tables) and Dataproc Spark/Flink access the same Iceberg table with full ACID guarantees.", concept: "BigLake Managed Tables, Apache Iceberg, open table format, multi-engine access" }, { q: "A data engineering team needs to document their BigQuery data assets including business definitions, ownership, and SLAs. Which GCP service is purpose-built for this?", opts: ["BigQuery INFORMATION_SCHEMA.TABLES with custom columns", "Dataplex Universal Catalog — supports business glossaries, data owner tags, custom metadata aspects, and SLA documentation alongside technical metadata", "Cloud Monitoring dashboards with custom labels", "A Google Sheets spreadsheet shared with the team"], ans: 1, explain: "Dataplex Universal Catalog (evolved from Data Catalog) enables attaching custom metadata aspects and tag templates to BigQuery tables: business owner, data classification, SLA, quality score, glossary terms. This creates a unified business + technical metadata layer discoverable across the organization.", concept: "Dataplex Universal Catalog, data documentation, metadata management, business glossary" }, { q: "A Pub/Sub topic receives messages from multiple publishers. Some messages have duplicate message IDs. What behavior should a correctly designed consumer expect?", opts: ["Pub/Sub guarantees exactly-once delivery and deduplicates messages with the same message ID automatically", "Pub/Sub guarantees at-least-once delivery — duplicates are possible even without publisher duplicates. Consumers must implement idempotent processing or deduplication using message IDs", "Pub/Sub Lite guarantees exactly-once; standard Pub/Sub cannot handle duplicates", "Duplicate message IDs cause Pub/Sub to reject the second message automatically"], ans: 1, explain: "Standard Pub/Sub guarantees at-least-once delivery. Even with unique publisher message IDs, network retries can cause the same message to be delivered twice. Consumer applications must be idempotent (processing the same message twice produces the same result) or perform deduplication using insertion tables keyed on message ID.", concept: "Pub/Sub delivery guarantees, at-least-once, consumer idempotency, deduplication" }, { q: "You have a Cloud Composer environment running critical pipelines. The Airflow webserver becomes slow during peak DAG execution. What should you do?", opts: ["Restart the Airflow webserver pod to clear memory", "Upgrade the Composer environment to a larger environment size (scale up the webserver and scheduler resources); optimize DAG count and complexity to reduce scheduler load", "Switch to Apache Airflow on Kubernetes (GKE) for manual scaling control", "Disable the Airflow webserver — use gcloud composer environments run instead"], ans: 1, explain: "Webserver slowness during peak execution is usually due to resource contention. In Composer 2, you can independently scale webserver resources. Also: reduce DAG complexity (fewer Python imports, simpler task graph), use concurrency limits, and ensure scheduler resources are adequate for the DAG count.", concept: "Cloud Composer sizing, webserver scaling, Composer 2 resource configuration" }, { q: "A Dataflow pipeline reads from BigQuery and the read is taking longer than expected. The BigQuery table has 100 partitions but Dataflow is using only 10 read streams. How do you increase read parallelism?", opts: ["Increase Dataflow worker count; it automatically increases BigQuery read parallelism", "Use BigQueryIO.read().withTemplateCompatibility() to force more read streams", "Use BigQueryIO.read().withMethod(DIRECT_READ) which uses the BigQuery Storage Read API with configurable parallelism, or set .withMaxStreamCount()", "Read the data into Cloud Storage first, then read from GCS with Dataflow"], ans: 2, explain: "BigQueryIO has two read modes: EXPORT (copies to GCS then reads — slow) and DIRECT_READ (uses BigQuery Storage Read API for parallel streaming). DIRECT_READ with withMaxStreamCount() controls the number of parallel Storage API streams, directly mapping to read parallelism. This is the fastest way to read large BigQuery tables in Dataflow.", concept: "BigQuery Storage Read API, Dataflow DIRECT_READ, parallel read streams" }, { q: "You need to implement a Blue/Green deployment for a BigQuery transformation pipeline (dbt project). How would you structure this in GCP?", opts: ["BigQuery doesn't support Blue/Green — you must use in-place updates", "Maintain two BigQuery datasets (blue and prod): run dbt against the blue dataset; validate results; use BigQuery dataset copy or view swapping to atomically promote blue to prod; the switch is instant with no downtime", "Create two separate GCP projects for blue and green environments", "Use BigQuery table swap (ALTER TABLE RENAME) for atomic schema promotion"], ans: 1, explain: "Blue/Green for BigQuery transformations: dbt runs against a blue dataset (separate from prod). After validation, promote: either rename/swap datasets atomically, or update views in the prod dataset to point to blue table names. This gives zero-downtime promotion with instant rollback capability.", concept: "Blue/Green deployment, BigQuery dataset promotion, zero-downtime transformation" }, { q: "A streaming pipeline with Dataflow receives 1,000,000 events/sec. The processing logic is CPU-intensive (parsing and transforming JSON). Workers are CPU-bound at 100%. What is the correct scaling action?", opts: ["Increase worker disk size to speed up I/O-limited operations", "Add more Dataflow workers (increase maxNumWorkers); if using Streaming Engine, increase n2-standard machine type to higher CPU; Dataflow autoscaling should handle this if maxNumWorkers is configured correctly", "Switch to Dataproc — it handles CPU-intensive workloads better", "Reduce the number of concurrent ParDo operations in the pipeline"], ans: 1, explain: "CPU-bound Dataflow workers need either more workers (horizontal scaling via maxNumWorkers) or larger machines (higher CPU count per worker, e.g., n2-highcpu-16). With Streaming Engine, autoscaling adds workers automatically. The root cause is often JSON parsing overhead — consider switching to Avro/Protobuf to reduce parsing cost.", concept: "Dataflow CPU scaling, machine types, streaming performance, serialization formats" }, { q: "What is the purpose of BigQuery's column-level security using Policy Tags in Dataplex?", opts: ["Policy tags encrypt column data at rest", "Policy tags define a taxonomy of sensitive data types; columns tagged with a policy tag require the reader to have a specific IAM role (Fine-Grained Reader) to see that column's data — others get Access Denied", "Policy tags are only for documentation; they don't affect query execution", "Policy tags replace row access policies for row-level filtering"], ans: 1, explain: "BigQuery column-level security (CLS) uses Dataplex Policy Tag taxonomies. Tag a column (e.g., SSN) with a sensitive tag. Users without roles/datacatalog.categoryFineGrainedReader on that tag get 'Access Denied' when querying that column — even if they have access to the table. This enforces column-level data access control.", concept: "BigQuery column-level security, Policy Tags, Dataplex taxonomy, Fine-Grained Reader" }, { q: "You need to ingest streaming data from Apache Kafka (on-premises) to BigQuery. Which GCP service provides a managed connector for this without writing custom code?", opts: ["Cloud Pub/Sub with Kafka MirrorMaker 2 for replication", "Dataflow with the Apache Beam Kafka connector (KafkaIO); or use the managed Dataflow Kafka-to-BigQuery template", "Cloud Data Fusion with Kafka source plugin and BigQuery sink", "All three options require custom code; GCP has no managed Kafka connector"], ans: 1, explain: "Dataflow provides a managed Kafka-to-BigQuery template: configure Kafka bootstrap servers, topic, schema, and BigQuery destination — no code needed. Alternatively, KafkaIO in Beam gives code-level control. Both are production-grade. Pub/Sub cannot directly ingest from Kafka without a bridge.", concept: "Kafka to BigQuery, Dataflow Kafka connector, KafkaIO, managed templates" }, { q: "A data governance policy requires that all production BigQuery tables have a defined owner, data classification, and retention period. How do you enforce this organization-wide?", opts: ["Document the requirements in a wiki and rely on team compliance", "Create mandatory tag templates in Dataplex (owner, classification, retention_days fields); use Organization Policy or Dataplex metadata enforcement to require tags before tables can be queried; automate tagging in CI/CD pipelines", "Use BigQuery table descriptions — annotate each table description with the required fields", "Apply IAM conditions that check for tag presence before granting access"], ans: 1, explain: "Dataplex tag templates define required metadata fields. Create a mandatory template and use Dataplex data quality + governance automation: CI/CD pipeline (Cloud Build + Terraform) applies tags at table creation time. Dataplex policies can enforce tag completeness. This shifts governance left — metadata is enforced at creation, not retroactively.", concept: "Dataplex tag templates, metadata enforcement, governance automation, CI/CD data governance" }, { q: "A pipeline fails because a Dataflow job cannot read from a Cloud Storage bucket in a different GCP project. All IAM permissions are correct. What else might be causing the issue?", opts: ["Cross-project Cloud Storage access is not supported in Dataflow", "The Cloud Storage bucket is in a VPC Service Controls perimeter that does not include the Dataflow job's project. Add the Dataflow project to the perimeter or create an access level exception", "Dataflow workers need to be in the same region as the Cloud Storage bucket", "The Cloud Storage bucket requires Requester Pays to be enabled for cross-project access"], ans: 1, explain: "VPC Service Controls perimeters are a common cause of cross-project access failures even when IAM is correct. If the Cloud Storage bucket's project is in a perimeter, all access must come from within the perimeter. Add the Dataflow worker project to the same perimeter, or configure an ingress/egress rule to allow the specific access.", concept: "VPC Service Controls, perimeter access, cross-project Dataflow, troubleshooting" }] },

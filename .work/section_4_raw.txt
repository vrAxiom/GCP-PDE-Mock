      { id: 4, title: "Preparing and Using Data for Analysis", weight: "15%", color: "#9334e6", questions: [{ q: "A data scientist wants to train a custom ML model using BigQuery data but avoid exporting data. Which Google Cloud service allows ML model training directly within BigQuery?", opts: ["Vertex AI Feature Store", "BigQuery ML (BQML) using CREATE MODEL statement", "Dataproc ML with Spark MLlib", "Cloud AutoML Tables with BigQuery connector"], ans: 1, explain: "BigQuery ML allows creating and training ML models using standard SQL with CREATE MODEL — no data export needed. Supported: linear/logistic regression, k-means, XGBoost, deep neural nets, and remote LLM models.", concept: "BigQuery ML, CREATE MODEL, in-database ML training" }, { q: "A BI tool (Looker) issues thousands of identical queries to BigQuery simultaneously. You want to reduce cost and latency. Which BigQuery feature should you enable?", opts: ["BigQuery Query Caching — identical queries return cached results for 24 hours at no charge", "BigQuery Reservations with flat-rate pricing for all Looker queries", "Create a materialized view that pre-computes every dashboard query", "Enable BI Engine for in-memory analysis of all tables"], ans: 0, explain: "BigQuery's query cache returns results from a previous identical query within 24 hours at no charge. For BI tools issuing repeated dashboard queries, this eliminates most compute cost. Requires deterministic queries (no CURRENT_TIMESTAMP(), etc.).", concept: "BigQuery query cache, cost reduction, BI tool patterns" }, { q: "You need to implement data lineage tracking — knowing which upstream sources contributed to a BigQuery table. Which service provides this natively in GCP?", opts: ["Cloud Audit Logs with BigQuery job history parsing", "Dataplex Data Lineage API, which auto-captures lineage from BigQuery, Dataflow, and Data Fusion", "Cloud Monitoring custom metrics tracking table reads/writes", "Apache Atlas deployed on Dataproc for lineage tracking"], ans: 1, explain: "Dataplex Data Lineage API automatically captures lineage events from BigQuery (DDL/DML), Dataflow pipelines, and Cloud Data Fusion without any code changes — building a lineage graph of upstream sources and downstream consumers.", concept: "Dataplex Data Lineage, data governance, lineage tracking" }, { q: "A dashboard runs a complex aggregation query over a 10 TB BigQuery table, refreshed hourly. The query takes 45 seconds. How do you reduce dashboard query latency to under 2 seconds?", opts: ["Increase BigQuery slots via reservations to run the query faster", "Create a BigQuery materialized view of the aggregation; queries automatically use it if eligible", "Enable BigQuery BI Engine on the dashboard table", "Export the 10TB to Cloud Spanner for lower latency analytics"], ans: 1, explain: "BigQuery materialized views pre-compute and cache aggregation results. When a query matches, BigQuery uses the pre-computed result — reducing 45-second full scans to sub-second lookups. The view refreshes automatically when underlying data changes.", concept: "BigQuery materialized views, query acceleration, incremental refresh" }, { q: "Which Looker Studio connector allows real-time data exploration of BigQuery data without data extracts?", opts: ["Looker Studio BigQuery direct connector with Live Connection mode", "Looker Studio extracts pulled every 15 minutes", "BigQuery scheduled query pushing results to Google Sheets, then Sheets connector", "JDBC connector from Looker Studio to BigQuery"], ans: 0, explain: "Looker Studio's BigQuery direct connector in Live Connection mode issues SQL queries to BigQuery on demand whenever a report loads — no data extract needed. This ensures data freshness but incurs BigQuery query costs.", concept: "Looker Studio, Live Connection, BigQuery connector" }, { q: "A data analyst writes: SELECT * FROM orders WHERE CAST(order_date AS STRING) = '2024-01-15'. Why is this problematic?", opts: ["CAST is not supported in BigQuery WHERE clauses", "CAST on order_date prevents partition pruning — BigQuery cannot use the date partition filter on a function-wrapped column. Fix: WHERE order_date = DATE '2024-01-15'", "The query will fail because STRING comparison to DATE is invalid", "There is no issue; BigQuery optimizes CAST automatically"], ans: 1, explain: "Wrapping a partitioned column in a function (CAST, FORMAT, etc.) prevents BigQuery from performing partition pruning — it must scan ALL partitions. Always filter partitioned columns directly.", concept: "BigQuery partition pruning, function-wrapped predicates, query optimization" }, { q: "A machine learning team needs consistent features between online serving (millisecond latency) and batch training. Which GCP service is purpose-built for this requirement?", opts: ["BigQuery ML Feature Store", "Vertex AI Feature Store — provides both online serving and batch offline storage with point-in-time correctness", "Cloud Bigtable as a dual-purpose feature store for online and offline", "Dataflow pipeline that writes features to both BigQuery and Memorystore Redis"], ans: 1, explain: "Vertex AI Feature Store solves online-offline skew. Features are written once and served for both batch retrieval (with point-in-time correctness to prevent leakage) and online serving (low-latency HTTP lookups).", concept: "Vertex AI Feature Store, online-offline consistency, training-serving skew" }, { q: "You are building a recommendation system and need to find the 10 most similar items to a query vector from 100 million items within 100ms. Which GCP service supports approximate nearest neighbor (ANN) search at this scale?", opts: ["BigQuery ARRAY_AGG with cosine similarity computed in SQL", "Vertex AI Vector Search (Matching Engine) — purpose-built for billion-scale ANN search", "Cloud Bigtable with vector embeddings stored as column values", "Elasticsearch on GKE with vector search plugin"], ans: 1, explain: "Vertex AI Vector Search (formerly Matching Engine) uses ScaNN (Scalable Approximate Nearest Neighbors) to serve billion-scale vector similarity searches with < 100ms p99 latency. A full SQL scan of 100M vectors would take seconds.", concept: "Vertex AI Vector Search, ANN, embeddings, recommendation systems" }, { q: "A compliance team requires that all BigQuery query results involving columns tagged 'CONFIDENTIAL' must be masked for users without explicit access. Which feature enforces this without modifying the underlying data?", opts: ["BigQuery Authorized Views filtering out confidential columns", "BigQuery Dynamic Data Masking — apply masking rules to columns based on IAM policy bindings; users without access see masked values", "BigQuery Column-Level Security with policy tags — users without access get permission denied errors", "Encrypt confidential columns with AEAD functions before storage"], ans: 1, explain: "BigQuery Dynamic Data Masking extends Column-Level Security: instead of blocking access, masking rules (nullify, default value, SHA256 hash, etc.) are applied for users without the unmasked data role.", concept: "BigQuery Dynamic Data Masking, policy tags, column-level security" }, { q: "A data team uses dbt on BigQuery. They want to ensure that dbt models are only re-run when their source data has changed. Which dbt feature enables this?", opts: ["dbt --select flag to manually choose which models to run", "dbt snapshots with strategy=timestamp", "dbt state-based selection with --select state:modified+ to run only models with changed SQL or changed upstream sources", "dbt seeds to cache source data"], ans: 2, explain: "dbt's state-based selection compares the current project state to a previous manifest.json. --select state:modified+ runs only models whose SQL or upstream dependencies have changed — avoiding unnecessary reprocessing.", concept: "dbt state-based selection, dbt on BigQuery, incremental model execution" }, { q: "What is the purpose of BigQuery Reservations (formerly known as Flat-rate pricing)?", opts: ["Reservations ensure data is stored in a specific geographic region", "Reservations provide dedicated BigQuery compute slots; workloads are isolated and not subject to on-demand slot contention", "Reservations enable automatic schema detection for external tables", "Reservations are required for BigQuery ML model training jobs"], ans: 1, explain: "BigQuery Reservations (Capacity-based pricing) provides dedicated slot capacity. Organizations buy slots and assign them to projects/folders via assignments. Workloads use only their reserved slots, providing cost predictability and isolation.", concept: "BigQuery Reservations, slot management, capacity vs on-demand pricing" }, { q: "You need to profile a 1 TB BigQuery table to identify null rates, outlier distributions, and schema anomalies. Which GCP service automates this?", opts: ["Run manual COUNT/AVG/STDDEV SQL queries across all 500 columns", "Dataplex data quality auto-profiling — scans tables and generates statistical profiles without writing SQL", "BigQuery INFORMATION_SCHEMA.COLUMNS for statistical profiling", "Cloud Data Fusion Data Preview feature"], ans: 1, explain: "Dataplex auto data profiling scans BigQuery tables and generates column-level statistics (null %, distinct count, min/max, quartiles, top values) automatically — without writing SQL.", concept: "Dataplex data profiling, data quality, automated scanning" }, { q: "A BI analyst needs to join a BigQuery table (10 TB) with a Google Sheets spreadsheet updated daily. What is the recommended approach?", opts: ["Export BigQuery data to Google Sheets daily and join in Sheets", "Create a BigQuery External Table pointing to the Google Sheet; join it with the BigQuery table in standard SQL", "Use Cloud Data Fusion to sync Google Sheets to BigQuery as a native table", "Manually download Sheets as CSV and load to BigQuery each day"], ans: 1, explain: "BigQuery supports Google Sheets as an external data source. Create an External Table pointing to the Sheet URL — BigQuery queries it live. This allows SQL JOIN between the 10 TB BigQuery table and the Sheets data without manual data movement.", concept: "BigQuery external tables, Google Sheets integration, federated analysis" }, { q: "Which SQL window function correctly computes a 7-day rolling average of daily sales in BigQuery?", opts: ["AVG(sales) OVER (PARTITION BY product_id ORDER BY sale_date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW)", "AVG(sales) OVER (PARTITION BY product_id ORDER BY sale_date RANGE BETWEEN INTERVAL 7 DAY PRECEDING AND CURRENT ROW)", "AVG(sales) OVER (ORDER BY sale_date ROWS BETWEEN -7 AND 0)", "SUM(sales) / 7 OVER (ORDER BY sale_date PARTITION BY product_id)"], ans: 0, explain: "ROWS BETWEEN 6 PRECEDING AND CURRENT ROW creates a 7-row window (current + 6 previous). This computes a 7-day rolling average per product when data has one row per day.", concept: "BigQuery window functions, rolling aggregations, ROWS vs RANGE" }, { q: "A company wants to implement row-level security in BigQuery: analysts in EMEA should only see European customer rows, analysts in APAC should only see APAC rows. Which feature implements this cleanly?", opts: ["Create separate tables per region and grant IAM access accordingly", "BigQuery Row-Level Access Policies with CREATE ROW ACCESS POLICY filtering by region and binding to IAM groups", "Authorized views per region with region-specific WHERE clauses", "VPC Service Controls restricting BigQuery access by network region"], ans: 1, explain: "BigQuery Row Access Policies: CREATE ROW ACCESS POLICY statements filter rows based on SESSION_USER() or IAM group membership. EMEA analysts automatically see only WHERE region='EMEA' — no separate tables or views needed.", concept: "BigQuery row access policies, row-level security, IAM group filtering" }, { q: "You need to calculate the percentile distribution (P50, P90, P99) of API response times from a BigQuery table of 1 billion rows. Which BigQuery function is appropriate?", opts: ["AVG() — computes approximate average response time", "APPROX_QUANTILES(response_time, 100) — returns approximate quantiles efficiently on large datasets", "PERCENTILE_CONT(0.99) OVER () — exact window function but requires full sort", "STDDEV() — approximates percentile distribution via standard deviation"], ans: 1, explain: "APPROX_QUANTILES(expression, N) computes approximate quantiles using HyperLogLog++ sketches — O(1) memory regardless of dataset size. For 1 billion rows, this is dramatically more efficient than exact methods like PERCENTILE_CONT which require a full sort. Returns an ARRAY of N+1 quantile values.", concept: "BigQuery APPROX_QUANTILES, approximate aggregations, percentile computation" }, { q: "A data team runs 200 BigQuery queries per day from a Looker dashboard. Each query costs $5 on average (on-demand). What is the break-even analysis for switching to BigQuery Reservations?", opts: ["Reservations are never cost-effective for dashboard workloads — always use on-demand", "Daily on-demand cost: 200 queries × $5 = $1,000/day = $30,000/month. Compare to Reservation cost: at ~$0.04/slot/hour, ~750 slots needed for 200 queries, costing ~$21,600/month. Reservations break even at consistent high utilization", "Reservations are always cheaper than on-demand by 50%", "Reservations cost more than on-demand but provide guaranteed performance"], ans: 1, explain: "Break-even analysis: calculate daily on-demand spend × 30 days vs. monthly reservation cost at the required slot count. Reservations become cost-effective when consistent high utilization (>60-70% of the time) fills the reserved slots. For bursty or unpredictable workloads, on-demand may be cheaper.", concept: "BigQuery cost analysis, Reservations vs on-demand break-even, slot utilization" }, { q: "What is the difference between BigQuery's DISTINCT vs APPROX_COUNT_DISTINCT for counting unique users in a 100 billion row table?", opts: ["They are identical in results and performance", "COUNT(DISTINCT user_id) computes the exact count using full deduplication — memory and time scales with unique value count. APPROX_COUNT_DISTINCT uses HyperLogLog++ with ~1% error — processes in constant memory regardless of dataset size. Use APPROX for large-scale analytics, COUNT(DISTINCT) only when exact count is required", "APPROX_COUNT_DISTINCT is only available in BigQuery ML models", "COUNT(DISTINCT) is faster in BigQuery because it uses native SQL optimization"], ans: 1, explain: "For 100 billion rows with millions of unique users, COUNT(DISTINCT) requires holding all unique values in memory for deduplication — potentially slow and resource-intensive. APPROX_COUNT_DISTINCT uses HyperLogLog++ sketches with ~1-2% error but O(1) memory. Choose based on business need for exactness.", concept: "BigQuery COUNT DISTINCT vs APPROX_COUNT_DISTINCT, HyperLogLog++, scalable cardinality" }, { q: "You need to run a BigQuery query that compares each customer's purchase to the average purchase of all customers in the same city. Which SQL construct achieves this efficiently?", opts: ["Subquery: WHERE amount > (SELECT AVG(amount) FROM orders WHERE city = o.city)", "Window function: AVG(amount) OVER (PARTITION BY city) — computes city average in a single pass without subquery", "Self-join: JOIN orders as avg_orders ON o.city = avg_orders.city", "Create a separate table of city averages and join to the main table"], ans: 1, explain: "Window function AVG(amount) OVER (PARTITION BY city) computes the average amount per city in a single table scan — no subquery or self-join. The original row context is preserved, enabling direct comparison: amount vs. AVG(amount) OVER (PARTITION BY city).", concept: "BigQuery window functions, PARTITION BY, analytical queries" }, { q: "A data analyst needs to understand why a specific BigQuery query is slow. Which tools in BigQuery help diagnose query performance?", opts: ["Cloud Monitoring metrics — they show query-level execution time breakdown", "BigQuery Query Execution Details (query plan explanation) in the BigQuery UI or via EXPLAIN statement — shows each stage's elapsed time, slot usage, bytes processed, and shuffle data", "Cloud Logging query logs — they contain query performance details", "BigQuery INFORMATION_SCHEMA.JOBS for aggregate statistics only"], ans: 1, explain: "BigQuery's query execution plan (available in the UI under 'Execution Details' or via INFORMATION_SCHEMA.JOBS_TIMELINE) shows: each query stage, worker count, elapsed time, bytes shuffled between stages, and slot consumption. This pinpoints bottlenecks (large shuffles, slow stages).", concept: "BigQuery query plan, execution details, performance diagnosis" }, { q: "Which BigQuery feature allows creating reusable SQL components that can be called like functions, supporting complex business logic without code duplication?", opts: ["BigQuery stored procedures only (no function support)", "BigQuery User-Defined Functions (UDFs) using SQL or JavaScript; Persistent UDFs are stored in datasets and reusable across queries", "BigQuery macros via the DECLARE statement", "BigQuery table-valued functions — they replace views for reusable logic"], ans: 1, explain: "BigQuery UDFs: SQL UDFs use standard SQL expressions; JavaScript UDFs enable complex logic not expressible in SQL. Persistent UDFs are stored in a dataset and callable with project.dataset.function_name() syntax. Remote Functions call Cloud Run/Cloud Functions for external computation.", concept: "BigQuery UDFs, user-defined functions, reusable SQL logic" }, { q: "You need to join a streaming Pub/Sub event stream with a slowly-changing BigQuery dimension table in real-time. The dimension table updates once per hour. What is the most efficient join pattern in Dataflow?", opts: ["Query BigQuery for each event in the stream (one SQL call per event)", "Load the BigQuery dimension table as a Dataflow side input at pipeline start; use a timer to reload it every hour. Join the stream against the in-memory side input for sub-millisecond per-event lookups", "Write streaming events to BigQuery and use BigQuery scheduled queries to join hourly", "Use Cloud Spanner as an intermediary — Dataflow queries Spanner for each event"], ans: 1, explain: "For slowly-changing dimension tables, side inputs with periodic refresh are optimal: the dimension table is loaded once per hour into worker memory (side input view). Each streaming event is joined locally against the cached data — no external service call per event. This provides sub-millisecond join latency.", concept: "Dataflow side inputs, streaming dimension join, slowly-changing data refresh" }, { q: "What is the purpose of a BigQuery 'Scripting' and DECLARE/SET in a BigQuery SQL script?", opts: ["BigQuery scripting is only for DDL operations (CREATE, DROP)", "BigQuery scripting enables imperative SQL: DECLARE variables, SET values, IF/ELSE conditions, WHILE loops, CALL procedures — enabling complex multi-step data transformations in a single session without client-side code", "DECLARE and SET are synonyms for CREATE TABLE and INSERT", "BigQuery scripting only works inside stored procedures"], ans: 1, explain: "BigQuery Scripting brings procedural SQL to BigQuery: DECLARE @var INT64; SET @var = (SELECT COUNT(*) FROM table); IF @var > 0 THEN ... END IF; LOOP...END LOOP. This enables complex conditional logic, loops over result sets, and multi-step transformations without orchestration tools.", concept: "BigQuery scripting, procedural SQL, DECLARE/SET, conditional logic" }, { q: "You are building a data pipeline that transforms raw clickstream data into sessionized user journeys. Each session = consecutive clicks with < 30 min gap. Which BigQuery SQL approach sessionizes efficiently?", opts: ["Use DISTINCT on user_id and timestamp to identify unique sessions", "Use window functions: LAG() to compare timestamp with previous click per user; mark session start when gap > 30 min; use SUM(is_new_session) OVER (PARTITION BY user_id ORDER BY timestamp) to assign session IDs", "Create a stored procedure that iterates row by row for each user", "Use BigQuery ML time-series models to detect session boundaries"], ans: 1, explain: "Standard sessionization SQL: LAG(timestamp) OVER (PARTITION BY user_id ORDER BY timestamp) gets the previous event time. CASE WHEN timestamp - prev_timestamp > INTERVAL 30 MINUTE THEN 1 ELSE 0 END marks new sessions. Running sum assigns incrementing session IDs per user.", concept: "BigQuery sessionization, LAG window function, session analytics" }, { q: "What is the BigQuery MERGE statement used for and what are its key clauses?", opts: ["MERGE is used for joining two tables for analytical queries", "MERGE (DML) implements upsert semantics: WHEN MATCHED THEN UPDATE, WHEN NOT MATCHED BY TARGET THEN INSERT, WHEN NOT MATCHED BY SOURCE THEN DELETE — enabling efficient CDC/SCD updates in a single atomic statement", "MERGE is BigQuery's version of UNION for combining tables", "MERGE replaces CREATE TABLE AS SELECT for ETL workloads"], ans: 1, explain: "BigQuery MERGE performs upsert/CDC operations: MERGE target USING source ON key WHEN MATCHED THEN UPDATE SET ... WHEN NOT MATCHED THEN INSERT .... A single MERGE statement atomically handles inserts, updates, and deletes — essential for Type 2 SCD, CDC replay, and idempotent pipeline reruns.", concept: "BigQuery MERGE statement, upsert, CDC, SCD Type 2, idempotent ETL" }, { q: "A data team needs to analyze customer churn using BigQuery ML. Which BQML model type is appropriate for binary classification (churned / not churned)?", opts: ["k-means clustering (KMEANS) — groups customers into churn segments", "Logistic regression (LOGISTIC_REG) or Boosted Trees (BOOSTED_TREE_CLASSIFIER) — binary classification models predicting probability of churn", "Linear regression (LINEAR_REG) — predicts a continuous churn score", "Time series (ARIMA_PLUS) — forecasts future churn rates"], ans: 1, explain: "Binary classification (churned=1/0) requires LOGISTIC_REG or BOOSTED_TREE_CLASSIFIER in BigQuery ML. Logistic regression is interpretable and fast to train; boosted trees (XGBoost) typically achieve higher accuracy. Both output predicted probabilities via ML.PREDICT().", concept: "BigQuery ML model types, binary classification, churn prediction" }, { q: "You need to visualize BigQuery data in Looker Studio but the report is slow. Which optimizations improve performance for a 100M-row BigQuery table?", opts: ["Enable Looker Studio caching only — it refreshes every 15 minutes automatically", "Apply multiple optimizations: partition and cluster the BigQuery table for dashboard filters; enable Looker Studio data extract for frequently-used date ranges; configure BigQuery BI Engine for in-memory acceleration of common aggregations", "Export data to Cloud SQL for Looker Studio to read directly", "Reduce the number of charts to limit the number of BigQuery queries per page load"], ans: 1, explain: "Multi-layer optimization for Looker Studio: (1) BigQuery partitioning/clustering aligns with dashboard filter patterns; (2) Looker Studio extracts cache stable data; (3) BI Engine accelerates common aggregations in-memory. Layer these based on report freshness requirements.", concept: "Looker Studio performance, BigQuery BI Engine, data extracts, dashboard optimization" }, { q: "What is BigQuery's EXCEPT ALL and INTERSECT ALL for set operations?", opts: ["EXCEPT returns rows in the left query that are not in the right query; INTERSECT returns rows present in both. ALL retains duplicates; without ALL, duplicates are removed", "EXCEPT and INTERSECT are not supported in BigQuery", "EXCEPT ALL is the same as NOT IN; INTERSECT ALL is the same as IN", "EXCEPT and INTERSECT only work on tables with identical schemas and row counts"], ans: 0, explain: "BigQuery supports standard SQL set operations: UNION (combine), INTERSECT (common rows), EXCEPT (left-only rows). The ALL modifier retains duplicate rows; without it, duplicates are implicitly removed (DISTINCT). Useful for finding delta between two datasets.", concept: "BigQuery set operations, EXCEPT ALL, INTERSECT ALL, UNION" }, { q: "A report requires calculating the month-over-month growth rate for each product. Which BigQuery approach is most expressive and efficient?", opts: ["Create a self-join on the sales table joining on product_id and month offset", "Use LAG(revenue) OVER (PARTITION BY product_id ORDER BY month) to get prior month revenue; calculate (current - prior) / prior as growth rate in a single query", "Use a subquery that calculates each month's total separately and divides", "Create a materialized view that pre-computes growth rates for all products"], ans: 1, explain: "LAG() window function retrieves the value from the previous row within the partition. (revenue - LAG(revenue) OVER (PARTITION BY product_id ORDER BY month)) / LAG(revenue) ... computes MoM growth in a single pass. Self-joins for time-offset comparisons are less readable and less efficient.", concept: "BigQuery LAG window function, month-over-month analysis, time-series comparison" }, { q: "What is the purpose of BigQuery's UNNEST function?", opts: ["UNNEST flattens a repeated STRING column to create comma-separated values", "UNNEST converts an ARRAY column into a set of rows — one row per array element — enabling SQL operations on individual array elements as if they were regular rows", "UNNEST creates a new table from an existing BigQuery array column", "UNNEST is used to flatten STRUCT columns into separate columns"], ans: 1, explain: "UNNEST flattens ARRAY fields into rows: SELECT * FROM table, UNNEST(tags) AS tag creates one row per tag value per original row. This enables filtering, grouping, and joining on array elements. Essential for working with BigQuery's repeated fields.", concept: "BigQuery UNNEST, ARRAY flattening, repeated fields, SQL on arrays" }, { q: "You need to build a real-time analytics dashboard that shows product sales in the last 5 minutes with 1-second refresh. What is the optimal architecture?", opts: ["BigQuery streaming inserts + Looker Studio with 1-second auto-refresh", "Pub/Sub → Dataflow aggregation in 1-minute windows → BigQuery → Looker Studio with 1-second query refresh", "Pub/Sub → Dataflow 5-minute sliding window aggregation → BigQuery → Looker Studio Live Connection. For truly real-time (< 1 sec), use Bigtable for the aggregated results and a custom dashboard querying Bigtable", "Cloud Spanner for all sales data with Looker Studio direct connection"], ans: 2, explain: "For 1-second real-time dashboards: BigQuery has 1-2 second query latency minimum — insufficient for true 1-second refresh. The optimal architecture: Dataflow computes 5-minute sliding window aggregations → writes to Bigtable (sub-ms reads) → custom dashboard polls Bigtable every second.", concept: "Real-time dashboard architecture, Bigtable for real-time, Dataflow aggregation" }, { q: "A data team wants to understand the business impact of outliers in a BigQuery dataset. Which statistical functions identify outliers?", opts: ["Use DISTINCT to find unique values that occur only once", "Use STDDEV() and AVG() to identify rows where value > AVG + 3*STDDEV (Z-score method); or use APPROX_QUANTILES() to find P1 and P99 as outlier boundaries; or use BigQuery ML's k-means to cluster and identify outliers as small clusters", "Use MAX() and MIN() — any value between max and min is not an outlier", "Use ARRAY_AGG with ORDER BY and check the first/last elements"], ans: 1, explain: "Statistical outlier detection in BigQuery: (1) Z-score: WHERE ABS(value - AVG(value) OVER()) > 3 * STDDEV(value) OVER(); (2) IQR method using APPROX_QUANTILES for Q1/Q3; (3) BQML k-means clustering where small clusters indicate outliers. All three approaches are expressible in standard BigQuery SQL.", concept: "BigQuery outlier detection, statistical functions, Z-score, IQR" }, { q: "What is a BigQuery 'reservation assignment' and how does it control which workloads use reserved slots?", opts: ["Reservation assignments are BigQuery table-level tags for billing purposes", "A reservation assignment binds a project, folder, or organization to a specific reservation. Any BigQuery job submitted from the assigned project uses slots from that reservation — not from the on-demand pool. Unassigned projects use on-demand pricing", "Reservation assignments are a synonym for BigQuery IAM role assignments", "Reservation assignments are required for all BigQuery operations in Enterprise edition"], ans: 1, explain: "Reservations + Assignments: create a Reservation (slot pool), then create an Assignment linking a project/folder/org to the reservation. Jobs from assigned entities draw from the reservation's slot pool. Multiple reservations provide workload isolation — ETL jobs don't compete with analytics queries for slots.", concept: "BigQuery reservation assignments, slot pool management, workload isolation" }, { q: "You need to implement a Type 2 Slowly Changing Dimension (SCD) in BigQuery — tracking historical changes with start/end dates. What is the correct approach?", opts: ["Use BigQuery INSERT OVERWRITE to update the dimension table with current values", "Use BigQuery MERGE statement: WHEN MATCHED AND source differs from target THEN INSERT new row with effective_start = today and UPDATE old row's effective_end = yesterday", "Recreate the entire dimension table daily from source data", "Use BigQuery Append-Only mode and filter for the latest record at query time"], ans: 1, explain: "SCD Type 2 in BigQuery: MERGE target USING source ON natural_key WHEN MATCHED AND target.current_flag AND (differences exist) THEN mark old row (effective_end = today, current_flag = false) AND INSERT new row (effective_start = today, current_flag = true) WHEN NOT MATCHED THEN INSERT new row.", concept: "SCD Type 2, BigQuery MERGE, slowly changing dimensions, historical tracking" }, { q: "A data scientist exports BigQuery data to Pandas for EDA on a local machine. The table has 100 million rows. What is the efficient way to do this?", opts: ["Use bq extract to Cloud Storage then download the files", "Use the BigQuery Storage Read API with the BigQuery client library (read_gbq or BigQuery Storage Read API directly) — streams data directly to Pandas/Arrow without intermediate GCS export", "Download rows one by one using the REST API with pagination", "Use Google Sheets as an intermediate — export to Sheets, download as CSV"], ans: 1, explain: "BigQuery Storage Read API (available in google-cloud-bigquery via read_gbq or bigquery_storage) streams data directly to Pandas DataFrames via Apache Arrow format. This is 10-100x faster than the export-to-GCS method and doesn't require intermediate storage.", concept: "BigQuery Storage Read API, Pandas integration, read_gbq, Apache Arrow" }, { q: "You need to implement a pipeline that detects when a sensor reading deviates more than 3 standard deviations from its historical mean. The analysis must happen in BigQuery using available ML features. Which approach is most straightforward?", opts: ["Train a BigQuery ML isolation forest model for anomaly detection", "Compute historical mean and std per sensor using window functions; flag anomalies where |current - mean| > 3 * std in a SQL query. Alternatively, use BigQuery ML PCA or autoencoder models for multivariate anomaly detection", "Use Cloud AI Platform for statistical anomaly detection", "Export to Python pandas and apply scipy.stats functions"], ans: 1, explain: "SQL-based Z-score anomaly detection: SELECT *, ABS(value - AVG(value) OVER (PARTITION BY sensor_id ORDER BY ts ROWS BETWEEN 100 PRECEDING AND 1 PRECEDING)) / NULLIF(STDDEV(value) OVER (...), 0) AS z_score FROM readings. Flag rows where z_score > 3.", concept: "BigQuery anomaly detection, Z-score, window function analytics" }, { q: "What does BigQuery's PARTITION BY clause in window functions do versus CREATE TABLE PARTITION BY?", opts: ["They are identical — both partition data the same way", "PARTITION BY in window functions defines groups within which the window function is applied — it's a logical grouping within a query, not physical storage. CREATE TABLE PARTITION BY physically stores data in separate partitions on disk. One is a query-time analytical construct; the other is a physical storage optimization", "PARTITION BY in window functions physically splits the table", "CREATE TABLE PARTITION BY is deprecated in favor of window functions"], ans: 1, explain: "Two different PARTITION BY concepts: (1) Window function PARTITION BY logically divides rows into groups within a query for analytic functions (ROW_NUMBER, AVG, etc.) — no physical effect. (2) CREATE TABLE PARTITION BY physically stores data in separate partitions — enables partition pruning, expiration, and efficient filtering.", concept: "BigQuery window PARTITION BY vs table partitioning, logical vs physical partitioning" }, { q: "A marketing team needs a BigQuery query that ranks customers by lifetime value and assigns each to a decile (1-10). Which SQL approach implements this?", opts: ["Use RANK() OVER (ORDER BY ltv DESC) to assign ranks", "Use NTILE(10) OVER (ORDER BY ltv DESC) to assign each customer to one of 10 equally-sized groups (deciles)", "Use PERCENT_RANK() and multiply by 10", "Use CUME_DIST() to compute cumulative distribution and bucket into deciles"], ans: 1, explain: "NTILE(N) divides the result set into N buckets of approximately equal size, ordered by the specified column. NTILE(10) OVER (ORDER BY ltv DESC) assigns each customer a decile 1-10. NTILE handles ties by distributing them across buckets.", concept: "BigQuery NTILE, percentile bucketing, customer segmentation, window functions" }, { q: "You need to create a BigQuery dataset that is accessible to all authenticated users in your GCP organization for BI reporting. What is the correct IAM configuration?", opts: ["Grant roles/bigquery.dataViewer to allUsers (public access)", "Grant roles/bigquery.dataViewer to allAuthenticatedUsers — restricts to any Google account but not specifically your org. For org-wide internal access: grant to 'domain:yourcompany.com' which covers all Workspace users in the org", "Grant roles/bigquery.dataViewer to your org's Workspace group", "Create a public dataset with no access restrictions"], ans: 1, explain: "For organization-wide access: bind roles/bigquery.dataViewer to 'domain:yourcompany.com' — this grants access to all Google Workspace users in your domain automatically without managing individual accounts or groups. allAuthenticatedUsers is too broad (any Google account worldwide).", concept: "BigQuery IAM, domain-wide access, Workspace integration, org-wide datasets" }, { q: "Which BigQuery function efficiently computes a Bloom filter over a large dataset for approximate membership testing?", opts: ["BigQuery does not support Bloom filters natively", "BF_BUILD() creates a Bloom filter from a large column; BF_CONTAINS() tests membership. These are HyperLogLog-based probabilistic data structures with configurable false positive rates", "Use APPROX_COUNT_DISTINCT for membership testing", "Store all values in a BigQuery ARRAY and use OFFSET for lookups"], ans: 1, explain: "BigQuery (2023+) introduced Bloom filter functions: BF_BUILD(column, fp_rate) creates a binary Bloom filter blob from a large dataset; BF_CONTAINS(bloom_filter, value) tests whether a value was in the original dataset with controlled false positive rate. Useful for large-scale approximate joins.", concept: "BigQuery Bloom filters, BF_BUILD, BF_CONTAINS, approximate membership testing" }, { q: "You need to run a BigQuery job that requires reading from a table in Project A and writing to a table in Project B. The job runs under a service account in Project C. What billing project is charged?", opts: ["Project A — where the source data lives", "Project B — where results are written", "Project C — the project where the job runs (the billing project) is charged for query compute; Project A and B are charged for storage only", "Split evenly between Projects A and B based on data volume"], ans: 2, explain: "BigQuery charges compute (query processing) to the project running the job — Project C in this case (the billing project configured in the client). Source data storage stays in Project A; result storage in Project B. Only the job-running project is charged for compute.", concept: "BigQuery billing project, cross-project queries, compute vs storage charges" }, { q: "A data analyst needs to compare sales performance across multiple months side by side in a single BigQuery query row. Which technique achieves this?", opts: ["Multiple subqueries each returning one month, UNIONed together", "PIVOT or conditional aggregation: SUM(CASE WHEN month='2024-01' THEN amount END) AS jan_sales, SUM(CASE WHEN month='2024-02' THEN amount END) AS feb_sales, ... per product in a single query", "Create separate tables for each month and join them", "Use BigQuery ML time-series analysis"], ans: 1, explain: "Conditional aggregation (CASE WHEN) or BigQuery's PIVOT clause (added 2021) transforms row-oriented month data into columns. PIVOT (SUM(amount) FOR month IN ('2024-01' AS jan, '2024-02' AS feb, ...)) achieves the same result more declaratively.", concept: "BigQuery PIVOT, conditional aggregation, crosstab queries" }, { q: "What is the difference between BigQuery's correlated subquery and a JOIN, and when should you prefer each?", opts: ["There is no difference — BigQuery optimizes both identically", "A correlated subquery references columns from the outer query and is re-evaluated per row — potentially expensive. A JOIN is evaluated once and can be optimized by BigQuery's distributed engine. Prefer JOINs for performance; use correlated subqueries when the logic requires row-by-row evaluation (e.g., complex EXISTS conditions)", "Correlated subqueries are always faster — they avoid the data shuffle of a JOIN", "BigQuery does not support correlated subqueries"], ans: 1, explain: "In BigQuery's distributed execution, JOIN operations are highly optimized (broadcast or hash join). Correlated subqueries (SELECT ... WHERE x = outer_table.x) may be re-executed per row in the outer query, creating N subquery executions. BigQuery often rewrites correlated subqueries as JOINs, but complex ones may not be rewritten — test with EXPLAIN.", concept: "BigQuery correlated subqueries vs JOINs, query optimization, execution plan" }, { q: "A team uses Google Data Studio (now Looker Studio) and needs to blend data from BigQuery and Google Analytics 4 in a single report. What is the recommended approach?", opts: ["Export GA4 data to a spreadsheet and use manual VLOOKUP", "Export GA4 data to BigQuery using GA4's BigQuery Export; join with other BigQuery data in SQL or Looker Studio data blending; use the unified BigQuery connector in Looker Studio", "Use Looker Studio data blending to combine the BigQuery connector and GA4 connector in the report", "Use Cloud Data Fusion to merge GA4 and BigQuery data"], ans: 1, explain: "GA4 BigQuery Export streams events to BigQuery daily. Once in BigQuery, you can JOIN with CRM, ad spend, or other datasets using SQL. Looker Studio connects to BigQuery for the unified dataset. This is more powerful than Looker Studio data blending (which has join limitations).", concept: "GA4 BigQuery Export, Looker Studio, cross-source analytics" }, { q: "A Vertex AI Feature Store serves online features for a recommendation model. A data engineer needs to update features in real-time (sub-second) as user behavior changes. Which ingestion pattern achieves this?", opts: ["Batch ingest features from BigQuery to Feature Store once per day", "Use Pub/Sub → Dataflow → Vertex AI Feature Store online ingestion API (FeaturestoreService.WriteFeatureValues) for real-time feature updates with sub-second latency", "Use Cloud Composer to trigger feature ingestion every minute", "Use BigQuery scheduled queries to push features to Feature Store hourly"], ans: 1, explain: "For real-time feature updates: Pub/Sub captures user behavior events → Dataflow processes and computes features → WriteFeatureValues API updates the Feature Store online serving layer. This enables features to reflect user behavior within seconds of it occurring.", concept: "Vertex AI Feature Store real-time ingestion, WriteFeatureValues, streaming features" }, { q: "You need to detect and remediate personally identifiable information (PII) in BigQuery query results before they reach an analytics dashboard. Which pipeline handles this?", opts: ["Review query results manually before publishing to the dashboard", "BigQuery Dynamic Data Masking with policy tags: tag PII columns; grant dashboard service account the masked data role — PII appears as hashed or nulled values automatically", "Cloud DLP must scan each query result exported to Cloud Storage before the dashboard reads it", "Use authorized views that exclude PII columns from the dashboard's dataset"], ans: 1, explain: "Dynamic Data Masking is the right tool here: tag PII columns (SSN, email, DOB) with a taxonomy policy tag; grant the dashboard's service account only the Masked Data Reader role (not Fine-Grained Reader). The dashboard automatically receives masked values — hashed SSN, nulled DOB, etc.", concept: "BigQuery Dynamic Data Masking, PII protection, dashboard access control" }, { q: "What is BigQuery's table sampling function TABLESAMPLE and when is it appropriate?", opts: ["TABLESAMPLE creates a permanent sample table at reduced cost", "TABLESAMPLE SYSTEM (N PERCENT) returns an approximate N% random sample of rows — read from a random selection of BigQuery storage blocks. Appropriate for exploratory data analysis, model prototyping, and approximate analytics where scanning the full table is cost-prohibitive", "TABLESAMPLE provides exactly N% of rows — deterministic and repeatable", "TABLESAMPLE is only available for tables under 1 TB"], ans: 1, explain: "SELECT * FROM table TABLESAMPLE SYSTEM (10 PERCENT) returns approximately 10% of rows (sampled at block level — not exact row count). Cost is approximately 10% of a full table scan. Use for EDA, testing transformations, or validating logic before running on full data.", concept: "BigQuery TABLESAMPLE, random sampling, cost-effective EDA" }, { q: "You have a BigQuery dataset that users query with many different filter combinations on 5 columns. How do you optimize this without creating a separate view or index for each combination?", opts: ["Create 32 (2^5) separate materialized views for each filter combination", "Cluster the BigQuery table on the 5 most frequently filtered columns (BigQuery supports up to 4 clustering columns); queries with any prefix subset of clustering columns benefit from block elimination", "Create a separate partition per filter combination", "BigQuery cannot optimize queries with multiple filter combinations"], ans: 1, explain: "BigQuery clustering eliminates blocks based on any left-prefix of the clustering columns. If you cluster on (region, product, customer_tier, channel), queries filtering on region alone, region+product, region+product+tier all benefit from block elimination. Queries not starting with region (the first cluster key) don't benefit.", concept: "BigQuery clustering, block elimination, multi-column filter optimization" }, { q: "A data scientist needs to run feature importance analysis after training a BQML model. Which BQML function provides this?", opts: ["ML.EVALUATE — evaluates model accuracy metrics", "ML.FEATURE_IMPORTANCE — returns the importance of each feature used in training a supported model type (linear regression, boosted trees, random forest)", "ML.EXPLAIN_PREDICT — explains individual prediction outputs", "ML.TRAINING_INFO — shows training loss curves per iteration"], ans: 1, explain: "ML.FEATURE_IMPORTANCE(MODEL model_name) returns a table with each feature's importance score (relative contribution to model predictions). Supported for: linear regression (coefficients), boosted trees (gain/weight), random forest, AutoML. Use it after training to identify the most predictive features.", concept: "BigQuery ML feature importance, ML.FEATURE_IMPORTANCE, model interpretability" }, { q: "You need to apply a complex custom business rule to categorize BigQuery rows that cannot be expressed in standard SQL. Which feature extends BigQuery with custom logic?", opts: ["BigQuery stored procedures for complex if/else logic", "BigQuery Remote Functions — define a Cloud Function or Cloud Run service with the custom logic; register it as a BigQuery function; call it in SQL like a regular function", "Create a Dataflow pipeline to apply the classification", "Use CASE WHEN for all business rules no matter how complex"], ans: 1, explain: "BigQuery Remote Functions bridge BigQuery SQL with external compute: CREATE FUNCTION my_func(x STRING) RETURNS STRING REMOTE WITH CONNECTION ... RETURNS your-cloud-run-url. SQL calls the function; Cloud Run executes the Python/Java/Go business logic. Enables any custom logic accessible in SQL.", concept: "BigQuery Remote Functions, custom logic, Cloud Run integration, function extension" }, { q: "What is the BigQuery INFORMATION_SCHEMA.PARTITIONS view used for?", opts: ["PARTITIONS view shows all BigQuery tables in a project", "PARTITIONS view shows metadata for each partition in a partitioned table: partition_id, total_rows, total_logical_bytes, last_modified_time. Data engineers use it to monitor partition freshness, size imbalances, and identify empty partitions", "PARTITIONS view shows which IAM principals have access to each partition", "PARTITIONS view is used to DROP old partitions programmatically"], ans: 1, explain: "INFORMATION_SCHEMA.PARTITIONS provides per-partition metadata: SELECT partition_id, total_rows, last_modified_time FROM dataset.INFORMATION_SCHEMA.PARTITIONS WHERE table_name = 'my_table'. Essential for: detecting stale partitions (data freshness), monitoring partition size distribution, and identifying missing partitions in pipelines.", concept: "BigQuery INFORMATION_SCHEMA.PARTITIONS, partition metadata, data freshness monitoring" }, { q: "A analytics engineer uses ARRAY_AGG() in BigQuery to collect all purchases per customer. What does ARRAY_AGG(DISTINCT product_id ORDER BY amount DESC LIMIT 10) compute?", opts: ["An unordered array of all unique product_ids per customer", "The top 10 distinct product_ids per customer ordered by total amount descending — ARRAY_AGG with DISTINCT, ORDER BY, and LIMIT all applied within the aggregate function", "The 10 most common product_ids across all customers", "An error — ARRAY_AGG does not support ORDER BY or LIMIT"], ans: 1, explain: "BigQuery's ARRAY_AGG supports modifiers: DISTINCT removes duplicates before aggregation; ORDER BY sorts elements within the array; LIMIT caps array size. Combined: ARRAY_AGG(DISTINCT product_id ORDER BY amount DESC LIMIT 10) returns the 10 highest-amount distinct products per customer as an ordered array.", concept: "BigQuery ARRAY_AGG, aggregate function modifiers, DISTINCT ORDER BY LIMIT" }, { q: "What is Vertex AI's role in GCP's data engineering stack for model serving?", opts: ["Vertex AI is only for model training; Cloud Functions serve model predictions", "Vertex AI provides end-to-end ML lifecycle: model training (custom containers, AutoML), Model Registry (versioning), Online Prediction (REST endpoint with autoscaling), Batch Prediction (BigQuery or GCS input/output), and monitoring (model drift, skew). Data engineers integrate Vertex AI endpoints into data pipelines for real-time scoring", "Vertex AI only serves TensorFlow models; other frameworks require Dataproc", "Vertex AI replaces BigQuery ML entirely for all ML use cases"], ans: 1, explain: "Vertex AI Online Prediction deploys any ML framework (TF, PyTorch, XGBoost, scikit-learn, custom containers) as autoscaling REST endpoints. Data pipelines call these endpoints via HTTP for real-time scoring. Batch Prediction takes BigQuery tables as input and writes results back to BigQuery — no endpoint needed for batch workflows.", concept: "Vertex AI, model serving, online prediction, batch prediction, MLOps" }, { q: "You need to create a BigQuery view that aggregates data from 3 source tables. How do you grant access to the view without granting access to the underlying tables?", opts: ["Grant access to all 3 source tables to the view users", "Create the view in a separate dataset; authorize the view to access the source tables (Dataset Settings → Authorized Views); grant users access only to the view dataset — users cannot query source tables directly", "Use BigQuery materialized views — they automatically restrict source table access", "Apply Column-Level Security to the source tables to hide data from non-authorized views"], ans: 1, explain: "Authorized views pattern: (1) Create the view in dataset_B referencing source tables in dataset_A; (2) In dataset_A settings, add dataset_B.view as an Authorized View — this grants the view service access to the source tables; (3) Grant users roles/bigquery.dataViewer on dataset_B only. Users see view results but cannot access source tables.", concept: "BigQuery authorized views, cross-dataset access, view-level security" }, { q: "A data team wants to use BigQuery for both analytics and ML training. The ML training requires the last 2 years of data; analytics queries typically look at the last 30 days. How do you optimize the table design for both use cases?", opts: ["Create two separate tables: one 30-day for analytics, one 2-year for ML", "Partition the table by month (DATE_TRUNC(event_date, MONTH)) and cluster by feature columns. Analytics queries filter to the last 30 days (3 partitions scanned); ML training filters to 2 years (24 partitions scanned). One table, optimal for both", "Use BigQuery materialized views for analytics and the raw table for ML", "Create a separate BigQuery dataset for ML with a copy of 2 years of data"], ans: 1, explain: "Monthly partitioning: analytics queries scan 1-3 partitions (last 30 days = 1-2 months); ML training scans 24 partitions (2 years). Both use cases benefit from the same partition strategy. Clustering on feature columns further optimizes ML training queries that filter by specific features.", concept: "BigQuery partition design for multiple use cases, analytics vs ML optimization" }, { q: "What is the purpose of BigQuery's BI Engine memory reservations?", opts: ["BI Engine memory is allocated per-query to hold query intermediate results", "BI Engine memory reservations purchase dedicated in-memory capacity for a specific BigQuery project/location. Queries that fit within the memory are served from memory (sub-second); queries that don't fit fall back to standard BigQuery execution", "BI Engine memory replaces the need for BigQuery slots — you only pay for memory", "BI Engine memory is shared across all GCP projects in an organization"], ans: 1, explain: "BI Engine reservations: purchase X GB of in-memory capacity for a project in a region. Looker, Looker Studio, and direct SQL queries on eligible tables are accelerated using the in-memory data. Queries exceeding memory fall back to standard BigQuery. Memory is not per-query — it's shared capacity for all concurrent queries.", concept: "BigQuery BI Engine, in-memory reservations, sub-second query acceleration" }, { q: "You need to join two BigQuery tables: Table A (10 TB) and Table B (50 MB). Which join optimization does BigQuery automatically apply?", opts: ["BigQuery applies a hash join on the larger table and sort-merge on the smaller", "BigQuery automatically applies a broadcast join (shuffle-free join): Table B (50 MB) is broadcast to all workers processing Table A shards; workers perform local lookups against the broadcast copy — no Table B shuffle needed", "BigQuery requires identical partitioning on both tables for efficient joins", "BigQuery does not optimize joins — performance depends on query structure"], ans: 1, explain: "BigQuery's distributed query engine automatically detects when one table is small enough for a broadcast join. The 50 MB table is distributed to all workers; each worker holds a complete copy in memory and performs local joins against its shard of the 10 TB table. No shuffle needed for the small table.", concept: "BigQuery broadcast join, automatic join optimization, large-small table joins" }, { q: "What does the BigQuery function JSON_VALUE() do and when would you use JSON_QUERY() instead?", opts: ["JSON_VALUE() and JSON_QUERY() are identical", "JSON_VALUE() extracts a scalar (STRING, NUMBER, BOOL) from a JSON string and returns it as a STRING. JSON_QUERY() extracts a JSON sub-object or array and returns it as a JSON STRING (preserving the JSON structure). Use JSON_QUERY() when the result is a nested object or array, not a scalar value", "JSON_QUERY() is for BigQuery JSON type; JSON_VALUE() is for STRING-stored JSON", "JSON_VALUE() returns NULL for nested JSON; JSON_QUERY() handles all nesting levels"], ans: 1, explain: "JSON_VALUE('{'a':{'b':1}}', '$.a.b') returns '1' (string scalar). JSON_QUERY('{'a':{'b':1}}', '$.a') returns '{\"b\":1}' (JSON object as string). Use JSON_VALUE for leaf values; JSON_QUERY when you need a sub-object to further parse or pass to another JSON function.", concept: "BigQuery JSON_VALUE vs JSON_QUERY, JSON extraction, nested JSON handling" }, { q: "A data analyst notices that their BigQuery query ran in 10 seconds yesterday but takes 5 minutes today with the same query. The table size hasn't changed. What could cause this?", opts: ["BigQuery caches all queries — the query must be different", "Possible causes: (1) table was recently modified (bypassing query cache); (2) slots were consumed by concurrent high-priority jobs leaving fewer slots for this query; (3) data skew increased in a partition; (4) a new column with high cardinality was added affecting JOIN performance. Use INFORMATION_SCHEMA.JOBS_TIMELINE to compare slot consumption and execution stages", "The BigQuery service had downtime — check the GCP Status page", "The query plan changed — BigQuery randomly changes execution strategies"], ans: 1, explain: "Variable query performance with same query and table size: (1) Query cache miss (table modified → no cache hit); (2) Slot contention (concurrent jobs); (3) Data distribution change (skew). Diagnose: check INFORMATION_SCHEMA.JOBS for today's vs yesterday's slot_ms and bytes_processed. Compare execution stage timings in the query plan.", concept: "BigQuery query performance variability, slot contention, query cache, diagnosis" }, { q: "You need to run a Vertex AI Batch Prediction job using a BigQuery table as input and write results back to BigQuery. What permissions does the Vertex AI service account need?", opts: ["roles/bigquery.admin on all datasets in the project", "roles/bigquery.dataViewer on the input dataset (to read input rows) and roles/bigquery.dataEditor on the output dataset (to write prediction results). Vertex AI uses a default service agent that needs these minimal permissions", "roles/bigquery.jobUser only — it can read and write automatically", "No additional permissions — Vertex AI has built-in BigQuery access"], ans: 1, explain: "Vertex AI Batch Prediction with BigQuery: the Vertex AI service agent needs bigquery.dataViewer on the input table's dataset to read feature rows, and bigquery.dataEditor on the output dataset to write prediction results. Apply the principle of least privilege — not project-level admin.", concept: "Vertex AI Batch Prediction, BigQuery permissions, IAM least privilege" }, { q: "BigQuery supports 'connected sheets' which directly links Google Sheets to BigQuery. What is the primary advantage over Looker Studio for non-technical users?", opts: ["Connected Sheets supports custom SQL queries via Looker Studio", "Connected Sheets brings BigQuery data into familiar Google Sheets interface: pivot tables, charts, and formulas on BigQuery data using Sheets' UI without SQL knowledge. Looker Studio requires understanding the connector interface. Connected Sheets runs BigQuery queries transparently when users refresh", "Connected Sheets is faster than Looker Studio for large queries", "Connected Sheets replaces BigQuery ML for data analysts"], ans: 1, explain: "Connected Sheets: non-technical users create pivot tables, charts, and formulas in Google Sheets backed by BigQuery. When they refresh or change the pivot, Sheets runs the corresponding BigQuery SQL automatically. This democratizes BigQuery analytics for business users who know Excel/Sheets but not SQL.", concept: "BigQuery Connected Sheets, non-technical analytics, Sheets-BigQuery integration" }, { q: "What is the recommended approach for handling slowly-changing configuration parameters in BigQuery SQL queries (e.g., tax rates that change quarterly)?", opts: ["Hardcode tax rates in every SQL query — simplest approach", "Store configuration in a separate BigQuery lookup table; JOIN in SQL queries. Alternatively, use BigQuery scripting (DECLARE @tax_rate) set from a config table. This centralizes config and avoids updating 100s of queries when rates change", "Use Cloud Firestore to store config and call via Remote Functions in each query", "Use BigQuery ML model parameters to store configuration values"], ans: 1, explain: "Centralized config management: create a bigquery_config.parameters table with effective_start_date, parameter_name, value. SQL queries JOIN this table WHERE CURRENT_DATE BETWEEN effective_start AND effective_end to pick the active rate. Changing a rate requires updating only the config table, not all queries.", concept: "BigQuery configuration management, lookup tables, centralized parameters" }, { q: "A data team uses Dataform for BigQuery transformations. How does Dataform handle incremental table updates to avoid reprocessing historical data?", opts: ["Dataform always recreates tables from scratch (full refresh only)", "Dataform incremental tables: define a SQLX model with config type='incremental' and an is_incremental() conditional. On first run: full insert. On subsequent runs: WHERE clause filters to new data only; Dataform uses INSERT, MERGE, or INSERT OVERWRITE (depending on strategy) to add only new records", "Dataform uses BigQuery time travel to identify changed rows since last run", "Dataform incremental tables require partitioned tables and partition pruning"], ans: 1, explain: "Dataform incremental models: config { type: 'incremental' } with ${when(incremental(), 'WHERE timestamp > (SELECT MAX(timestamp) FROM ' + self() + ')', '')} in the WHERE clause. Dataform detects if the table exists; on first run inserts all; on subsequent runs appends/merges only new rows.", concept: "Dataform incremental tables, is_incremental(), SQLX, BigQuery incremental processing" }, { q: "You need to debug why a BigQuery scheduled query is producing incorrect aggregation results. What information should you check first?", opts: ["Check the Cloud Storage bucket for corrupted source files", "Review the query's @run_date parameter substitution — if the query uses @run_date for incremental filtering and the parameter is incorrectly set (e.g., UTC vs local time), incorrect date ranges may be aggregated. Also verify the WRITE_DISPOSITION (WRITE_TRUNCATE vs WRITE_APPEND) is correct for idempotent reruns", "The issue is always a data quality problem in the source — run Cloud DLP", "Check if the BigQuery dataset was accidentally deleted and recreated"], ans: 1, explain: "Scheduled query debugging checklist: (1) @run_date parameter value — does it match the expected date? Is timezone handling correct? (2) WRITE_DISPOSITION — WRITE_APPEND accumulates data on rerun (double-counting); WRITE_TRUNCATE is idempotent. (3) Query logic — test manually with the specific @run_date that produced incorrect results.", concept: "BigQuery scheduled queries, @run_date parameter, WRITE_DISPOSITION debugging" }, { q: "A financial analyst wants to run what-if analysis in BigQuery: 'What would total revenue be if we increased the price of Product X by 10%?' Which BigQuery feature supports this without modifying data?", opts: ["Create a copy of the table with modified prices", "Use BigQuery scripting: DECLARE @price_increase FLOAT64 DEFAULT 1.10; SELECT product, SUM(quantity * unit_price * @price_increase) AS adjusted_revenue FROM sales WHERE product = 'X' GROUP BY product", "Create a BigQuery ML model to predict revenue under different price scenarios", "Use Looker Studio calculated fields for what-if analysis"], ans: 1, explain: "BigQuery scripting with DECLARE/SET enables parameterized what-if queries: declare a variable for the price multiplier, apply it in the SELECT clause. This runs as a single script without data modification. For interactive what-if, BigQuery BI Engine + Looker Studio parameters can pass values dynamically.", concept: "BigQuery scripting, what-if analysis, parameterized queries, DECLARE/SET" }, { q: "You need to calculate customer lifetime value (CLV) in BigQuery as the sum of all transactions weighted by a time decay factor (more recent = higher weight). Which SQL approach implements this efficiently?", opts: ["Use CASE WHEN with 10 date ranges to manually assign weights", "Use a window function or correlated calculation: SUM(amount * EXP(-decay_rate * DATE_DIFF(CURRENT_DATE, transaction_date, DAY))) OVER (PARTITION BY customer_id) — applies exponential time decay to all transactions in a single pass", "Create a separate BigQuery ML regression model for CLV prediction", "Export to Python for scipy-based weighted calculations"], ans: 1, explain: "Time-decay weighted sum in BigQuery: EXP(-lambda * days_ago) is the exponential decay factor. Combining with SUM() OVER (PARTITION BY customer_id) computes CLV per customer in a single pass. The decay rate (lambda) controls how fast historical transactions diminish in value.", concept: "BigQuery time-decay functions, EXP, weighted aggregation, CLV calculation" }, { q: "What is the effect of running CREATE OR REPLACE TABLE in BigQuery?", opts: ["It fails if the table already exists — use ALTER TABLE to modify existing tables", "CREATE OR REPLACE TABLE atomically drops the existing table and creates a new one with the specified schema and data. Any existing IAM bindings, time travel history, and partition configurations are lost. Use CTAS (CREATE TABLE AS SELECT) for non-destructive operations", "CREATE OR REPLACE TABLE only replaces schema, not data — existing rows are preserved", "CREATE OR REPLACE TABLE is an incremental operation — it merges new schema with existing"], ans: 1, explain: "CREATE OR REPLACE TABLE is destructive: it drops and recreates the table atomically. Consequences: (1) time travel history is reset (cannot recover data before the replace); (2) IAM bindings on the table are lost; (3) partition/clustering configuration must be re-specified. For non-destructive updates, use MERGE or INSERT.", concept: "BigQuery CREATE OR REPLACE TABLE, destructive DDL, time travel reset" }, { q: "Vertex AI Pipelines vs Cloud Composer — when would you use Vertex AI Pipelines for ML workflows instead of Cloud Composer?", opts: ["Cloud Composer is always preferred for all workflow orchestration including ML", "Vertex AI Pipelines (Kubeflow Pipelines) is optimized for ML workflows: built-in component caching (re-run only failed steps), artifact lineage tracking, ML metadata storage, and native Vertex AI integration (AutoML, training jobs, model registry). Cloud Composer is better for data engineering workflows mixing ML with non-ML steps", "Vertex AI Pipelines is only for batch prediction; Cloud Composer handles training pipelines", "They are identical; choose based on team preference"], ans: 1, explain: "Choose Vertex AI Pipelines for pure ML workflows: experiment tracking, component caching, and ML metadata are built-in. Cloud Composer is better for heterogeneous workflows mixing Dataflow, BigQuery, and ML steps — its Airflow operators cover all GCP services. For pure ML → Vertex AI Pipelines; for mixed data + ML → Cloud Composer.", concept: "Vertex AI Pipelines vs Cloud Composer, ML workflow orchestration, pipeline selection" }, { q: "You need to run ad-hoc queries on BigQuery for exploratory analysis without incurring costs for repeated full table scans. What query patterns minimize cost?", opts: ["Always use SELECT * for simplicity — BigQuery charges per job, not per data scanned", "Use SELECT only needed columns (column pruning reduces bytes scanned); add WHERE clause filters on partitioned/clustered columns; use LIMIT with TABLESAMPLE for exploration; preview with bq query --dry_run to estimate bytes before executing", "BigQuery has a free tier of 1 TB/month — explore without worrying about cost", "Use SELECT DISTINCT to reduce returned rows and therefore cost"], ans: 1, explain: "BigQuery cost = bytes scanned. Cost reduction: (1) SELECT only needed columns (columnar storage scans only those columns); (2) WHERE on partition/cluster keys prunes data; (3) TABLESAMPLE scans a fraction of data; (4) --dry_run estimates cost before execution. SELECT DISTINCT doesn't reduce bytes scanned — the full scan happens then DISTINCT is applied.", concept: "BigQuery cost optimization, column pruning, partition filtering, exploratory queries" }, { q: "What is the recommended way to share aggregated BigQuery data with external partners while ensuring underlying individual records are not exposed?", opts: ["Grant the partner direct table access and trust them not to query individual records", "Create BigQuery authorized views with GROUP BY aggregations (e.g., by region and week) — partners see only aggregated results. For additional protection, apply BigQuery differential privacy aggregation functions (with noise injection) to prevent re-identification from aggregates", "Export data to Cloud Storage as pre-aggregated CSV files", "Use BigQuery column-level encryption — partners can decrypt only aggregate keys"], ans: 1, explain: "Authorized views with aggregations prevent individual record access. For regulatory-grade protection, BigQuery's differential privacy functions (ANON_SUM, ANON_AVG, ANON_COUNT with privacy budget parameters) add mathematical noise to aggregate results, preventing re-identification even from aggregate data.", concept: "BigQuery authorized views, differential privacy, data sharing, ANON functions" }, { q: "You need to run a BigQuery query that processes 1 TB of data hourly for 30 days = 720 TB/month. With on-demand pricing at $5/TB, the monthly cost is $3,600. When would switching to Reservations (flat-rate) be cost-effective?", opts: ["Reservations are never cost-effective for predictable workloads", "If 720 TB/month of consistent queries uses approximately 1,000 slots continuously, the monthly Reservation cost (1,000 slots × $0.04/slot-hr × 720 hrs = $28,800) would be MORE expensive. Reservations break even only when workload uses slots heavily (high concurrency or large concurrent jobs) — at 1 TB/hr sequential, on-demand is cheaper", "Reservations are always 50% cheaper than on-demand", "Switch to Reservations immediately — they provide guaranteed performance"], ans: 1, explain: "Cost comparison depends on actual slot consumption, not just bytes. 1 TB processed sequentially may use only 200-500 slots for 30 seconds. Monthly slot-hours may be low, making on-demand ($3,600) cheaper than Reservations. Reservations excel when continuous high slot utilization fills the reserved capacity.", concept: "BigQuery cost analysis, Reservations vs on-demand, slot utilization economics" }, { q: "What is a BigQuery authorized dataset and how does it differ from an authorized view?", opts: ["They are identical — authorized dataset is just the new term for authorized view", "An authorized view grants a specific view access to query data in another dataset. An authorized dataset grants ALL views and tables within an entire dataset cross-dataset query access — useful when you have many views in a shared analysis dataset that all need access to the source data dataset", "Authorized datasets are for external data; authorized views are for internal data", "Authorized datasets require BigQuery Enterprise Edition"], ans: 1, explain: "Authorized views: grant one specific view access to another dataset's tables. Authorized datasets: grant an entire dataset (all its resources) access to another dataset. Use authorized datasets when you have a reporting dataset with 20+ views that all need access to the same source dataset — one authorization covers all instead of authorizing each view individually.", concept: "BigQuery authorized datasets vs authorized views, cross-dataset access, dataset authorization" }, { q: "You need to enable BigQuery to query data stored in an Apache Iceberg format on Cloud Storage. Which configuration is needed?", opts: ["BigQuery cannot query Iceberg format directly", "Create a BigLake Managed Table or a BigLake external table with file_format=ICEBERG pointing to the Iceberg table's metadata.json location. BigQuery then reads Iceberg snapshots and respects partition evolution, schema evolution, and time travel from Iceberg", "Use Dataproc Spark to convert Iceberg to Parquet before BigQuery can read it", "Create a standard external table — BigQuery auto-detects Iceberg format"], ans: 1, explain: "BigQuery + Iceberg: create a BigLake table (CREATE EXTERNAL TABLE ... WITH CONNECTION ... OPTIONS (format='ICEBERG', uris=['gs://bucket/iceberg_table'])) pointing to the Iceberg table path. BigQuery reads the Iceberg metadata (snapshot selection, partition pruning) and data files (Parquet). Supports ACID reads and time travel.", concept: "BigQuery Iceberg integration, BigLake, open table formats" }] },

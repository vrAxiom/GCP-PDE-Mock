[
  {
    "q": "Your BigQuery table has 5 years of transaction data. Analysts query only the last 30 days. The table currently costs $8,000/month in query costs. What is the single most impactful optimization?",
    "opts": [
      "Enable BigQuery BI Engine for sub-second queries",
      "Partition the table on transaction_date and ensure queries include a date filter — BigQuery will prune irrelevant partitions",
      "Cluster the table on customer_id to speed up individual customer queries",
      "Move data older than 30 days to Cloud Storage Nearline"
    ],
    "ans": 1,
    "explain": "Partitioning on transaction_date means queries with WHERE transaction_date >= DATE_SUB(CURRENT_DATE(), INTERVAL 30 DAY) scan only 30 of 1,825 partitions — drastically reducing bytes scanned and cost.",
    "concept": "BigQuery partitioning, partition pruning, cost optimization"
  },
  {
    "q": "You need a database for a global e-commerce platform: strong consistency, ACID transactions, SQL queries, horizontal scaling, and 99.999% availability across 3 continents. Which database is the only GCP-native option meeting ALL requirements?",
    "opts": [
      "Cloud SQL with multi-region read replicas",
      "Cloud Spanner with multi-region configuration",
      "Firestore in Native mode with multi-region",
      "BigQuery with ACID transactions via DML"
    ],
    "ans": 1,
    "explain": "Cloud Spanner uniquely provides externally consistent ACID transactions, SQL support, horizontal scaling without re-sharding, and 99.999% SLA with multi-region configurations.",
    "concept": "Cloud Spanner, global consistency, horizontal scaling, ACID transactions"
  },
  {
    "q": "A financial application stores time-series account balances in Bigtable. Row keys are formatted as account_id#timestamp. Over time, performance degrades as recent data overwhelms older data with hotspot patterns. What is the root cause and fix?",
    "opts": [
      "Timestamps as the trailing part of row keys cause monotonically increasing keys, routing all new writes to the same tablet. Fix: reverse the timestamp or use a hashed prefix",
      "Bigtable automatically handles time-series data — performance degradation is a hardware issue",
      "The column family count is too high; reduce to 1 column family",
      "Increase Bigtable node count to distribute tablet pressure"
    ],
    "ans": 0,
    "explain": "Sequential/monotonically increasing row keys create write hotspots — all new writes go to the last tablet. Reversing the timestamp scatters recent writes across all tablets.",
    "concept": "Bigtable row key design, hotspot prevention, reversed timestamps"
  },
  {
    "q": "You need to choose a Cloud Storage storage class for a disaster recovery archive that must be retained for 7 years but will only be accessed if a disaster occurs (< once per year). What class minimizes total cost?",
    "opts": [
      "Standard — highest availability for critical DR data",
      "Nearline — for data accessed less than once per month",
      "Coldline — for data accessed less than once per quarter",
      "Archive — for data accessed less than once per year with 365-day minimum storage duration"
    ],
    "ans": 3,
    "explain": "Cloud Storage Archive class is designed for data accessed fewer than once per year with the lowest storage cost ($0.0012/GB/month) — perfect for DR archives held for 7 years.",
    "concept": "Cloud Storage storage classes, Archive, cost-access tradeoffs"
  },
  {
    "q": "Which BigQuery feature allows you to query data in Cloud Storage (Parquet, ORC, CSV) without loading it into BigQuery native storage?",
    "opts": [
      "BigQuery Materialized Views over Cloud Storage",
      "BigQuery External Tables (using CREATE EXTERNAL TABLE or federated queries)",
      "BigQuery Data Transfer Service with Cloud Storage source",
      "BigQuery Omni for multi-cloud queries"
    ],
    "ans": 1,
    "explain": "BigQuery External Tables allow SQL queries directly against data in Cloud Storage without ingestion. Supported formats include Parquet, ORC, CSV, Avro, JSON.",
    "concept": "BigQuery External Tables, federated queries, Cloud Storage formats"
  },
  {
    "q": "Your Firestore data model has a collection of 10 million 'orders' documents. A query filters on customer_id and then orders by created_at. What must you create to support this query efficiently?",
    "opts": [
      "A single-field index on customer_id only",
      "A composite index on (customer_id ASC, created_at ASC) — Firestore requires composite indexes for multi-field queries",
      "No index needed; Firestore auto-indexes all fields",
      "A Collection Group index on all subcollections"
    ],
    "ans": 1,
    "explain": "Firestore automatically creates single-field indexes. However, queries that filter on one field AND order/filter on another require a composite index — without it, Firestore returns an error.",
    "concept": "Firestore composite indexes, query limitations, index management"
  },
  {
    "q": "You are designing a BigQuery table for storing user events. The table receives 100K inserts/second via the Storage Write API. Which combination of settings ensures both ingest performance and query performance?",
    "opts": [
      "Partition by ingestion time (_PARTITIONTIME), cluster by user_id and event_type, use committed mode streams in Storage Write API",
      "Use a single unpartitioned table with no clustering",
      "Partition by user_id (string hash); Storage Write API will auto-balance",
      "Enable materialized views — they replace the need for partitioning"
    ],
    "ans": 0,
    "explain": "Ingestion-time partitioning auto-assigns rows to date partitions. Clustering on user_id and event_type enables efficient block elimination. Committed mode Storage Write API provides exactly-once guarantees.",
    "concept": "BigQuery ingestion-time partitioning, clustering, Storage Write API"
  },
  {
    "q": "A company needs to encrypt BigQuery data with their own encryption key stored in an on-premises HSM. Which solution supports this?",
    "opts": [
      "Default Google-managed encryption keys (GMEK)",
      "Customer-managed encryption keys (CMEK) using Cloud KMS",
      "Customer-supplied encryption keys (CSEK) via Cloud External Key Manager (Cloud EKM) pointing to on-premises HSM",
      "AES-256 encryption applied at the application layer before insertion"
    ],
    "ans": 2,
    "explain": "Cloud External Key Manager (Cloud EKM) allows GCP services to use encryption keys stored in third-party key management systems like on-premises HSMs. CSEK with Cloud EKM is the mechanism for using HSM-resident keys with BigQuery.",
    "concept": "CMEK, CSEK, Cloud EKM, HSM integration, BigQuery encryption"
  },
  {
    "q": "What is the maximum number of columns (fields) supported in a single BigQuery table?",
    "opts": [
      "1,000",
      "10,000",
      "15,000",
      "Unlimited"
    ],
    "ans": 1,
    "explain": "BigQuery supports up to 10,000 columns per table. Consider using nested STRUCT or ARRAY fields to group related columns in wide-table designs.",
    "concept": "BigQuery limits, schema design, nested/repeated fields"
  },
  {
    "q": "You want to ensure that a BigQuery dataset is only accessible from within your VPC and not from the public internet. Which control enforces this?",
    "opts": [
      "Set IAM bindings to domain-restricted sharing for your organization",
      "Enable Private Google Access on the subnet and configure VPC Service Controls with an access level for the BigQuery API",
      "Use a BigQuery authorized view to limit access",
      "Enable BigQuery Column-Level Security on all sensitive columns"
    ],
    "ans": 1,
    "explain": "VPC Service Controls creates a security perimeter. Adding BigQuery to the perimeter and configuring access levels to allow only traffic from your VPC via Private Google Access ensures BigQuery is inaccessible from the public internet.",
    "concept": "VPC Service Controls, Private Google Access, network-based access control"
  },
  {
    "q": "What is the purpose of BigQuery's INFORMATION_SCHEMA and when would a data engineer use it?",
    "opts": [
      "It stores the actual data in BigQuery tables",
      "It provides metadata views (TABLES, COLUMNS, JOBS, PARTITIONS, etc.) used for governance, auditing, and capacity planning",
      "It is the UI layer for BigQuery Studio",
      "It contains GCP billing data for BigQuery usage"
    ],
    "ans": 1,
    "explain": "INFORMATION_SCHEMA is a set of read-only views exposing BigQuery metadata: table schemas, job history, partition info, slot usage. Data engineers use it to audit slow queries, monitor data freshness, and optimize reservations.",
    "concept": "BigQuery INFORMATION_SCHEMA, metadata queries, operational monitoring"
  },
  {
    "q": "Cloud Bigtable is being used to store IoT sensor readings (10M devices, 1 reading/second each). The SLA requires p99 read latency < 10ms. The cluster has 10 nodes and CPU is at 80%. What should you do?",
    "opts": [
      "Switch from Bigtable to Cloud Spanner for better read latency",
      "Add more Bigtable nodes — Bigtable performance scales linearly with node count",
      "Enable Bigtable compression to reduce I/O and improve latency",
      "Increase the column family to store more data per row and reduce round trips"
    ],
    "ans": 1,
    "explain": "Bigtable's performance scales linearly with node count. At 80% CPU utilization with latency SLA risk, adding nodes is the correct action. Bigtable nodes can be added with no downtime.",
    "concept": "Bigtable scaling, node count vs. performance, linear scalability"
  },
  {
    "q": "You need to store machine learning model artifacts, training data, and prediction results, with different teams needing different access levels. Which Cloud Storage feature manages access at the folder/prefix level?",
    "opts": [
      "Cloud Storage Object Lifecycle Management",
      "Cloud Storage Uniform Bucket-Level Access with IAM Conditions on object names/prefixes",
      "Cloud Storage Object ACLs for per-object access",
      "Cloud Storage HMAC keys for programmatic access control"
    ],
    "ans": 1,
    "explain": "Best practice is Uniform Bucket-Level Access with IAM Conditions. IAM Conditions on resource.name allow prefix-based policies (e.g., grant data-scientists access to gs://bucket/training-data/* only).",
    "concept": "Cloud Storage IAM, Uniform Bucket-Level Access, IAM Conditions"
  },
  {
    "q": "A data team wants to use BigQuery's time travel feature. A table was accidentally DROPped 4 days ago. Can they recover it?",
    "opts": [
      "No — dropped tables cannot be recovered; time travel only works on existing tables",
      "Yes — BigQuery time travel retains table data for up to 7 days; use FOR SYSTEM_TIME AS OF to recover data. Dropped tables can be recovered with bq cp",
      "No — time travel only applies to data changes (DML), not DDL operations like DROP",
      "Yes — but only if a snapshot was manually taken before the drop"
    ],
    "ans": 1,
    "explain": "BigQuery's time travel window is 7 days by default. For a table dropped 4 days ago, query its data using FOR SYSTEM_TIME AS OF with a timestamp before the drop. The bq cp command can restore a dropped table within the retention window.",
    "concept": "BigQuery time travel, table recovery, FOR SYSTEM_TIME AS OF"
  },
  {
    "q": "You need to share a BigQuery dataset with an external partner without giving them direct table access. The partner needs to query a subset of columns and rows. What is the recommended approach?",
    "opts": [
      "Create an authorized view that filters columns and rows; grant the partner access to the view only in a separate dataset",
      "Export the filtered data to Cloud Storage and share the bucket",
      "Enable BigQuery Data Transfer Service to push data to the partner's project",
      "Grant the partner's service account roles/bigquery.dataViewer on the full table"
    ],
    "ans": 0,
    "explain": "Authorized views are the standard mechanism for row/column-level security. Create a view with SELECT on specific columns and WHERE filters, authorize it against the source dataset, then share only the view dataset with the external partner.",
    "concept": "BigQuery authorized views, row/column security, cross-project sharing"
  },
  {
    "q": "You have a Bigtable table with 500 billion rows. You need to perform a full table scan for a batch analytics job. Which Bigtable feature makes this efficient?",
    "opts": [
      "Bigtable does not support full table scans; use BigQuery instead",
      "Use Bigtable's batch ReadRows API with row range scans; or export to Cloud Storage via Dataflow using the BigtableIO source for parallel scans",
      "Enable Bigtable replication to a separate cluster dedicated to analytics reads",
      "Use Cloud Spanner federated queries against the Bigtable table"
    ],
    "ans": 1,
    "explain": "Bigtable supports efficient range scans via ReadRows. For batch analytics, use Dataflow's BigtableIO source which parallelizes the scan across multiple workers, each reading a different key range. This provides linear-scale throughput.",
    "concept": "Bigtable full table scan, BigtableIO, Dataflow parallel scan"
  },
  {
    "q": "A Cloud Storage bucket has 100 TB of data across millions of objects. You need to automatically delete objects older than 90 days in a specific prefix. How do you configure this?",
    "opts": [
      "Write a Cloud Function that runs daily and deletes old objects",
      "Configure a Cloud Storage Object Lifecycle Management rule: condition age=90 days + matches_prefix='old-data/'; action=Delete — GCP applies this automatically",
      "Use gsutil rm with a date filter in a Cloud Scheduler job",
      "Use Cloud Storage Retention Policies which enforce minimum retention but also delete after maximum"
    ],
    "ans": 1,
    "explain": "Cloud Storage Lifecycle Management applies rules automatically. A rule with condition {age: 90, matchesPrefix: ['old-data/']} and action {type: Delete} runs continuously — GCP evaluates all objects against the rules without any manual scheduling.",
    "concept": "Cloud Storage Lifecycle Management, automatic deletion, prefix-based rules"
  },
  {
    "q": "What is the difference between Bigtable's 'column family' and 'column qualifier'?",
    "opts": [
      "Column family and column qualifier are the same concept — synonyms",
      "Column family is a top-level grouping defined at table creation time (with its own GC policy); column qualifier is the actual column name within a family — a table has few families but potentially millions of unique qualifiers",
      "Column family determines row key format; column qualifier determines data type",
      "Column families are for strings; column qualifiers are for numeric values"
    ],
    "ans": 1,
    "explain": "Bigtable's schema: row key identifies the row; column families (defined at table creation, typically 1-5) group related columns with shared GC policies; column qualifiers are the actual column names within a family (can be dynamic, created on write). Data is stored as {row key, column family, column qualifier, timestamp} → value.",
    "concept": "Bigtable schema design, column families, column qualifiers, GC policies"
  },
  {
    "q": "You need to store semi-structured documents (user profiles with varying attributes) that require real-time single-document reads/writes and support for array-contains queries. Which GCP service is most appropriate?",
    "opts": [
      "BigQuery — supports JSON type for semi-structured data",
      "Firestore — document model with ARRAY_CONTAINS queries, real-time updates, and sub-millisecond reads",
      "Cloud Bigtable — wide-column store for flexible schemas",
      "Cloud Spanner — relational with JSON column support"
    ],
    "ans": 1,
    "explain": "Firestore is purpose-built for semi-structured documents with real-time updates (onSnapshot listeners), sub-millisecond reads, and rich query support including ARRAY_CONTAINS. It scales to millions of concurrent connections automatically.",
    "concept": "Firestore, document storage, ARRAY_CONTAINS, real-time updates"
  },
  {
    "q": "A BigQuery table has 10 columns partitioned by date. A query uses WHERE transaction_date = '2024-01-15' AND UPPER(customer_name) = 'ALICE'. How much of the table does BigQuery scan?",
    "opts": [
      "Only rows matching both conditions — BigQuery applies all filters at read time",
      "One day's partition (transaction_date filter prunes partitions); within that partition, all rows are scanned since UPPER(customer_name) cannot use an index",
      "Two days of partitions — BigQuery scans adjacent partitions for safety",
      "Zero rows — the partition filter is enough to return no data"
    ],
    "ans": 1,
    "explain": "BigQuery prunes partitions based on the transaction_date filter — only one day's data is read. Within that partition, BigQuery scans all rows (no secondary indexes) and then applies the UPPER(customer_name) = 'ALICE' filter. This is a key difference from traditional databases: BigQuery has no row-level indexes.",
    "concept": "BigQuery partition pruning, no row-level indexes, scan behavior"
  },
  {
    "q": "Which Cloud Spanner feature allows you to add new rows to an existing table without downtime and without locking existing rows?",
    "opts": [
      "Cloud Spanner DDL ALTER TABLE is an offline operation requiring downtime",
      "Cloud Spanner supports online schema changes — DDL statements are applied asynchronously without locking reads/writes on existing data; rows get NULL for new columns until backfilled",
      "Cloud Spanner requires creating a new table and migrating data for any schema change",
      "Use Cloud Spanner database branching to apply schema changes before promoting"
    ],
    "ans": 1,
    "explain": "Cloud Spanner's online schema changes apply DDL (ADD COLUMN, CREATE INDEX, etc.) asynchronously in the background while the database continues serving traffic. New columns default to NULL for existing rows. No downtime, no row locks.",
    "concept": "Cloud Spanner online schema changes, DDL, zero-downtime migrations"
  },
  {
    "q": "You need to design Bigtable row keys for a multi-tenant SaaS application. Each tenant has millions of records. Which row key design prevents hotspots while enabling efficient per-tenant queries?",
    "opts": [
      "tenant_id#timestamp — places all records for a tenant together; recent writes create hotspots if many tenants write simultaneously",
      "Hashed tenant_id prefix (first 4 bytes of MD5(tenant_id))#record_id — distributes writes across tablets while allowing per-tenant range scans on a secondary index",
      "Sequential auto-increment IDs — simple and avoids any design complexity",
      "Store all tenants in separate Bigtable tables — one table per tenant"
    ],
    "ans": 0,
    "explain": "tenant_id#record_id is actually correct for per-tenant queries (scans all rows with prefix tenant_id#). Hotspots occur when a SINGLE tenant generates all writes. If writes are spread across many tenants, tenant_id prefix is appropriate. Salting is needed only for single-tenant hotspots.",
    "concept": "Bigtable multi-tenant row key design, prefix scans, hotspot analysis"
  },
  {
    "q": "What is Cloud Storage's 'dual-region' configuration and when would you use it?",
    "opts": [
      "Dual-region stores two copies of each object in the same zone",
      "Dual-region replicates data across two specific GCP regions with turbo replication option for < 15-minute RPO; use it for disaster recovery requiring geographic separation but predictable data location",
      "Dual-region is the same as multi-region with only two regions active",
      "Dual-region is a cost-optimization that halves storage costs by splitting data across two regions"
    ],
    "ans": 1,
    "explain": "Cloud Storage dual-region provides georedundancy across two specific, paired regions (e.g., NAM4 = Iowa + South Carolina). Turbo replication guarantees < 15 minute RPO. Use dual-region when you need DR redundancy with predictable data location (vs. multi-region which uses multiple unpredictable regions).",
    "concept": "Cloud Storage dual-region, turbo replication, RPO, geographic redundancy"
  },
  {
    "q": "You need to implement a BigQuery table that automatically expires partitions older than 90 days to control storage costs. How do you configure this?",
    "opts": [
      "Write a Cloud Function that runs daily and deletes old partitions",
      "Set require_partition_filter=TRUE on the table and query the partition to delete it manually",
      "Configure partition expiration when creating the table: CREATE TABLE ... PARTITION BY DATE(event_time) OPTIONS(partition_expiration_days=90)",
      "Use Cloud Storage Lifecycle Management to expire BigQuery data"
    ],
    "ans": 2,
    "explain": "BigQuery supports partition-level expiration natively. When creating a table, set partition_expiration_days in the OPTIONS clause. BigQuery automatically deletes partitions older than 90 days — no external automation needed.",
    "concept": "BigQuery partition expiration, automatic data lifecycle, storage cost control"
  },
  {
    "q": "A Bigtable table stores click events with row key = user_id#reverse_timestamp. A Dataflow pipeline needs to read the last 10 clicks for each user for real-time processing. What is the efficient read pattern?",
    "opts": [
      "Full table scan, then filter for each user in Dataflow",
      "For each user, read the first 10 rows from the row range [user_id#, user_id$) — due to reverse timestamps, the first 10 rows are the most recent 10 clicks",
      "Read all rows for user_id and sort by timestamp in Dataflow",
      "Use Bigtable secondary index to find the last 10 rows by timestamp"
    ],
    "ans": 1,
    "explain": "With reversed timestamps, the most recent events sort lexicographically first within the user's key prefix. A row range scan [user_id#, user_id$) with a row limit of 10 returns the 10 most recent events in a single RPC — O(1) regardless of total event count.",
    "concept": "Bigtable prefix scan, reversed timestamp row key, efficient reads"
  },
  {
    "q": "BigQuery supports two types of external tables: regular external tables and BigLake tables. What is the key difference?",
    "opts": [
      "BigLake tables support only Parquet; regular external tables support all formats",
      "BigLake tables support fine-grained access control (row/column policies), Cloud DLP integration, and data sharing across projects. Regular external tables bypass access policies — BigLake adds a centralized security and governance layer",
      "Regular external tables are faster than BigLake tables for ad-hoc queries",
      "BigLake tables require BigQuery Enterprise Edition; regular external tables work with any edition"
    ],
    "ans": 1,
    "explain": "BigLake extends external tables with centralized governance: row access policies, column masking (DLP integration), and project-independent access control. Regular external tables have no row/column security. BigLake tables can be shared via Analytics Hub with consistent policy enforcement.",
    "concept": "BigLake vs external tables, governance, access control, DLP integration"
  },
  {
    "q": "You need to migrate a 200 TB Cloud Spanner database to a different Spanner instance in a different region. What is the recommended approach?",
    "opts": [
      "Use Spanner database branching to create a branch in the target region",
      "Export the Spanner database to Cloud Storage using the Dataflow Spanner export template; import to the target instance using the Spanner import template",
      "Use Spanner cross-region replication to mirror the database",
      "Use Datastream for Spanner-to-Spanner CDC migration"
    ],
    "ans": 1,
    "explain": "Cloud Spanner export/import: use the managed Dataflow template 'Cloud Spanner to GCS Avro' to export to Cloud Storage, then 'GCS Avro to Cloud Spanner' to import. For large databases, schedule during off-peak; the export captures a consistent snapshot.",
    "concept": "Cloud Spanner export/import, Dataflow Spanner templates, cross-region migration"
  },
  {
    "q": "A team needs BigQuery tables to be accessible to Looker but inaccessible to direct SQL clients. What mechanism restricts direct BigQuery SQL access?",
    "opts": [
      "BigQuery cannot distinguish between Looker and direct SQL clients at the API level",
      "Use BigQuery OAuth scopes to restrict clients",
      "IAM conditions cannot restrict by client type in BigQuery",
      "This cannot be accomplished with BigQuery IAM alone; you would need VPC Service Controls access levels that allowlist Looker's IP ranges while blocking other clients"
    ],
    "ans": 3,
    "explain": "BigQuery IAM grants/denies access to resources but cannot distinguish client applications. VPC Service Controls access levels can restrict access based on IP ranges or device attributes — allowlist Looker's cloud infrastructure IPs while blocking direct client access. This is the closest mechanism available.",
    "concept": "VPC Service Controls, access levels, BigQuery client restriction"
  },
  {
    "q": "Which Cloud Spanner feature provides an automatically managed, low-latency read-only replica in the same region as your application?",
    "opts": [
      "Cloud Spanner read replicas configured in each zone",
      "Cloud Spanner read-only replicas are automatic — every Spanner database has read-only replicas in each zone of the configured region. Use read-only transactions or stale reads to route to local replicas",
      "Configure Cloud Spanner with a multi-region setup for read replicas",
      "Deploy a Cloud SQL read replica alongside Spanner for read traffic"
    ],
    "ans": 1,
    "explain": "Cloud Spanner automatically maintains replicas in each zone of the configured region. To use them efficiently, use read-only transactions (no locking) or bounded staleness reads (stale_read) which can be served from the nearest replica without requiring a global quorum.",
    "concept": "Cloud Spanner read replicas, read-only transactions, stale reads, local serving"
  },
  {
    "q": "What BigQuery feature allows you to create a virtual dataset in your project that points to an authorized dataset in another project, without copying data?",
    "opts": [
      "BigQuery Authorized Views in your project pointing to the remote project",
      "BigQuery dataset links (linked datasets) created by Analytics Hub — subscribers get a virtual dataset in their project that mirrors the publisher's data in real-time without copying",
      "BigQuery Cross-Project Queries using the project.dataset.table syntax",
      "BigQuery Data Transfer Service that copies data between projects on a schedule"
    ],
    "ans": 1,
    "explain": "Analytics Hub linked datasets: the publisher creates a listing; the subscriber adds a linked dataset to their project. The linked dataset appears as a regular BigQuery dataset but reads directly from the publisher's data. No data copy, no storage cost for subscriber.",
    "concept": "Analytics Hub, linked datasets, cross-project data sharing, no-copy"
  },
  {
    "q": "A Bigtable table experiences high read latency after adding a new application that issues many small reads. The cluster CPU is low (20%). What is the likely cause and fix?",
    "opts": [
      "Low CPU means the cluster is undersized — add more nodes",
      "The new application is reading many individual cells, causing high RPC overhead. Fix: redesign reads to use row range scans (read multiple rows per RPC) or batch point reads using ReadRows with multiple RowSet entries instead of one-RPC-per-row",
      "Enable Bigtable compression to reduce read latency",
      "The Bigtable replication is causing read-write conflicts"
    ],
    "ans": 1,
    "explain": "Low CPU + high latency = network RPC overhead, not compute limitation. Many individual single-row reads generate excessive RPCs. Batching: use a single ReadRows call with a RowSet containing multiple row keys, or redesign to scan ranges rather than point lookups.",
    "concept": "Bigtable read optimization, batch reads, RPC overhead, RowSet"
  },
  {
    "q": "You need to store JSON configuration documents that are read thousands of times per second by microservices. Documents update infrequently (once per hour). Which storage option provides the best read latency and throughput?",
    "opts": [
      "Cloud SQL with a JSON column",
      "Firestore — sub-millisecond reads, automatic scaling to millions of reads/sec",
      "Cloud Storage — read the JSON file directly from GCS",
      "BigQuery external tables reading from GCS JSON"
    ],
    "ans": 1,
    "explain": "Firestore provides sub-millisecond document reads at unlimited scale. For configuration documents read thousands of times per second, Firestore is ideal: built-in caching, automatic global distribution, and no connection management. Cloud Storage reads have 10-100ms latency — too slow for per-request configuration reads.",
    "concept": "Firestore, high-throughput reads, configuration storage, sub-millisecond latency"
  },
  {
    "q": "A BigQuery dataset uses column-level security with policy tags. An analyst queries a table and gets 'Access Denied' on specific columns. How do you grant them access without removing security?",
    "opts": [
      "Remove the policy tag from those columns",
      "Grant the analyst the roles/datacatalog.categoryFineGrainedReader role on the specific policy tag (taxonomy node) — this grants access to tagged columns without changing the underlying policy",
      "Add the analyst to the dataset's IAM with dataViewer role — this overrides column-level restrictions",
      "Use a BigQuery authorized view that selects the restricted columns for the analyst"
    ],
    "ans": 1,
    "explain": "Column-level security with policy tags: access to tagged columns requires roles/datacatalog.categoryFineGrainedReader on the specific tag. Grant this role to the analyst on the taxonomy node for the specific policy tag. This is additive — other users without this role still have column access denied.",
    "concept": "BigQuery column-level security, policy tags, Fine-Grained Reader, taxonomy"
  },
  {
    "q": "You need to run a SQL query in BigQuery that must complete in under 10 seconds and return results from a 50 TB table. The table is partitioned by date and clustered by region. The query filters by date range and region. What do you verify?",
    "opts": [
      "Check that BigQuery has enough on-demand quota for the query",
      "Verify the query includes both the date filter (enabling partition pruning) and the region filter (enabling cluster block elimination); these filters ensure BigQuery scans a small fraction of the 50 TB",
      "Enable BigQuery BI Engine on the table for in-memory acceleration",
      "Create a materialized view of the commonly queried date/region combinations"
    ],
    "ans": 1,
    "explain": "For this 50 TB table, the query must include both the partition key (date) in the WHERE clause for partition pruning AND the clustering key (region) for block elimination. Both filters together reduce the scan to a tiny fraction of 50 TB, enabling sub-10-second execution.",
    "concept": "BigQuery partition pruning + cluster block elimination, query optimization, large tables"
  },
  {
    "q": "What is Cloud Storage's default consistency model?",
    "opts": [
      "Eventual consistency — reads may return stale data after a write",
      "Strong consistency — read-after-write is immediately consistent for all operations including list operations after object creation, deletion, or metadata updates",
      "Monotonic read consistency only — guarantees you see your own writes but not others",
      "Session consistency — consistent within a single HTTP session"
    ],
    "ans": 1,
    "explain": "Cloud Storage provides strong consistency for all operations: after a successful write, the new object is immediately visible to all readers (including list operations). This changed from eventual consistency in November 2020. No read-your-own-writes lag exists.",
    "concept": "Cloud Storage consistency model, strong consistency, read-after-write"
  },
  {
    "q": "A Bigtable cluster is performing poorly on random reads. Key analysis shows reads are uniformly distributed across the key space. CPU is at 70%. What is the recommended optimization?",
    "opts": [
      "Redesign the row key for better access locality",
      "Add more Bigtable nodes — random reads with 70% CPU indicates compute saturation; linear scaling with nodes applies",
      "Enable Bigtable caching by creating a Cloud Memorystore Redis cluster in front of Bigtable",
      "Compress all column families to reduce I/O"
    ],
    "ans": 1,
    "explain": "Uniformly distributed random reads with 70% CPU means the cluster is near saturation. Bigtable performance scales linearly with node count — adding nodes directly increases read throughput and reduces latency. This is the correct action when key design is already optimal.",
    "concept": "Bigtable linear scaling, random read performance, node count"
  },
  {
    "q": "You need to implement a BigQuery table where analysts can only see their own department's data (10 departments, 10 million rows each). Which implementation is most maintainable at scale?",
    "opts": [
      "Create 10 separate BigQuery tables, one per department",
      "Create 10 authorized views with department-specific WHERE clauses",
      "Use BigQuery Row Access Policies (CREATE ROW ACCESS POLICY) with SESSION_USER() or IAM group binding per department; one table, automatic row filtering",
      "Use column-level encryption per department and share decryption keys"
    ],
    "ans": 2,
    "explain": "Row Access Policies scale elegantly: CREATE ROW ACCESS POLICY emea_policy ON table GRANT TO GROUP 'emea-analysts@company.com' FILTER USING (department = 'EMEA'). Create one policy per department. All 10 departments share one table; each analyst automatically sees only their rows.",
    "concept": "BigQuery Row Access Policies, row-level security, multi-tenant data"
  },
  {
    "q": "Cloud SQL for PostgreSQL is running at 100% disk I/O. The database handles 10,000 writes/second. What is the most effective scaling action?",
    "opts": [
      "Add more CPU to the Cloud SQL instance",
      "Increase the storage size — Cloud SQL on SSD automatically provisions more IOPS as storage size increases; or upgrade to a larger instance with more IOPS",
      "Enable Cloud SQL read replicas to distribute write I/O",
      "Enable Cloud SQL automatic storage increase — it increases storage but not IOPS"
    ],
    "ans": 1,
    "explain": "Cloud SQL IOPS scale with storage size on SSD: larger storage = more IOPS (3,000 IOPS per 1 TB minimum). If I/O is the bottleneck, increase storage provisioning to get more IOPS. Also consider upgrading instance tier for higher IOPS limits or enabling Cloud SQL Enterprise Plus.",
    "concept": "Cloud SQL scaling, IOPS, storage-based IOPS scaling"
  },
  {
    "q": "You need to archive 500 TB of BigQuery data to Cloud Storage for long-term retention (10 years) at minimum cost. Data may be queried occasionally. What is the optimal approach?",
    "opts": [
      "Keep data in BigQuery — after 90 days of no modification, it automatically moves to long-term storage pricing",
      "Export BigQuery data to Cloud Storage Archive class as Parquet files; create BigQuery external tables for occasional queries; delete from BigQuery native storage",
      "Export to Cloud Storage Coldline and never query again",
      "Keep in BigQuery Standard Edition which auto-archives inactive data"
    ],
    "ans": 1,
    "explain": "Cloud Storage Archive class at $0.0012/GB/month vs BigQuery native storage long-term at $0.01/GB/month — 8x cheaper. Export as Parquet to preserve queryability via BigQuery external tables when needed. This reduces storage cost by 8x while maintaining SQL access.",
    "concept": "BigQuery archival, Cloud Storage Archive vs BigQuery long-term pricing, external tables"
  },
  {
    "q": "A multi-region BigQuery dataset spans US and EU. A query is run by a user in Asia. Where does BigQuery execute the query?",
    "opts": [
      "BigQuery routes the query to the physically nearest region (Asia)",
      "BigQuery executes the query in the region where the dataset is stored — for US multi-region, computation happens in a US region; the query result is returned to the user regardless of their location",
      "BigQuery replicated the dataset to Asia automatically for the user",
      "Query routing is user-configurable via the --location flag"
    ],
    "ans": 1,
    "explain": "BigQuery compute is co-located with data storage. A query against a US multi-region dataset runs in a US region. Data never moves — results are returned to the user over the network. This is why data residency requirements must be set at dataset creation.",
    "concept": "BigQuery compute-storage co-location, query execution location, data residency"
  },
  {
    "q": "What is the maximum number of partitions a BigQuery table can have?",
    "opts": [
      "365 (one per day for one year)",
      "4,000",
      "10,000",
      "Unlimited"
    ],
    "ans": 2,
    "explain": "BigQuery supports up to 10,000 partitions per table (for date/timestamp partitioned tables). For integer range partitioning, the maximum is also 10,000 partitions. Design partition granularity accordingly — daily partitioning supports ~27 years of data within this limit.",
    "concept": "BigQuery partition limits, table design, partition count"
  },
  {
    "q": "You need to enable Bigtable's automated backups for a production table. What are the backup characteristics?",
    "opts": [
      "Bigtable backups must be manually triggered via API; no automated backup scheduling exists",
      "Bigtable supports scheduled automated backups. You configure a backup schedule on a cluster; backups are stored within the same Bigtable instance and can be restored to create a new table",
      "Bigtable backups are full copies stored in Cloud Storage",
      "Bigtable backups replicate data to a different Cloud project"
    ],
    "ans": 1,
    "explain": "Bigtable scheduled backups: configure a BackupSchedule on a cluster (e.g., daily at 2 AM). Backups are stored within the Bigtable service (not Cloud Storage). Backup retention is configurable (up to 90 days). Restore creates a new Bigtable table from the backup.",
    "concept": "Bigtable automated backups, backup schedules, restore"
  },
  {
    "q": "Which BigQuery feature provides sub-second query performance on dashboards by caching results in memory on dedicated, reserved VMs?",
    "opts": [
      "BigQuery Materialized Views",
      "BigQuery BI Engine — an in-memory analysis service that accelerates Looker and Looker Studio queries without changing SQL syntax",
      "BigQuery Reservations with high-priority slots",
      "BigQuery query cache (24-hour result caching)"
    ],
    "ans": 1,
    "explain": "BigQuery BI Engine reserves in-memory capacity (1-250 GB) and accelerates SQL queries for BI tools. Eligible queries (simple aggregations, filters on cached columns) return in milliseconds. Unlike the query cache (exact query match), BI Engine caches column data and accelerates diverse queries on the same table.",
    "concept": "BigQuery BI Engine, in-memory acceleration, sub-second BI queries"
  },
  {
    "q": "You need to store 1 petabyte of data in BigQuery. What is the most cost-effective storage approach assuming most data is never queried?",
    "opts": [
      "Keep all data in active BigQuery storage — it supports petabyte scale efficiently",
      "Use BigQuery's active vs long-term pricing: data not modified for 90+ consecutive days automatically moves to long-term pricing ($0.01/GB vs $0.02/GB/month), saving 50%",
      "Export all data to Cloud Storage Archive and query via external tables",
      "Use BigQuery Reservations which include free storage up to a quota"
    ],
    "ans": 1,
    "explain": "BigQuery's long-term storage pricing is automatic: tables/partitions not modified for 90 days are charged at half price ($0.01/GB/month vs $0.02/GB/month). For 1 PB of mostly-inactive data, this saves ~$10,000/month. No manual archival required.",
    "concept": "BigQuery long-term storage pricing, 90-day threshold, automatic cost reduction"
  },
  {
    "q": "A data engineering team needs Cloud Spanner to handle a sudden 10x traffic spike. What scaling action is fastest?",
    "opts": [
      "Add more Spanner nodes via the console — takes 10-15 minutes to provision",
      "Spanner autoscaling automatically adjusts node count (processing units) within configured bounds in minutes with no manual intervention",
      "Create a second Spanner instance and distribute traffic",
      "Scale up the database machine type via ALTER DATABASE"
    ],
    "ans": 1,
    "explain": "Cloud Spanner supports autoscaling: configure min/max processing units; Spanner automatically scales within bounds. This is faster than manual node addition. For immediate burst handling, ensure the max is set high enough before the spike. Spanner also handles traffic spikes gracefully within its provisioned capacity.",
    "concept": "Cloud Spanner autoscaling, processing units, traffic spike handling"
  },
  {
    "q": "You need to store genomic data (billions of VCF records) with complex queries (gene_id, chromosome, position range). Which storage option supports this best?",
    "opts": [
      "Cloud SQL PostgreSQL with partitioning on chromosome",
      "BigQuery with partitioning on chromosome and clustering on gene_id — enables efficient range scans on position with sub-minute query times",
      "Bigtable with row key chromosome#position for range scans",
      "Firestore with genomic-specific compound indexes"
    ],
    "ans": 1,
    "explain": "BigQuery is ideal for genomic analytics: partition by chromosome (ranges 1-22 + X, Y, MT), cluster by gene_id and position. Complex analytical SQL queries (window functions, joins with gene annotations) run at petabyte scale. BigQuery also integrates with Vertex AI for ML on genomic features.",
    "concept": "BigQuery genomics, partitioning and clustering for range queries, scientific analytics"
  },
  {
    "q": "What is a Cloud Spanner 'split' and why does it matter for performance?",
    "opts": [
      "A split is a manual operation to divide large tables into smaller ones",
      "A split is how Spanner automatically divides data across servers (SpanNodes) for horizontal scaling. Poor row key design (sequential IDs) can cause hotspots where new rows always land on the same split. Use UUIDs or hashed keys to distribute splits evenly",
      "A split creates a read-only replica for geographic distribution",
      "A split is a Spanner backup copy stored in a different region"
    ],
    "ans": 1,
    "explain": "Spanner automatically splits data across servers to distribute load. Sequential row keys (auto-increment IDs) concentrate writes on the 'last' split — creating hotspots. Use random/UUIDs or bit-reversed sequence keys to ensure new rows land on different splits evenly.",
    "concept": "Cloud Spanner splits, hotspot prevention, key design, horizontal distribution"
  },
  {
    "q": "You need to configure a BigQuery reservation for a batch ETL workload and a separate reservation for interactive analytics, ensuring ETL jobs don't starve analytics users of slots. How do you configure this?",
    "opts": [
      "Create a single reservation with HIGHEST priority for analytics queries",
      "Create two separate BigQuery Reservations (ETL_RESERVATION and ANALYTICS_RESERVATION); create Assignments for each project/folder; configure slot counts appropriately — each workload gets isolated slots",
      "Use BigQuery on-demand pricing for analytics and reservations for ETL",
      "Set job priority to INTERACTIVE for analytics queries — BigQuery automatically prioritizes them"
    ],
    "ans": 1,
    "explain": "BigQuery Reservations provide slot isolation. Create ETL_RESERVATION with dedicated slots assigned to ETL projects/folders; create ANALYTICS_RESERVATION with slots assigned to analytics projects. Each workload uses only its reserved slots — neither can starve the other. Idle slots can optionally be shared via reservation idle slot sharing.",
    "concept": "BigQuery Reservations, workload isolation, slot assignments, multi-workload management"
  },
  {
    "q": "A Bigtable instance has 3 clusters for replication (us-central1, europe-west1, asia-east1). A write arrives at the us-central1 cluster. When is it available in europe-west1?",
    "opts": [
      "Immediately — Bigtable uses synchronous replication",
      "Within a few seconds — Bigtable replication is asynchronous; writes replicate in the background with typical latency of seconds. Reads from europe-west1 may serve stale data briefly",
      "Within 1 hour — Bigtable replication batches changes hourly",
      "Within 24 hours — Bigtable replication is a daily batch process"
    ],
    "ans": 1,
    "explain": "Bigtable replication is asynchronous. Writes committed at one cluster replicate to others with typically seconds of lag (similar to Google's global network replication speed). Applications that require zero replication lag must use single-cluster routing and accept the geographic latency tradeoff.",
    "concept": "Bigtable replication, asynchronous replication, replication lag, multi-cluster"
  },
  {
    "q": "You need to efficiently store and query a 100 TB financial dataset where queries frequently join a 100 TB transactions table with a 10 GB accounts table. What BigQuery design minimizes query time?",
    "opts": [
      "Store both tables separately and let BigQuery's distributed join handle performance",
      "Denormalize: JOIN accounts into transactions at ingestion time, creating a single wide table. Alternatively, use a materialized view that pre-joins frequently used combinations",
      "Use Bigtable for transactions and BigQuery for accounts, joining via Dataflow",
      "Create a separate BigQuery table for each account with its transactions"
    ],
    "ans": 1,
    "explain": "For a 100 TB × 10 GB join executed frequently: pre-join at ingestion (denormalize the 10 GB accounts into transactions) creates a single wide table eliminating the join overhead entirely. Alternatively, materialize the joined result. 100 TB joins are expensive — pre-computation amortizes the cost.",
    "concept": "BigQuery denormalization, pre-joining, large table join optimization, materialized views"
  },
  {
    "q": "What is Cloud SQL's connection limit and how do you handle connection exhaustion in a high-concurrency application?",
    "opts": [
      "Cloud SQL has unlimited connections; connection errors are a client-side issue",
      "Cloud SQL has per-instance connection limits (varies by instance tier: ~4,000 for large instances). Connection pooling via Cloud SQL Proxy or PgBouncer prevents connection exhaustion by sharing a smaller pool of database connections across many application threads",
      "Use Cloud Spanner instead — it has unlimited connections",
      "Increase Cloud SQL connection limit via ALTER SYSTEM SET max_connections"
    ],
    "ans": 1,
    "explain": "Cloud SQL connection limits: a connections-per-second limit and a maximum concurrent connections limit (varies by tier). High-concurrency apps exhaust connections without pooling. Cloud SQL Auth Proxy with PgBouncer (for PostgreSQL) or ProxySQL (for MySQL) implement connection pooling — many app threads share fewer DB connections.",
    "concept": "Cloud SQL connection limits, connection pooling, PgBouncer, Cloud SQL Auth Proxy"
  },
  {
    "q": "You need to implement a data lineage system that automatically captures how BigQuery tables are created from upstream sources. Which GCP service provides this natively?",
    "opts": [
      "Cloud Audit Logs with manual lineage reconstruction from job history",
      "Dataplex Lineage API — automatically captures lineage events from BigQuery DML/DDL operations, Dataflow jobs, and Data Fusion pipelines without instrumentation",
      "BigQuery INFORMATION_SCHEMA.JOBS with manual lineage parsing",
      "Apache Atlas deployed on Dataproc"
    ],
    "ans": 1,
    "explain": "Dataplex Lineage API auto-captures lineage: when a BigQuery CREATE TABLE AS SELECT runs, Dataplex records the source tables and destination. Dataflow and Data Fusion jobs also emit lineage events automatically. The resulting lineage graph shows upstream and downstream dependencies for any data asset.",
    "concept": "Dataplex Lineage API, automatic lineage capture, BigQuery lineage"
  },
  {
    "q": "Which Cloud Storage feature prevents objects from being deleted or overwritten for a specified retention period, useful for regulatory compliance?",
    "opts": [
      "Cloud Storage Object Versioning — keeps all previous versions",
      "Cloud Storage Retention Policies on buckets — objects cannot be deleted until the policy duration expires. Retention Lock makes the policy itself irremovable",
      "Cloud Storage lifecycle rules — prevent deletion based on conditions",
      "IAM deny policies — prevent deletion by all users including admins"
    ],
    "ans": 1,
    "explain": "Cloud Storage Retention Policies: set a minimum retention duration on a bucket. Objects cannot be deleted or overwritten until the policy duration expires from the creation/modification time. Locking the policy makes it permanent and cannot be shortened — required for SEC Rule 17a-4 and similar regulations.",
    "concept": "Cloud Storage retention policies, compliance, retention lock, WORM storage"
  },
  {
    "q": "A BigQuery table has JSON data stored as STRING columns. Queries need to extract values using JSON_VALUE() functions. What are the performance implications?",
    "opts": [
      "JSON_VALUE() on STRING columns is the same performance as accessing native typed columns",
      "JSON_VALUE() on STRING columns requires parsing the JSON string at query time for every row scanned — significantly slower and more CPU-intensive than native typed columns or the BigQuery JSON type",
      "BigQuery caches JSON parsing results, so first query is slow but subsequent are fast",
      "STRING columns with JSON are faster than STRUCT columns because they avoid schema overhead"
    ],
    "ans": 1,
    "explain": "JSON stored as STRING requires runtime parsing via JSON_VALUE(). For large tables, this CPU overhead is significant. Better alternatives: (1) use BigQuery's native JSON type (optimized internal storage with lazy parsing); (2) use STRUCT columns for known schemas; (3) parse at ingestion time into typed columns.",
    "concept": "BigQuery JSON performance, JSON_VALUE overhead, schema design"
  },
  {
    "q": "You need to create a BigQuery table that stores user events. The table will be queried 90% of the time by user_id and event_type. You also need to occasionally run full table scans. Which design is optimal?",
    "opts": [
      "No partitioning, no clustering — full table scans need to scan everything anyway",
      "Partition by _PARTITIONTIME (ingestion time) and cluster by user_id, event_type. Queries filtering by user_id and event_type eliminate blocks; full scans are unaffected by clustering overhead",
      "Cluster by user_id, event_type without partitioning — clustering alone optimizes the common queries",
      "Partition by user_id — enables fast per-user queries"
    ],
    "ans": 1,
    "explain": "Ingestion-time partitioning limits the total data scanned by date for most queries. Clustering by user_id, event_type enables block elimination for the 90% of user_id/event_type queries within each partition. Full table scans are not penalized by clustering — they read all blocks regardless. This is the optimal combination.",
    "concept": "BigQuery partitioning and clustering combination, block elimination, full scan behavior"
  },
  {
    "q": "You need to implement cross-region disaster recovery for a Bigtable production cluster. A failure in us-central1 should automatically failover reads to europe-west1. How do you configure this?",
    "opts": [
      "Manually update application DNS to point to europe-west1 cluster after detecting failure",
      "Configure a multi-cluster Bigtable instance with clusters in us-central1 and europe-west1; use an app profile with multi-cluster routing (any-cluster). On failure, Bigtable automatically routes requests to the available cluster",
      "Enable Bigtable cross-region replication and configure Cloud Load Balancing for failover",
      "Use Cloud Spanner instead — Bigtable does not support automatic failover"
    ],
    "ans": 1,
    "explain": "Bigtable multi-cluster routing: create two clusters (us-central1 + europe-west1) in a single Bigtable instance; configure an app profile with routing_policy=multi-cluster-use-any. If us-central1 becomes unavailable, Bigtable automatically routes to europe-west1 — no application changes needed.",
    "concept": "Bigtable multi-cluster routing, automatic failover, disaster recovery, app profiles"
  },
  {
    "q": "What is the difference between BigQuery's ARRAY and STRUCT types and when would you use each?",
    "opts": [
      "ARRAY and STRUCT are the same — both store multiple values",
      "ARRAY stores an ordered list of same-type values (e.g., ARRAY<STRING>). STRUCT stores named fields of different types (like a row within a row). Use ARRAY for repeated values of one type; use STRUCT for grouping related fields; use ARRAY<STRUCT<...>> for repeated nested records",
      "ARRAY is only for numbers; STRUCT is for strings and dates",
      "STRUCT replaces row normalization; ARRAY replaces table joins"
    ],
    "ans": 1,
    "explain": "BigQuery's nested data model: ARRAY<T> is a repeated field of a single type (e.g., list of tags). STRUCT<field1 TYPE1, field2 TYPE2> nests multiple named fields. ARRAY<STRUCT<...>> represents repeated nested records (e.g., an array of order line items). These enable storing denormalized hierarchical data efficiently.",
    "concept": "BigQuery ARRAY and STRUCT, nested data model, denormalization"
  },
  {
    "q": "Cloud SQL for PostgreSQL needs to handle geospatial queries (ST_Within, ST_Distance). Which extension provides this capability?",
    "opts": [
      "Cloud SQL includes geospatial support via the built-in GEOMETRY type",
      "PostGIS extension — enable it with CREATE EXTENSION postgis; provides geometry/geography types and spatial functions. Cloud SQL for PostgreSQL fully supports PostGIS",
      "Cloud SQL Geospatial API — a separate GCP service for spatial queries",
      "BigQuery GIS functions should be used instead of Cloud SQL for geospatial queries"
    ],
    "ans": 1,
    "explain": "PostGIS is the standard PostgreSQL geospatial extension. Cloud SQL for PostgreSQL supports PostGIS — enable it via CREATE EXTENSION postgis. This provides: geometry/geography data types, spatial indexes (GiST), and spatial functions (ST_Within, ST_Distance, ST_Intersection) required for geospatial queries.",
    "concept": "Cloud SQL PostGIS, geospatial queries, PostgreSQL extensions"
  },
  {
    "q": "A BigQuery project is shared by 50 teams. One team runs a rogue query that scans the entire 100 TB dataset daily, causing slot contention. How do you prevent this?",
    "opts": [
      "Grant that team only roles/bigquery.dataViewer — read-only access prevents slot usage",
      "Set a maximum bytes billed per query on that team's project (service account); add BigQuery project-level quotas for max bytes per day. Use BigQuery Reservations to isolate slot pools per team",
      "Remove the team's BigQuery access until they agree to query optimization practices",
      "Use BigQuery authorized views to limit what data the team can see"
    ],
    "ans": 1,
    "explain": "Two controls: (1) maximum bytes billed per query prevents individual runaway queries; (2) per-project daily byte quotas cap total consumption. BigQuery Reservations provide slot isolation — the rogue team's Reservation limits their slots without affecting other teams.",
    "concept": "BigQuery cost controls, bytes billed limits, per-project quotas, Reservations isolation"
  },
  {
    "q": "You need to implement a BigQuery table clone for development testing without incurring the full storage cost of a copy. Which BigQuery feature provides this?",
    "opts": [
      "BigQuery table copy (bq cp) — makes a full physical copy",
      "BigQuery table clone (CREATE TABLE ... CLONE) — creates a zero-storage-cost clone using copy-on-write semantics; only changed rows incur additional storage",
      "BigQuery snapshot — creates a point-in-time copy of the table",
      "BigQuery materialized view — pre-computes and stores query results"
    ],
    "ans": 1,
    "explain": "BigQuery table clones use copy-on-write: CREATE TABLE dev.events CLONE prod.events creates a logical copy that references the original data. Storage cost is zero until rows are modified in the clone. Only changed/added rows in the clone incur storage cost — perfect for dev/test without the 100% storage overhead of a full copy.",
    "concept": "BigQuery table clone, copy-on-write, zero-storage-cost development tables"
  },
  {
    "q": "Firestore's maximum document size is 1 MB. A design stores large text blobs (up to 10 MB) in documents. How should you redesign this?",
    "opts": [
      "Use GZIP compression to fit large text within the 1 MB limit",
      "Store the large blobs in Cloud Storage; store the GCS URI in the Firestore document as a reference. Firestore documents contain only metadata and the pointer to the blob",
      "Split each document into multiple sub-documents to stay under 1 MB",
      "Use Cloud Spanner instead — it has no document size limits"
    ],
    "ans": 1,
    "explain": "The Firestore pattern for large blobs: store metadata (title, author, timestamps, tags) in Firestore for fast querying; store the actual blob (PDF, large text, media) in Cloud Storage. The Firestore document includes the GCS URI. Applications fetch metadata from Firestore and the blob from GCS separately.",
    "concept": "Firestore document limits, blob storage pattern, Firestore + Cloud Storage"
  },
  {
    "q": "You need to implement a BigQuery access pattern where different users see different versions of the same data (some see real values, some see masked values, some see NULL). Which feature set implements this with a single underlying table?",
    "opts": [
      "Create three separate tables with different data for each user group",
      "BigQuery Dynamic Data Masking: create masking rules (NULL_MASK, DEFAULT_MASK, SHA256_MASK, etc.) on policy-tagged columns; bind different masking rules to different IAM principals — same table, different views depending on user",
      "Use three authorized views with CASE WHEN SESSION_USER() THEN ... ELSE ...",
      "Use Row Access Policies with different filter conditions per user group"
    ],
    "ans": 1,
    "explain": "Dynamic Data Masking allows multiple masking rules on the same column: Admin group → unmasked values; Analyst group → SHA256 hash; External group → NULL. All groups query the same table — BigQuery applies the appropriate mask at query time based on the user's group membership.",
    "concept": "BigQuery Dynamic Data Masking, multiple masking rules, role-based data views"
  },
  {
    "q": "What is the recommended row key design for a Bigtable table storing user clickstream events where you need to: (1) retrieve all events for a user, (2) retrieve the last N events for a user, and (3) avoid hotspots?",
    "opts": [
      "user_id#event_id (sequential)",
      "Hashed_user_id (8 chars)#reverse_timestamp#event_id — hash prefix distributes writes across tablets; reverse timestamp sorts recent events first within each user",
      "timestamp#user_id — enables time-based range scans across all users",
      "event_id (UUID) — fully random, no hotspots, excellent distribution"
    ],
    "ans": 1,
    "explain": "This design achieves all three requirements: (1) hashed_user_id prefix scatters writes across tablets preventing hotspots for any single user; (2) all events for a user share the same hashed prefix enabling prefix scans; (3) reverse_timestamp within the prefix sorts recent events first — limit N returns the N most recent events.",
    "concept": "Bigtable row key design, multi-criteria optimization, hash prefix, reversed timestamp"
  },
  {
    "q": "BigQuery supports which of the following transaction isolation levels for DML operations?",
    "opts": [
      "Read Uncommitted — fastest but may read dirty data",
      "Serializable isolation for all DML operations",
      "Snapshot isolation — DML reads a consistent snapshot at statement start; concurrent DML is detected at commit and retried automatically",
      "No isolation — concurrent DML may produce undefined results"
    ],
    "ans": 2,
    "explain": "BigQuery DML uses snapshot isolation: each statement reads data from a consistent snapshot taken at statement start. Concurrent DML statements can run simultaneously; conflicts are detected at commit time and BigQuery retries the conflicting statement automatically (up to 5 times).",
    "concept": "BigQuery DML isolation, snapshot isolation, concurrent DML, ACID"
  },
  {
    "q": "You need to store time-series metrics for 10,000 servers, each emitting 100 metrics per second. Queries need: (1) last 5 minutes of any metric for one server, (2) aggregate across all servers for a specific metric. Which Bigtable row key design serves both patterns?",
    "opts": [
      "server_id#metric_name#timestamp — serves server queries but aggregation requires full table scan",
      "metric_name#server_id#reverse_timestamp — serves both: metric aggregation via metric_name prefix scan; per-server metric via metric_name#server_id prefix; reverse timestamp gives recency",
      "timestamp#server_id#metric_name — time-based scan with hotspot at current time",
      "server_id#reverse_timestamp#metric_name — serves server queries well; metric aggregation requires full scan"
    ],
    "ans": 1,
    "explain": "metric_name#server_id#reverse_timestamp serves both access patterns: (1) scan prefix metric_name#server_id# with row limit 300 for last 5 minutes (300 = 5 min × 60 sec × 1 reading/sec per metric); (2) scan prefix metric_name# for cross-server aggregation. Reverse timestamp avoids hotspots on current time.",
    "concept": "Bigtable multi-pattern row key design, time-series metrics, cross-server aggregation"
  },
  {
    "q": "A Cloud Storage bucket is accessed from both GCP and on-premises. On-premises access is slow. What can improve performance?",
    "opts": [
      "Enable Cloud CDN for the Cloud Storage bucket — it caches objects near on-premises locations",
      "Use Cloud Storage's multi-region bucket to replicate data nearest to on-premises location",
      "Deploy a storage proxy on-premises using Cloud Storage FUSE or a Cloud Storage client library with transfer optimization; also use Cloud Interconnect for dedicated bandwidth instead of public internet",
      "Move all data to Cloud SQL which has lower latency for on-premises access"
    ],
    "ans": 2,
    "explain": "Cloud Storage over public internet has variable latency. Cloud Interconnect provides dedicated, low-latency, high-bandwidth connectivity to GCP from on-premises. Additionally, use the Cloud Storage client library's parallel composite uploads/downloads and JSON API resumable uploads to maximize throughput.",
    "concept": "Cloud Storage on-premises access, Cloud Interconnect, transfer optimization"
  },
  {
    "q": "You need to implement a system where BigQuery table access is automatically revoked after 30 days of inactivity. What is the mechanism?",
    "opts": [
      "BigQuery automatically revokes IAM bindings after 30 days",
      "Use IAM Conditions with a date expression (request.time < timestamp('2024-03-01')) to create time-bound IAM bindings; or implement a Cloud Function that checks last query date from INFORMATION_SCHEMA.JOBS and revokes access if > 30 days",
      "BigQuery audit logs provide 30-day access reports; manually review and revoke",
      "Use Dataplex IAM policies with time-based conditions"
    ],
    "ans": 1,
    "explain": "GCP IAM supports time-based conditions: bind roles with condition request.time < timestamp('date'). For dynamic 30-day windows, implement a Cloud Scheduler + Cloud Function that checks INFORMATION_SCHEMA.JOBS per user for query activity in the last 30 days and removes IAM bindings for inactive users.",
    "concept": "IAM Conditions, time-based access, automated access revocation, INFORMATION_SCHEMA"
  },
  {
    "q": "What is the purpose of BigQuery's 'require_partition_filter' table option?",
    "opts": [
      "It requires BigQuery to use a partition filter for all queries on the table — queries without a partition filter in the WHERE clause are rejected with an error",
      "It enables partition pruning automatically on all queries",
      "It requires all data to be inserted with an explicit partition specification",
      "It prevents queries from using multiple partitions simultaneously"
    ],
    "ans": 0,
    "explain": "Setting require_partition_filter=TRUE on a BigQuery partitioned table forces all queries to include a filter on the partitioned column. Queries without the filter are rejected: 'Error: Cannot query over table without a filter that can be used for partition elimination.' This prevents accidental full table scans on large partitioned tables.",
    "concept": "BigQuery require_partition_filter, query guardrails, partition safety"
  },
  {
    "q": "A BigQuery dataset needs to be shared with users who are not in your GCP organization. What is the correct sharing mechanism?",
    "opts": [
      "Cross-organization IAM sharing is not supported in BigQuery",
      "Grant IAM roles directly to external users' Google accounts (gmail.com or Workspace accounts) on the BigQuery dataset — BigQuery IAM supports non-org identities; or use Analytics Hub for structured data exchange",
      "Create a service account for external users and share the key file",
      "External users must create a GCP project in your organization first"
    ],
    "ans": 1,
    "explain": "BigQuery IAM supports granting access to any Google account (including @gmail.com accounts outside your organization). Grant roles/bigquery.dataViewer on the specific dataset to the external user's email address. Analytics Hub provides a more structured data exchange mechanism with subscription management.",
    "concept": "BigQuery cross-organization sharing, external IAM, Analytics Hub"
  },
  {
    "q": "Explain the difference between Bigtable's 'Read Rows' and 'Sample Row Keys' API calls and when each is used.",
    "opts": [
      "Read Rows and Sample Row Keys are identical — both read data from Bigtable",
      "Read Rows retrieves actual row data (all cells or specified cell sets). Sample Row Keys returns approximate boundary keys that divide the table into roughly equal segments — used by parallel job frameworks (Dataflow BigtableIO) to split scans across multiple workers without reading data",
      "Sample Row Keys is a streaming API; Read Rows is a batch API",
      "Read Rows is for small tables; Sample Row Keys is optimized for tables over 1 TB"
    ],
    "ans": 1,
    "explain": "SampleRowKeys is the split planning API: it returns ~1000 boundary keys per cluster node, enabling Dataflow/MapReduce to create parallel scan ranges. Each worker then calls ReadRows on its assigned key range. Without SampleRowKeys, parallel frameworks would have to scan sequentially.",
    "concept": "Bigtable SampleRowKeys, parallel scan planning, Dataflow BigtableIO"
  },
  {
    "q": "A BigQuery table is updated every hour via a scheduled query. Analysts notice that their materialized views over this table sometimes show stale data. Why?",
    "opts": [
      "Materialized views on BigQuery do not refresh automatically",
      "BigQuery materialized views have a maximum staleness of 30 minutes by default. If the base table changes within the last refresh cycle, the materialized view serves slightly stale data. Queries can use the max_staleness option or trigger manual refresh",
      "Materialized views always show data from the previous day",
      "Materialized views are never stale — they update synchronously with base table changes"
    ],
    "ans": 1,
    "explain": "BigQuery materialized views refresh incrementally when queried (if the base table changed). The default max staleness is 30 minutes — if you query within 30 minutes of a base table change, BigQuery serves the last cached result. Use OPTIONS(max_staleness=INTERVAL '1' MINUTE) for lower staleness tolerance.",
    "concept": "BigQuery materialized view staleness, refresh cycle, max_staleness option"
  },
  {
    "q": "You are designing a multi-region BigQuery architecture where the same analytical queries need to run in both the US and EU on locally stored copies of the data. What is the most operational approach?",
    "opts": [
      "Write data to two separate BigQuery datasets (one US, one EU) from the pipeline; maintain identical schemas",
      "Use BigQuery Cross-Region Replication (when available) or set up two separate pipeline sinks (US dataset + EU dataset) from a shared Pub/Sub topic; use Cloud Composer to ensure both are synchronized",
      "Use BigQuery multi-region dataset (e.g., US multi-region) — queries automatically route to nearest region",
      "Use Datastream to replicate BigQuery tables between regions"
    ],
    "ans": 1,
    "explain": "For multi-region BigQuery with local data: write from a single ingest pipeline (Pub/Sub/Dataflow) to two separate regional datasets simultaneously. Cloud Composer orchestrates the dual writes and monitors freshness. Each region has its own dataset complying with data residency requirements.",
    "concept": "BigQuery multi-region architecture, dual-write pattern, data residency"
  },
  {
    "q": "What is the purpose of Cloud Storage's 'object versioning' feature and what are its cost implications?",
    "opts": [
      "Object versioning compresses old versions to reduce storage costs",
      "Object versioning retains previous versions of objects when they are overwritten or deleted. Each previous version is stored as a non-current version and billed at full storage rates. Use lifecycle rules (condition: isLive=false, action: Delete after N days) to control versioning costs",
      "Object versioning is free — previous versions are stored at no charge",
      "Object versioning stores only the diff between versions, minimizing storage cost"
    ],
    "ans": 1,
    "explain": "Cloud Storage versioning stores complete copies of each previous version — not diffs. Each non-current version is billed at full storage rates. Without lifecycle rules to delete old versions, costs can grow unboundedly. Best practice: enable versioning + lifecycle rule to delete non-current versions after N days.",
    "concept": "Cloud Storage object versioning, non-current versions, versioning cost management"
  },
  {
    "q": "A Cloud Spanner table has a hotspot on a frequently-read row (the 'global settings' row accessed by all users). How do you resolve this read hotspot?",
    "opts": [
      "Increase Cloud Spanner nodes — more nodes handle more reads on the hot row",
      "Application-level caching (Cloud Memorystore Redis, in-process cache): cache the global settings row in the application layer with a TTL; only read from Spanner on cache miss. The hot row reads become cache hits — Spanner load drops dramatically",
      "Create a read-only Cloud Spanner replica dedicated to global settings",
      "Partition the global settings table to distribute the hot row across splits"
    ],
    "ans": 1,
    "explain": "A single frequently-read Spanner row creates a read hotspot on one split. Spanner cannot distribute reads of a single row across nodes. Solution: application-level caching (Memorystore Redis) with appropriate TTL. The read-heavy row is served from cache; Spanner only serves cache misses (infrequent).",
    "concept": "Cloud Spanner read hotspot, application caching, Memorystore Redis, cache-aside pattern"
  },
  {
    "q": "You need to store and query 50 billion GPS coordinates (latitude, longitude, timestamp, device_id) with queries: (1) find all coordinates for device_id in a time range; (2) find all coordinates in a geographic bounding box. Which storage design handles both queries?",
    "opts": [
      "Cloud SQL with PostGIS geospatial index — handles bounding box queries but not 50 billion rows",
      "BigQuery with a device_id + timestamp partition/cluster for query (1); use BigQuery GIS functions (ST_Within, ST_DWithin) for geographic queries on the same table",
      "Bigtable with device_id#timestamp row key for query (1); geographic queries require a secondary Bigtable table with geohash row key",
      "Both B and C; BigQuery for analytics, Bigtable for real-time with appropriate row key design"
    ],
    "ans": 3,
    "explain": "Optimal design depends on use case: BigQuery (partitioned by date, clustered by device_id) handles both analytical queries via SQL — time range and ST_Within for geographic queries — but isn't real-time. Bigtable handles real-time per-device queries. For a full system, BigQuery handles analytics; Bigtable handles real-time serving.",
    "concept": "GPS data storage, BigQuery GIS, Bigtable time-series, multi-system architecture"
  },
  {
    "q": "A BigQuery dataset is in the EU multi-region. A Dataflow job in us-central1 writes to this dataset. Is this a problem?",
    "opts": [
      "No — Dataflow and BigQuery region don't need to match",
      "Yes — data transfer from us-central1 Dataflow to EU BigQuery incurs cross-region network egress charges and may violate EU data residency requirements since data traverses the US",
      "No — Google encrypts all data in transit between regions so it's compliant",
      "Yes — Dataflow cannot write to BigQuery in a different region"
    ],
    "ans": 1,
    "explain": "Cross-region data transfer: Dataflow workers in us-central1 process data in the US and write to EU BigQuery. This means data temporarily resides in the US (Dataflow workers) before moving to EU — potentially violating EU data residency requirements. Also incurs cross-region egress charges. Solution: deploy Dataflow in europe-west1 or europe-west4.",
    "concept": "Cross-region data processing, data residency compliance, Dataflow region selection"
  },
  {
    "q": "What is the Bigtable garbage collection (GC) policy and how does it help manage storage?",
    "opts": [
      "GC policy automatically rebalances data across Bigtable nodes",
      "GC policies define retention rules for cell versions per column family: maxVersions (keep only N most recent versions) and maxAge (delete versions older than N days). Bigtable applies GC asynchronously in the background — reducing storage by removing obsolete cell versions",
      "GC policy is a billing feature that automatically moves cold data to Archive storage",
      "GC policy only applies to entire rows, not individual cell versions"
    ],
    "ans": 1,
    "explain": "Bigtable GC policies are defined per column family: maxVersions keeps the N most recent timestamps for each cell; maxAge deletes cell versions older than a specified duration. These can be combined with union/intersection logic. Without GC policies, every write creates a new version and storage grows unboundedly.",
    "concept": "Bigtable GC policy, cell versions, storage management, maxVersions, maxAge"
  },
  {
    "q": "You need to implement BigQuery table-level access where Project A can query tables in Project B's dataset. How do you configure this?",
    "opts": [
      "Cross-project BigQuery access requires both projects to be in the same VPC",
      "Grant a role (e.g., roles/bigquery.dataViewer) to a Project A service account on the specific Project B dataset; queries from Project A use the TABLE syntax: project_b.dataset.table — no VPC configuration needed",
      "Use BigQuery Data Transfer Service to copy Project B data to Project A",
      "Create a VPC peering between Project A and Project B"
    ],
    "ans": 1,
    "explain": "BigQuery IAM is resource-based and project-agnostic. Grant the Project A service account roles/bigquery.dataViewer on the Project B dataset. Project A queries use fully qualified table names (project_b.dataset.table) in SQL. No VPC or network configuration required.",
    "concept": "BigQuery cross-project access, resource-based IAM, fully qualified table names"
  },
  {
    "q": "A Cloud Storage Standard bucket receives 10,000 writes/second and 100,000 reads/second. You observe increasing read latency as operations grow. What is likely causing this and how do you fix it?",
    "opts": [
      "Cloud Storage does not support more than 1,000 operations/second — contact support",
      "Cloud Storage has per-prefix QPS limits (~1,000 writes/sec, ~5,000 reads/sec per prefix). At your scale, you're hitting prefix hotspots. Fix: randomize object name prefixes (hash-prefix or date-based sharding) to distribute operations across multiple prefixes",
      "Upgrade to a Cloud Storage multi-region bucket for higher throughput",
      "Enable Cloud Storage parallel composite uploads to reduce write overhead"
    ],
    "ans": 1,
    "explain": "Cloud Storage has per-prefix throughput limits. Objects with the same prefix (e.g., all starting with 'data/') hit limits on that prefix. Fix: add a random hash prefix (e.g., first 4 chars of MD5 of object name as prefix) to distribute objects across multiple prefixes, each with independent throughput limits.",
    "concept": "Cloud Storage QPS limits, prefix hotspots, object name distribution"
  },
  {
    "q": "Your company is planning the infrastructure for a new large-scale application that will need to store over 100 TB or a petabyte of data in NoSQL format for Low-latency read/write and High-throughput analytics. Which storage option should you use?",
    "opts": [
      "Cloud Bigtable",
      "Cloud Spanner",
      "Cloud SQL",
      "Cloud Datastore"
    ],
    "ans": 0,
    "explain": "Correct answer is A as Bigtable is an ideal solution to provide low latency, high throughput data processing storage option with analytics;\n\nRefer GCP documentation - Storage Options: - https://cloud.google.com/storage/docs/;\n\nOptions B & C are wrong as they are relational databases;\n\nOption D is wrong as Cloud Datastore is not ideal for analytics.",
    "concept": "Bigtable"
  },
  {
    "q": "You are building new real-time data warehouse for your company and will use Google BigQuery streaming inserts. There is no guarantee that data will only be sent in once but you do have a unique ID for each row of data and an event timestamp. You want to ensure that duplicates are not included while interactively querying data. Which query type should you use?",
    "opts": [
      "Include ORDER BY DESK on timestamp column and LIMIT to 1.",
      "Use GROUP BY on the unique ID column and timestamp column and SUM on the values.",
      "Use the LAG window function with PARTITION by unique ID along with WHERE LAG IS NOT NULL.",
      "Use the ROW_NUMBER window function with PARTITION by unique ID along with WHERE row equals 1."
    ],
    "ans": 3,
    "explain": "Correct answer is D as the best approach is to ROW_NUMBER with PARTITION by the UNIQUE_ID and filter it by row_number = 1.\n\nRefer GCP documentation - BigQuery Streaming Data - Removing Duplicates: - https://cloud.google.com/bigquery/streaming-data-into-bigquery#manually_removing_duplicates;\n\nTo remove duplicates, perform the following query. You should specify a destination table, allow large results, and disable result flattening.\n\n#standardSQL SELECT * EXCEPT(row_number) FROM ( SELECT *, ROW_NUMBER() OVER (PARTITION BY ID_COLUMN) row_number FROM `TABLE_NAME`) WHERE row_number = 1 ;",
    "concept": "BigQuery"
  },
  {
    "q": "You want to optimize the performance of an accurate, real-time, weather-charting application. The data comes from 50,000 sensors sending 10 readings a second, in the format of a timestamp and sensor reading. Where should you store the data?",
    "opts": [
      "Google BigQuery",
      "Google Cloud SQL",
      "Google Cloud Bigtable",
      "Google Cloud Storage"
    ],
    "ans": 2,
    "explain": "Correct answer is C as Bigtable is a ideal solution for storing time series data. Storing time-series data in Cloud Bigtable is a natural fit. Cloud Bigtable stores data as unstructured columns in rows; each row has a row key, and row keys are sorted lexicographically.\n\nRefer GCP documentation - Storage Options: - https://cloud.google.com/storage/docs/;\n\nOption A is wrong as Google BigQuery is a scalable, fully-managed Enterprise Data Warehouse (EDW) with SQL and fast response times. It is for analytics and OLAP workload, though it also provides storage capacity and price similar to GCS. It cannot handle the required real time ingestion of data.\n\nOption B is wrong as Google Cloud SQL is a fully-managed MySQL and PostgreSQL relational database service for Structured data and OLTP workloads. It also won’t stand for this type of high ingesting rate in real time.\n\nOption D is wrong as Google Cloud Storage is a scalable, fully-managed, highly reliable, and cost-efficient object / blob store. It cannot stand for this amount of data streaming ingestion rate in real-time.",
    "concept": "BigQuery, Bigtable, Cloud SQL"
  },
  {
    "q": "You currently have a Bigtable instance you've been using for development running a development instance type, using HDD's for storage. You are ready to upgrade your development instance to a production instance for increased performance. You also want to upgrade your storage to SSD's as you need maximum performance for your instance. What should you do?",
    "opts": [
      "Upgrade your development instance to a production instance, and switch your storage type from HDD to SSD.",
      "Export your Bigtable data into a new instance, and configure the new instance type as production with SSD's",
      "Run parallel instances where one instance is using HDD and the other is using SSD.",
      "Use the Bigtable instance sync tool in order to automatically synchronize two different instances, with one having the new storage configuration."
    ],
    "ans": 1,
    "explain": "Correct answer is B as the storage for the cluster cannot be updated. You need to define the new cluster and copy or import the data to it.\n\nRefer GCP documentation - Bigtable Choosing HDD vs SSD: - https://cloud.google.com/bigtable/docs/choosing-ssd-hdd;\n\nSwitching between SSD and HDD storage;\n\nWhen you create a Cloud Bigtable instance and cluster, your choice of SSD or HDD storage for the cluster is permanent. You cannot use the Google Cloud Platform Console to change the type of storage that is used for the cluster.\n\nIf you need to convert an existing HDD cluster to SSD, or vice-versa, you can export the data from the existing instance and import the data into a new instance. Alternatively, you can use a Cloud Dataflow or Hadoop MapReduce job to copy the data from one instance to another. Keep in mind that migrating an entire instance takes time, and you might need to add nodes to your Cloud Bigtable clusters before you migrate your instance.\n\nOption A is wrong as storage type cannot be changed.\n\nOptions C & D are wrong as it would have two clusters running at the same time with same data, thereby increasing cost.",
    "concept": "Bigtable, Dataflow"
  },
  {
    "q": "You have spent a few days loading data from comma-separated values (CSV) files into the Google BigQuery table CLICK_STREAM. The column DT stores the epoch time of click events. For convenience, you chose a simple schema where every field is treated as the STRING type. Now, you want to compute web session durations of users who visit your site, and you want to change its data type to the TIMESTAMP. You want to minimize the migration effort without making future queries computationally expensive. What should you do?",
    "opts": [
      "Delete the table CLICK_STREAM, and then re-create it such that the column DT is of the TIMESTAMP type. Reload the data.",
      "Add a column TS of the TIMESTAMP type to the table CLICK_STREAM, and populate the numeric values from the column DT for each row. Reference the column TS instead of the column DT from now on.",
      "Create a view CLICK_STREAM_V, where strings from the column DT are cast into TIMESTAMP values. Reference the view CLICK_STREAM_V instead of the table CLICK_STREAM from now on.",
      "Construct a query to return every row of the table CLICK_STREAM, while using the built-in function to cast strings from the column DT into TIMESTAMP values. Run the query into a destination table NEW_CLICK_STREAM, in which the column TS is the TIMESTAMP type. Reference the table NEW_CLICK_STREAM instead of the table CLICK_STREAM from now on. In the future, new data is loaded into the table NEW_CLICK_STREAM."
    ],
    "ans": 3,
    "explain": "Correct answer is D as the column type cannot be changed and the column needs to casting loaded into a new table using either SQL Query or import/export.\n\nRefer GCP documentation - BigQuery Changing Schema: - https://cloud.google.com/bigquery/docs/manually-changing-schemas#changing_a_columns_data_type;\n\nOption A is wrong as with this approach all the data would be lost and needs to be reloaded;\n\nOption B is wrong as numeric values cannot be used directly and would need casting.\n\nOption C is wrong as view is not materialized views, so the future queries would always be taxed as the casting would be done always.",
    "concept": "BigQuery"
  },
  {
    "q": "A company has loaded its complete financial data for last year for analytics into BigQuery. A Data Analyst is concerned that a BigQuery query could be too expensive. Which methods can be used to reduce the number of rows processed by BigQuery?",
    "opts": [
      "Use the LIMIT clause to limit the number of values in the results.",
      "Use the SELECT clause to limit the amount of data in the query. Partition data by date so the query can be more focused.",
      "Set the Maximum Bytes Billed, which will limit the number of bytes processed but still run the query if the number of bytes requested goes over the limit.",
      "Use GROUP BY so the results will be grouped into fewer output values."
    ],
    "ans": 1,
    "explain": "Correct answer is B as SELECT with partition would limit the data for querying.\n\nRefer GCP documentation - BigQuery Cost Best Practices: - https://cloud.google.com/bigquery/docs/best-practices-costs;\n\nOption C is wrong as the query would fail and would not execute if the Maximum bytes limit is exceeded by the query.\n\nOption D is wrong as GROUP BY would return less output, but would still query the entire data.",
    "concept": "BigQuery"
  },
  {
    "q": "Your company has a BigQuery dataset created, which is located near Tokyo. For efficiency reasons, the company now wants the dataset duplicated in Germany. How can be dataset be made available to the users in Germany?",
    "opts": [
      "Change the dataset from a regional location to multi-region location, specifying the regions to be included.",
      "Export the data from BigQuery into a bucket in the new location, and import it into a new dataset at the new location.",
      "Copy the data from the dataset in the source region to the dataset in the target region using BigQuery commands.",
      "Export the data from BigQuery into nearby bucket in Cloud Storage. Copy to a new regional bucket in Cloud Storage in the new location and Import into the new dataset."
    ],
    "ans": 3,
    "explain": "Correct answer is D as the dataset location cannot be changed once created. The dataset needs to be copied using Cloud Storage.\n\nRefer GCP documentation - BigQuery Exporting Data: - https://cloud.google.com/bigquery/docs/exporting-data;\n\nYou cannot change the location of a dataset after it is created. Also, you cannot move a dataset from one location to another. If you need to move a dataset from one location to another, follow this process:\n\n1. Export the data from your BigQuery tables to a regional or multi-region Cloud Storage bucket in the same location as your dataset. For example, if your dataset is in the EU multi-region location, export your data into a regional or multi-region bucket in the EU. There are no charges for exporting data from BigQuery, but you do incur charges for storing the exported data in Cloud Storage. BigQuery exports are subject to the limits on export jobs.\n\n2. Copy or move the data from your Cloud Storage bucket to a regional or multi-region bucket in the new location. For example, if you are moving your data from the US multi-region location to the Tokyo regional location, you would transfer the data to a regional bucket in Tokyo. Note that transferring data between regions incurs network egress charges in Cloud Storage.\n\n3. After you transfer the data to a Cloud Storage bucket in the new location, create a new BigQuery dataset (in the new location). Then, load your data from the Cloud Storage bucket into BigQuery.You are not charged for loading the data into BigQuery, but you will incur charges for storing the data in Cloud Storage until you delete the data or the bucket. You are also charged for storing the data in BigQuery after it is loaded. Loading data into BigQuery is subject to the limits on load jobs.",
    "concept": "BigQuery, Cloud Storage"
  },
  {
    "q": "A client is using Cloud SQL database to serve infrequently changing lookup tables that host data used by applications. The applications will not modify the tables. As they expand into other geographic regions they want to ensure good performance. What do you recommend?",
    "opts": [
      "Migrate to Cloud Spanner",
      "Read replicas",
      "Instance high availability configuration",
      "Migrate to Cloud Storage"
    ],
    "ans": 1,
    "explain": "Correct answer is B as read replica will increase the availability of the service and can be located closer to the users in the new geographies.\n\nRefer GCP documentation - Cloud SQL Replication Options: - https://cloud.google.com/sql/docs/mysql/replication/;\n\nCloud SQL provides the ability to replicate a master instance to one or more read replicas. A read replica is a copy of the master that reflects changes to the master instance in almost real time.\n\nOption A is wrong as Cloud Spanner is suitable for read/write operations, as the requirement is mainly for read, read replicas would be best fit.\n\nOption C is wrong as high availability is for failover and not for performance.\n\nOption D is wrong as Cloud Storage is not ideal storage for relational data.",
    "concept": "Cloud Spanner, Spanner, Cloud SQL"
  },
  {
    "q": "A company’s BigQuery data is currently stored in external CSV files in Cloud Storage. As the data has increased over the period of time, the query performance has dropped. What steps can help improve the query performance maintaining the cost-effectiveness?",
    "opts": [
      "Import the data into BigQuery for better performance.",
      "Request more slots for greater capacity to improve performance.",
      "Divide the data into partitions based on date.",
      "Time to move to Cloud Bigtable; it is faster in all cases."
    ],
    "ans": 0,
    "explain": "Correct answer is A as the performance issue is because the data is stored in a non-optimal format in an external storage medium.\n\nRefer GCP documentation - BigQuery External Data Sources: - https://cloud.google.com/bigquery/external-data-sources;\n\nQuery performance for external data sources may not be as high as querying data in a native BigQuery table. If query speed is a priority, load the data into BigQuery instead of setting up an external data source. The performance of a query that includes an external data source depends on the external storage type. For example, querying data stored in Cloud Storage is faster than querying data stored in Google Drive. In general, query performance for external data sources should be equivalent to reading the data directly from the external storage.\n\nOption B is wrong as there is feature to request more slots.\n\nOption C is wrong as partitioning of data at source would not improve query time for all use cases.\n\nOption D is wrong as Bigtable is more ideal for NoSQL data type and can get very expensive;",
    "concept": "BigQuery, Bigtable, Cloud Storage"
  },
  {
    "q": "Your company is migrating their 30-node Apache Hadoop cluster to the cloud. They want to re-use Hadoop jobs they have already created and minimize the management of the cluster as much as possible. They also want to be able to persist data beyond the life of the cluster. What should you do?",
    "opts": [
      "Create a Google Cloud Dataflow job to process the data.",
      "Create a Google Cloud Dataproc cluster that uses persistent disks for HDFS.",
      "Create a Hadoop cluster on Google Compute Engine that uses persistent disks.",
      "Create a Cloud Dataproc cluster that uses the Google Cloud Storage connector."
    ],
    "ans": 3,
    "explain": "Correct answer is D. As the requirement is to reuse Hadoop jobs with minimizing the infrastructure management with the ability to store data in a durable external storage, Dataproc with Cloud Storage would be an ideal solution.\n\nRefer GCP documentation - Dataproc FAQs: - https://cloud.google.com/dataproc/docs/resources/faq;\n\nOption A is wrong as Dataflow is not suited to execute Hadoop jobs.\n\nOption B is wrong as HDFS is associated with the Cluster. If the cluster is terminated, the data would be lost.\n\nOption C is wrong as Cluster on Compute Engine would increase infrastructure management and persistent disks would not provide scalability.\n\nOption E is wrong as Cluster on Compute Engine would increase infrastructure management and Local SSDs would not provide data durability.",
    "concept": "Cloud Storage, Dataflow, Dataproc"
  },
  {
    "q": "Your company is running their first dynamic campaign, serving different offers by analyzing real-time data during the holiday season. The data scientists are collecting terabytes of data that rapidly grows every hour during their 30-day campaign. They are using Google Cloud Dataflow to preprocess the data and collect the feature (signals) data that is needed for the machine learning model in Google Cloud Bigtable. The team is observing suboptimal performance with reads and writes of their initial load of 10 TB of data. They want to improve this performance while minimizing cost. What should they do?",
    "opts": [
      "Redefine the schema by evenly distributing reads and writes across the row space of the table.",
      "The performance issue should be resolved over time as the size of the Bigtable cluster is increased.",
      "Redesign the schema to use a single row key to identify values that need to be updated frequently in the cluster.",
      "Redesign the schema to use row keys based on numeric IDs that increase sequentially per user viewing the offers."
    ],
    "ans": 0,
    "explain": "Correct answer is A as the schema needs to be redesigned to distribute the reads and writes evenly across each table.\n\nRefer GCP documentation - Bigtable Performance: - https://cloud.google.com/bigtable/docs/performance;\n\nThe table's schema is not designed correctly. To get good performance from Cloud Bigtable, it's essential to design a schema that makes it possible to distribute reads and writes evenly across each table. See Designing Your Schema for more information.\n\nOption B is wrong as increasing the size of cluster would increase the cost.\n\nOption C is wrong as single row key for frequently updated identifiers reduces performance;\n\nOption D is wrong as sequential IDs would degrade the performance.\n\nA safer approach is to use a reversed version of the user's numeric ID, which spreads traffic more evenly across all of the nodes for your Cloud Bigtable table.",
    "concept": "Bigtable, Dataflow"
  },
  {
    "q": "Your company hosts a 2PB on-premises Hadoop cluster with sensitive data. They want to plan the migration of the cluster to Google Cloud as part of phase 1 activity before the jobs are moved. Current network speed between the colocation and cloud is 10Gbps. What is the efficient way to transfer the data?",
    "opts": [
      "Use Transfer appliance to transfer the data to Cloud Storage",
      "Expose the data as a public URL and Storage Transfer Service to transfer it",
      "Use gsutil command to transfer the data to Cloud Storage",
      "Use hadoop distcp command to copy the data between cluster"
    ],
    "ans": 0,
    "explain": "Correct answer is A as even with 10Gbps of transfer speed it would take minimum 24 days (assuming consistent speed and no interruption) to transfer the complete data. So the best option is to use Google Transfer Appliance.\n\nRefer GCP documentation - Data Transfer: - https://cloud.google.com/products/data-transfer/;\n\nGoogle Transfer Appliance - Securely capture, ship, and upload your data to Google Cloud Storage using the Transfer Appliance 100 TB or 480 TB models.\n\nOptions B, C & D are wrong as they would still route the request through Internet.",
    "concept": "Cloud Storage"
  },
  {
    "q": "Your company is in a highly regulated industry. You have 2 groups of analysts, who perform the initial analysis and sanitization of the data. You now need to provide analyst three secure access to these BigQuery query results, but not the underlying tables or datasets. How would you share the data?",
    "opts": [
      "Export the query results to a public Cloud Storage bucket.",
      "Create a BigQuery Authorized View and assign a project-level user role to analyst three.",
      "Assign the bigquery.resultsonly.viewer role to analyst three.",
      "Create a BigQuery Authorized View and assign an organizational level role to analyst three."
    ],
    "ans": 1,
    "explain": "Correct answer is B as you need to copy or store the query results in a separate dataset and provide authorization to view and/or use that dataset. The other solutions are not secure.\n\nRefer GCP documentation - BigQuery Authorized Views: - https://cloud.google.com/bigquery/docs/authorized-views;\n\nGiving a view access to a dataset is also known as creating an authorized view in BigQuery. An authorized view allows you to share query results with particular users and groups without giving them access to the underlying tables. You can also use the view's SQL query to restrict the columns (fields) the users are able to query.\n\nWhen you create the view, it must be created in a dataset separate from the source data queried by the view. Because you can assign access controls only at the dataset level, if the view is created in the same dataset as the source data, your users would have access to both the view and the data.\n\nOption A is wrong as a public Cloud Storage bucket is accessible to all.\n\nOption C is wrong as there is no resultsonly viewer role.\n\nOption D is wrong as an Organizational role would provide access to the underlying data as well.",
    "concept": "BigQuery, Cloud Storage"
  },
  {
    "q": "Your company is making the move to Google Cloud and has chosen to use a managed database service to reduce overhead. Your existing database is used for a product catalog that provides real-time inventory tracking for a retailer. Your database is 500 GB in size. The data is semi-structured and does not need full atomicity. You are looking for a truly no-ops/serverless solution. What storage option should you choose?",
    "opts": [
      "Cloud Datastore",
      "Cloud Bigtable",
      "Cloud SQL",
      "BigQuery"
    ],
    "ans": 0,
    "explain": "Correct answer is A as Cloud Datastore offers NoOps NoSQL solution which is suited for Semistructured data and ideal for product catalogs.\n\nRefer GCP documentation - Storage Options: - https://cloud.google.com/storage-options/;\n\nOptions B & C are wrong as they are not complete NoOps solution. Also Cloud SQL is not suited for Semi Structured data.\n\nOption D is wrong as BigQuery is ideal for analytics solution ;",
    "concept": "BigQuery, Cloud SQL"
  },
  {
    "q": "Your company receives a lot of financial data in CSV files. The files need to be processed, cleaned and transformed before they are made available for analytics. The schema of the data also changes every third month. The Data analysts should be able to perform the tasks;\n\n1. No prior knowledge of any language with no coding;\n\n2. Provided a GUI tool to build and modify the schema;\n\nWhat solution best fits the need?",
    "opts": [
      "Use Dataflow code and provide Data Analysts the access to the code. Store the schema externally to be easily modified.",
      "Use Dataprep with transformation recipes.",
      "Use Dataproc spark and provide Data Analysts the access to the code. Store the schema externally to be easily modified.",
      "Use DataLab with transformation recipes."
    ],
    "ans": 1,
    "explain": "Correct answer is B as Dataprep can be used to handle schema changes by Data Analysts without any programming knowledge, but through an easy to use GUI.\n\nRefer GCP documentation - Dataprep: - https://cloud.google.com/dataprep/;\n\nCloud Dataprep by Trifacta is an intelligent data service for visually exploring, cleaning, and preparing structured and unstructured data for analysis. Cloud Dataprep is serverless and works at any scale. There is no infrastructure to deploy or manage. Easy data preparation with clicks and no code.\n\nVisually explore and interact with data in seconds. Instantly understand data distribution and patterns. You don't need to write code. You can prepare data with a few clicks.\n\nProcess diverse datasets — structured and unstructured. Transform data stored in CSV, JSON, or relational table formats. Prepare datasets of any size, megabytes to terabytes, with equal ease.\n\nOptions A, C & D are wrong as they would need programming knowledge.",
    "concept": "GCP Data Engineering"
  },
  {
    "q": "A user wishes to generate reports on petabyte scale data using a Business Intelligence (BI) tools. Which storage option provides integration with BI tools and supports OLAP workloads up to petabyte-scale?",
    "opts": [
      "Bigtable",
      "Cloud Datastore",
      "Cloud Storage",
      "BigQuery"
    ],
    "ans": 3,
    "explain": "Correct answer is D as BigQuery is fully managed data warehouse and is fast and easy to use on data of any size. With BigQuery, you’ll get great performance on your data, while knowing you can scale seamlessly to store and analyze petabytes more without having to buy more capacity.\n\nRefer GCP documentation - Storage Options: - https://cloud.google.com/storage-options/;\n\nOptions A & B are wrong as Bigtable & Datastore are NoSQL solution and not suitable for OLAP data warehouse work loads.\n\nOption C is wrong as Cloud Storage provides object storage only.",
    "concept": "BigQuery, Bigtable, Cloud Storage"
  },
  {
    "q": "Your company is planning to migrate its data first to Google Cloud Storage. You need to keep the contents of this bucket in sync with a new Google Cloud Storage bucket to support a backup storage destination. What is the best method to achieve this?",
    "opts": [
      "Once per week, use a gsutil cp command to copy over newly modified files.",
      "Use gsutil rsync commands to keep both locations in sync.",
      "Use Storage Transfer Service to keep both the source and destination in sync.",
      "Use gsutil -m cp to keep both locations in sync."
    ],
    "ans": 1,
    "explain": "Correct answer is B as the data transfer is between on-premises and Google Cloud, the gsutil rsync can be used to keep the source and destination in sync.\n\ngsutil rsync command makes the contents under dst_url the same as the contents under src_url, by copying any missing files/objects (or those whose data has changed), and (if the -d option is specified) deleting any extra files/objects. src_url must specify a directory, bucket, or bucket subdirectory. ;\n\nOptions A & D are wrong as copy can be used to copy, however there needs to be more handling to keep it in sync.\n\nOption C is wrong as the data is not available in an online location.",
    "concept": "Cloud Storage"
  },
  {
    "q": "You have migrated your Hadoop jobs with external dependencies on a Dataproc cluster. As a security requirement, the cluster has been setup using internal IP addresses only and does not have a direct Internet connectivity. How can the cluster be configured to allow the installation of the dependencies?",
    "opts": [
      "Setup a SSH tunnel to Internet and route outbound requests through it.",
      "Store the external dependencies in Cloud Storage and modify the initialization scripts",
      "Setup a SOCKS proxy and route outbound requests through it.",
      "Setup the Dataproc master node is public subnet to be able to download external dependencies"
    ],
    "ans": 1,
    "explain": "Correct answer is B as the Dataproc cluster is configured with internal IP addresses only, the dependencies can be stored in Cloud Storage so that they can be accessed using internal IPs.\n\nRefer GCP documentation - Dataproc Init Actions: - https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/init-actions#important_things_to_know;\n\nIf you create a Cloud Dataproc cluster with internal IP addresses only, attempts to access the Internet in an initialization action will fail unless you have configured routes to direct the traffic through a NAT or a VPN gateway. Without access to the Internet, you can enable Private Google Access, and place job dependencies in Cloud Storage; cluster nodes can download the dependencies from Cloud Storage from internal IPs.\n\nOptions A, C & D are wrong as they would not allow secure outbound connection.",
    "concept": "Cloud Storage, Dataproc"
  },
  {
    "q": "Your infrastructure runs on AWS and includes a set of multi-TB enterprise databases that are backed up nightly on the S3. You need to create a redundant backup to Google Cloud. You are responsible for performing scheduled monthly disaster recovery drills. You want to create a cost-effective solution. What should you do?",
    "opts": [
      "Use Transfer Appliance to transfer the backup files to a Cloud Storage Nearline storage bucket as a final destination.",
      "Use Transfer Appliance to transfer the backup files to a Cloud Storage Coldline bucket as a final destination.",
      "Use Storage Transfer Service to transfer the backup files to a Cloud Storage Nearline storage bucket as a final destination.",
      "Use Storage Transfer Service to transfer the backup files to a Cloud Storage Coldline storage bucket as a final destination."
    ],
    "ans": 2,
    "explain": "Correct answer is C as the data needs to be backed up nightly and accessed monthly, Storage Transfer service can be used to transfer it from S3 and Nearline storage for cost-effective storage solution.\n\nRefer GCP documentation - Storage Classes & Data Transfer: - https://cloud.google.com/storage/docs/storage-classes & https://cloud.google.com/products/data-transfer/;\n\nOptions A & B are wrong Transfer Appliance is ideal for huge one time data.\n\nOption D is wrong as Coldline storage is suitable is data needs be accessed yearly.",
    "concept": "GCP Data Engineering"
  },
  {
    "q": "Your infrastructure includes two 100-TB enterprise file servers. You need to perform a one-way, one-time migration of this data to the Google Cloud securely. Only users in Germany will access this data. You want to create the most cost-effective solution. What should you do?",
    "opts": [
      "Use Transfer Appliance to transfer the offsite backup files to a Cloud Storage Regional storage bucket as a final destination.",
      "Use Transfer Appliance to transfer the offsite backup files to a Cloud Storage Multi-Regional bucket as a final destination.",
      "Use Storage Transfer Service to transfer the offsite backup files to a Cloud Storage Regional storage bucket as a final destination.",
      "Use Storage Transfer Service to transfer the offsite backup files to a Cloud Storage Multi-Regional storage bucket as a final destination."
    ],
    "ans": 0,
    "explain": "Correct answer is A as the data is huge it can be transferred using Transfer Appliance in a time and cost effective way. Also, as the data is going to be accessed in a single region it can be hosted in a regional bucket.\n\nRefer GCP documentation - Storage Classes: - https://cloud.google.com/storage/docs/storage-classes;\n\nOption B is wrong as the data is accessed in a single region, it would be more cost effective storing it in a regional bucket.\n\nOptions C & D are wrong as the data is huge it is more time and cost effective to transfer the data Transfer Appliance.",
    "concept": "GCP Data Engineering"
  },
  {
    "q": "Your company plans to migrate a multi-petabyte data set to the cloud. The data set must be available 24hrs a day. Your business analysts have experience only with using a SQL interface. How should you store the data to optimize it for ease of analysis?",
    "opts": [
      "Load data into Google BigQuery.",
      "Insert data into Google Cloud SQL.",
      "Put flat files into Google Cloud Storage.",
      "Stream data into Google Cloud Datastore."
    ],
    "ans": 0,
    "explain": "Correct answer is A as BigQuery is the only of these Google products that supports an SQL interface and a high enough SLA (99.9%) to make it readily available.\n\nOption B is wrong as Cloud SQL cannot support multi-petabyte data. Storage limit for Cloud SQL is 10TB;\n\nOption C is wrong as Cloud Storage does not provide SQL interface.\n\nOption D is wrong as Datastore does not provide a SQL interface and is a NoSQL solution.",
    "concept": "BigQuery, Cloud SQL, Cloud Storage"
  },
  {
    "q": "You want to display aggregate view counts for your YouTube channel data in Data Studio. You want to see the video tiles and view counts summarized over the last 30 days. You also want to segment the data by the Country Code using the fewest possible steps. What should you do?",
    "opts": [
      "Set up a YouTube data source for your channel data for Data Studio. Set Views as the metric and set Video Title as a report dimension. Set Country Code as a filter.",
      "Set up a YouTube data source for your channel data for Data Studio. Set Views as the metric and set Video Title and Country Code as report dimensions.",
      "Export your YouTube views to Cloud Storage. Set up a Cloud Storage data source for Data Studio. Set Views as the metric and set Video Title as a report dimension. Set Country Code as a filter.",
      "Export your YouTube views to Cloud Storage. Set up a Cloud Storage data source for Data Studio. Set Views as the metric and set Video Title and Country Code as report dimensions."
    ],
    "ans": 1,
    "explain": "Correct answer is B as there is no need to export; you can use the existing YouTube data source. Country Code is a dimension because it's a string and should be displayed as such, that is, showing all countries, instead of filtering.\n\nRefer GCP documentation - Data Studio Youtube connector: - https://support.google.com/datastudio/answer/7020432?hl=en;\n\nOption A is wrong as you cannot produce a summarized report that meets your business requirements using the options listed.\n\nOptions C & D are wrong as you do not need to export data from YouTube to Cloud Storage; you can simply use the existing YouTube data source.",
    "concept": "Cloud Storage"
  },
  {
    "q": "You want to process payment transactions in a point-of-sale application that will run on Google Cloud Platform. Your user base could grow exponentially, but you do not want to manage infrastructure scaling. Which Google database service should you use?",
    "opts": [
      "Cloud SQL",
      "BigQuery",
      "Cloud Bigtable",
      "Cloud Datastore"
    ],
    "ans": 3,
    "explain": "Correct answer is D as the payment transactions would need a transactional data service Datastore can support the same. Also it is fully managed with NoOps required.\n\nRefer GCP documentation - Storage Options: - https://cloud.google.com/storage-options/;\n\nOption A is wrong as Cloud SQL would need infrastructure scaling. Although storage can be automatically scaled (upto a limit), instance type needs to be changed as per the load manually.\n\nOption B is wrong as BigQuery is an data warehousing option.\n\nOption C is wrong as Bigtable is not a relational database but an NoSQL option.",
    "concept": "BigQuery, Bigtable, Cloud SQL"
  },
  {
    "q": "Your company hosts its data into multiple Cloud SQL databases. You need to export your Cloud SQL tables into BigQuery for analysis. How can the data be exported?",
    "opts": [
      "Convert your Cloud SQL data to JSON format, then import directly into BigQuery",
      "Export your Cloud SQL data to Cloud Storage, then import into BigQuery",
      "Import data to BigQuery directly from Cloud SQL.",
      "Use the BigQuery export function in Cloud SQL to manage exporting data into BigQuery."
    ],
    "ans": 1,
    "explain": "Correct answer is B as BigQuery does not provide direct load from Cloud SQL. The data needs to be loaded through Cloud Storage.\n\nRefer GCP documentation - BigQuery loading data: - https://cloud.google.com/bigquery/docs/loading-data;\n\nOptions A, C & D are wrong as they are not supported options.",
    "concept": "BigQuery, Cloud SQL, Cloud Storage"
  },
  {
    "q": "Your company is building a package tracking application to track the complete lifecycle of the package. The data is stored in a BigQuery time partitioned table. Over the period of time the data in the table and grown manifold and Data Scientists are complaining of slowness in their package tracking queries. How can the table be modified to improve the performance and maintaining cost effectiveness?",
    "opts": [
      "Import the table data to Bigtable",
      "Change the partitioned table column from time to date",
      "Update to table to perform clustering on package id",
      "Ask the Data Scientists to use LIMIT parameter on the queries"
    ],
    "ans": 2,
    "explain": "Correct answer is C as Clustering the data on the package Id can greatly improve the performance.\n\nRefer GCP documentation - BigQuery Clustered Table: - https://cloud.google.com/bigquery/docs/clustered-tables;\n\nClustering can improve the performance of certain types of queries such as queries that use filter clauses and queries that aggregate data. When data is written to a clustered table by a query job or a load job, BigQuery sorts the data using the values in the clustering columns. These values are used to organize the data into multiple blocks in BigQuery storage. When you submit a query containing a clause that filters data based on the clustering columns, BigQuery uses the sorted blocks to eliminate scans of unnecessary data;\n\nIn a table partitioned by a date or timestamp column, each partition contains a single day of data. When the data is stored, BigQuery ensures that all the data in a block belongs to a single partition. A partitioned table maintains these properties across all operations that modify it: query jobs, Data Manipulation Language (DML) statements, Data Definition Language (DDL) statements, load jobs, and copy jobs. This requires BigQuery to maintain more metadata than a non-partitioned table. As the number of partitions increases, the amount of metadata overhead increases.\n\nAlthough more metadata must be maintained, by ensuring that data is partitioned globally, BigQuery can more accurately estimate the bytes processed by a query before you run it. This cost calculation provides an upper bound on the final cost of the query.\n\nIn a clustered table, BigQuery automatically sorts the data based on the values in the clustering columns and organizes them in optimally sized storage blocks. You can achieve more finely grained sorting by creating a table that is clustered and partitioned. A clustered table maintains the sort properties in the context of each operation that modifies it. As a result, BigQuery may not be able to accurately estimate the bytes processed by the query or the query costs. When blocks of data are eliminated during query execution, BigQuery provides a best effort reduction of the query costs.\n\nOption A is wrong as Bigtable would not be a cost effective option.\n\nOption B is wrong as changing the partitioning from time to date would be impact queries on packages.\n\nOption D is wrong as LIMIT parameter does limit the amount of data queried.",
    "concept": "BigQuery, Bigtable"
  },
  {
    "q": "You have a dataset in BigQuery storing transaction data with details of product and date purchased. Query fired on the data for a product using --dry-run shows that is performs a complete scan. How can the performance of the query be improved?",
    "opts": [
      "Dry run always shows complete scan and the result would be different when the actual query is fired",
      "Use the limit parameter to limit the data queried",
      "Set maximum bytes on the query to limit the amount of data queried",
      "Use Partitioning and clustering on the table"
    ],
    "ans": 3,
    "explain": "Correct answer is D as it is recommended to have the table partitioned and clustered so as query only relevant data to improve the performance and keep the costs low.\n\nRefer GCP documentation - BigQuery Cost Best Practices and Clustering: - https://cloud.google.com/bigquery/docs/best-practices-costs & https://cloud.google.com/bigquery/docs/clustered-tables;\n\nOption A is wrong as dry-run shows the actual data that would be queried and is one of the best practices to check before the actual query is fired.\n\nOption B is wrong as limit does not limit the amount of data queried, but only the results.\n\nOption C is wrong as maximum bytes limits the amount of data queried. However, it fails if the bytes is exceeded without any results.",
    "concept": "BigQuery"
  },
  {
    "q": "You are upgrading your existing (development) Cloud Bigtable instance for use in your production environment. The instance contains a large amount of data that you want to make available for production immediately. You need to design for fastest performance. What should you do?",
    "opts": [
      "Change your Cloud Bigtable instance type from Development to Production, and set the number of nodes to at least 3. Verify that the storage type is HDD.",
      "Change your Cloud Bigtable instance type from Development to Production, and set the number of nodes to at least 3. Verify that the storage type is SSD.",
      "Export the data from your current Cloud Bigtable instance to Cloud Storage. Create a new Cloud Bigtable Production instance type with at least 3 nodes. Select the HDD storage type. Import the data into the new instance from Cloud Storage.",
      "Export the data from your current Cloud Bigtable instance to Cloud Storage. Create a new Cloud Bigtable Production instance type with at least 3 nodes. Select the SSD storage type. Import the data into the new instance from Cloud Storage."
    ],
    "ans": 1,
    "explain": "Correct answer is B as Cloud Bigtable allows you to 'scale-in-place,' which meets your requirements for this scenario.\n\nRefer GCP documentation - Cloud Bigtable & Choosing SSD vs HDD: - https://cloud.google.com/bigtable/docs/choosing-ssd-hdd;\n\nIncredible scalability. Cloud Bigtable scales in direct proportion to the number of machines in your cluster. A self-managed HBase installation has a design bottleneck that limits the performance after a certain threshold is reached. Cloud Bigtable does not have this bottleneck, so you can scale your cluster up to handle more reads and writes.\n\nSimple administration. Cloud Bigtable handles upgrades and restarts transparently, and it automatically maintains high data durability. To replicate your data, simply add a second cluster to your instance, and replication starts automatically. No more managing masters or regions; just design your table schemas, and Cloud Bigtable will handle the rest for you.\n\nCluster resizing without downtime. You can increase the size of a Cloud Bigtable cluster for a few hours to handle a large load, then reduce the cluster's size again—all without any downtime. After you change a cluster's size, it typically takes just a few minutes under load for Cloud Bigtable to balance performance across all of the nodes in your cluster.\n\nSSD is significantly faster and has more predictable performance than HDD. In a Cloud Bigtable cluster, SSD storage delivers 6 ms latencies for both reads and writes for 99% of all requests. By contrast, HDD storage delivers 200 ms read latencies and 50 ms write latencies on the same benchmark.\n\nOption A is wrong as you should be using SSD storage for this scenario.\n\nOptions C & D are wrong as creating a new Cloud Bigtable instance is extraneous and not needed to export, you can upgrade in place for nodes, but the storage type cannot be changed.",
    "concept": "Bigtable"
  },
  {
    "q": "You have been asked to select the storage system for the click-data of your company’s large portfolio of websites. This data is streamed in from a custom website analytics package at a typical rate of 6,000 clicks per minute. With bursts of up to 8,500 clicks per second. It must have been stored for future analysis by your data science and user experience teams. Which storage infrastructure should you choose?",
    "opts": [
      "Google Cloud SQL",
      "Google Cloud Bigtable",
      "Google Cloud Storage",
      "Google Cloud Datastore"
    ],
    "ans": 1,
    "explain": "Correct answer is B as Bigtable provides a scalable, fully-managed NoSQL wide-column database that is suitable for both real-time access and analytics workloads.\n\nRefer GCP documentation - Storage Options: - https://cloud.google.com/storage-options/;\n\nOption A is wrong as Google Cloud SQL is mainly for OLTP (Transactional, CRUD) not for taking and storing streaming data. It does not have the scalability and elasticity to absorb this amount of data in real time.\n\nOption D is wrong as Google Cloud Datastore does not provide analytics capabilities. Google Cloud Datastore is a NoSQL document database built for automatic scaling, high performance, and ease of application development and integrating well with App Engine.\n\nOption C is wrong as Google Cloud Storage is not suitable to handle real time streaming data. It also needs to be used with BigQuery for analytics.",
    "concept": "BigQuery, Bigtable, Cloud SQL"
  },
  {
    "q": "You are part of your company’s migration team to transfer 1PB of data to Google Cloud. The network speed between the on-premises data center and Google Cloud is 100Mbps. The migration activity has a timeframe of 6 months. What is the efficient way to transfer the data?",
    "opts": [
      "Use BigQuery Data Transfer Service to transfer the data to Cloud Storage",
      "Expose the data as a public URL and Storage Transfer Service to transfer it",
      "Use Transfer appliance to transfer the data to Cloud Storage",
      "Use gsutil command to transfer the data to Cloud Storage"
    ],
    "ans": 2,
    "explain": "Correct answer is C as even with 100Mbps of transfer speed it would take minimum 3 years (assuming consistent speed and no interruption) to transfer the complete data. So the best option is to use Google Transfer Appliance.\n\nRefer GCP documentation - Data Transfer: - https://cloud.google.com/products/data-transfer/;\n\nGoogle Transfer Appliance - Securely capture, ship, and upload your data to Google Cloud Storage using the Transfer Appliance 100 TB or 480 TB models.\n\nOptions A, B & D are wrong as they would still route the request through Internet.",
    "concept": "Cloud Storage"
  },
  {
    "q": "Your production Bigtable instance is currently using four nodes. Due to the increased size of your table, you need to add additional nodes to offer better performance. How should you accomplish this without the risk of data loss?",
    "opts": [
      "Power off your Bigtable instance, then increase the node count, then power back on. Be sure to schedule downtime in advance.",
      "Export your Bigtable data as sequence files into Cloud Storage, then import the data into a new Bigtable instance with additional nodes added.",
      "Use the node migration service to add additional nodes.",
      "Edit instance details and increase the number of nodes. Save your changes. Data will re-distribute with no downtime."
    ],
    "ans": 3,
    "explain": "Correct answer is D as Bigtable size i.e. nodes can be increased without any downtime.\n\nRefer GCP documentation - Modifying Bigtable Instance: - https://cloud.google.com/bigtable/docs/modifying-instance#nodes;\n\nOption A is wrong as Bigtable instance does not need to be turned off.\n\nOption B is wrong as this is not an optimized solution.\n\nOption C is wrong as there is no node migration service.",
    "concept": "Bigtable"
  },
  {
    "q": "Your company’s on-premises Apache Hadoop servers are approaching end-of-life, and IT has decided to migrate the cluster to Google Cloud Dataproc. A like-for-like migration of the cluster would require 50 TB of Google Persistent Disk per node. The CIO is concerned about the cost of using that much block storage. You want to minimize the storage cost of the migration. What should you do?",
    "opts": [
      "Put the data into Google Cloud Storage.",
      "Use preemptible virtual machines (VMs) for the Cloud Dataproc cluster.",
      "Tune the Cloud Dataproc cluster so that there is just enough disk for all data.",
      "Migrate some of the cold data into Google Cloud Storage, and keep only the hot data in Persistent Disk."
    ],
    "ans": 0,
    "explain": "Correct answer is A as moving the data to Cloud Storage would reduce the storage cost a lot. Also, this would enable the customer to use transient cluster.\n\nRefer GCP documentation - Dataproc Data Accessibility: - https://cloud.google.com/dataproc/docs/resources/faq#data_access_availability;\n\nHow can I get data in and out of a cluster?\n\nCloud Dataproc utilizes the Hadoop Distributed File System (HDFS) for storage. Additionally, Cloud Dataproc automatically installs the HDFS-compatible Google Cloud Storage connector, which enables the use of Cloud Storage in parallel with HDFS. Data can be moved in and out of a cluster through upload/download to HDFS or Cloud Storage.\n\nCan I use Cloud Storage with Dataproc?\n\nYes, Cloud Dataproc clusters automatically install the Cloud Storage connector. There are a number of benefits to choosing Cloud Storage over traditional HDFS including data persistence, reliability, and performance.\n\nOption B is wrong as preemptible VMs would only help improve execution performance and cost, but would not reduce the storage cost.\n\nOption C is wrong as it would not reduce costs.\n\nOption D is wrong as it would not reduce the cost by much as it would still need the data to be stored across nodes.",
    "concept": "Cloud Storage, Dataproc"
  },
  {
    "q": "Your company currently hosts an AWS S3 bucket. You need to keep the contents of this bucket in sync with a new Google Cloud Storage bucket to support a backup storage destination. What is the best method to achieve this?",
    "opts": [
      "Once per week, use a gsutil cp command to copy over newly modified files.",
      "Use gsutil rsync commands to keep both locations in sync.",
      "Use Storage Transfer Service to keep both the source and destination in sync.",
      "Use gsutil -m cp to keep both locations in sync."
    ],
    "ans": 2,
    "explain": "Correct answer is C as the source is an S3 bucket, Storage Transfer Service is the preferred option, which can help scheduled jobs as well.\n\nRefer GCP documentation - Storage Transfer Service: - https://cloud.google.com/storage-transfer/docs/overview;\n\nOptions A & D are wrong as copy can be used to copy, however there needs to be more handling to keep it in sync.\n\nOption B is wrong as gsutil rsync option is a valid option, however preferred when the transfer cannot be done through Storage transfer service.",
    "concept": "Cloud Storage"
  },
  {
    "q": "An application has the following data requirements. 1. It requires strongly consistent transactions. 2. Total data will be less than 500 GB. 3. The data does not need to be streaming or real time. Which data technology would fit these requirements?",
    "opts": [
      "BigQuery",
      "Cloud Bigtable",
      "Cloud SQL",
      "Cloud Memorystore"
    ],
    "ans": 2,
    "explain": "Correct answer is C as Cloud SQL supports strongly consistent transactions. And the size requirements will fit with a Cloud SQL instance.\n\nRefer GCP documentation - Storage Options: - https://cloud.google.com/storage-options/;\n\nOption A is wrong as BigQuery does not support consistent transactions.\n\nOption B is wrong as Bigtable is not recommended for data less than a TB as it can get very expensive and is not a relational database service.\n\nOption D is wrong as Cloud Memorystore for Redis provides a fully managed in-memory data store service built on scalable, secure, and highly available infrastructure managed by Google. It does not support consistent transactions;",
    "concept": "BigQuery, Bigtable, Cloud SQL"
  },
  {
    "q": "Your BigQuery dataset contains 1500 tables. When conducting a query, you are limited to a maximum of 1000 tables that you can query at once. You need to query data across all 1500 tables. What should you do?",
    "opts": [
      "Place tables into separate datasets.",
      "If possible, merge the 1500 tables to bring the total number below 1000. You may still partition single tables to divide data for queries.",
      "Export the data to Bigtable, and conduct your query inside of Bigtable.",
      "Create multiple views of chunks of the 1500 tables, then query the multiple views."
    ],
    "ans": 1,
    "explain": "Correct answer is B as the only option is to reduce the tables and using partitioning to query the data effectively.\n\nOption A is wrong as even with separate datasets the limit to tables in an query exists.\n\nOption C is wrong as Bigtable is an expensive option.\n\nOption D is wrong as multiple views cannot be queried. Also wildcard does not support views.",
    "concept": "BigQuery, Bigtable"
  },
  {
    "q": "An application that relies on Cloud SQL to read infrequently changing data is predicted to grow dramatically. How can you increase capacity for more read-only clients?",
    "opts": [
      "Configure high availability on the master node",
      "Establish an external replica in the customer's data center",
      "Use backups so you can restore if there's an outage",
      "Configure read replicas."
    ],
    "ans": 3,
    "explain": "Correct answer is D as read replicas can help handle the read traffic reducing the load from the primary database.\n\nRefer GCP documentation - Cloud SQL Replication Options: - https://cloud.google.com/sql/docs/mysql/replication/;\n\nCloud SQL provides the ability to replicate a master instance to one or more read replicas. A read replica is a copy of the master that reflects changes to the master instance in almost real time.\n\nOption A is wrong as high availability is for failover and not for performance.\n\nOption B is wrong as external replica is not recommended for scaling as it needs to be maintained and the network established for replication.\n\nOption C is wrong as backups are more to restore the database in case of any outage.",
    "concept": "Cloud SQL"
  },
  {
    "q": "A client wants to store files from one location and retrieve them from another location. Security requirements are that no one should be able to access the contents of the file while it is hosted in the cloud. What is the best option?",
    "opts": [
      "Default encryption should be sufficient",
      "Customer-Supplied Encryption Keys (CSEK)",
      "Customer Managed Encryption Keys (CMEK)",
      "Client-side encryption"
    ],
    "ans": 3,
    "explain": "Correct answer is D as the requirement is that the file cannot be decrypted in the cloud, so encrypt it before it is uploaded and after it is downloaded adds a layer of encryption.\n\nRefer GCP documentation - Data Encryption Options: - https://cloud.google.com/storage/docs/encryption/;\n\nServer-side encryption: encryption that occurs after Cloud Storage receives your data, but before the data is written to disk and stored.\n\nCustomer-supplied encryption keys: You can create and manage your own encryption keys for server-side encryption, which act as an additional encryption layer on top of the standard Cloud Storage encryption.\n\nCustomer-managed encryption keys: You can generate and manage your encryption keys using Cloud Key Management Service, which act as an additional encryption layer on top of the standard Cloud Storage encryption.\n\nClient-side encryption: encryption that occurs before data is sent to Cloud Storage. Such data arrives at Cloud Storage already encrypted but also undergoes server-side encryption.",
    "concept": "Cloud Storage"
  },
  {
    "q": "Your company has hired an external consultant to help import its relational data into BigQuery for analysis. The consultant mentions the data needs to be denormalized in BigQuery. What are the two benefits of using denormalized data structures in BigQuery?",
    "opts": [
      "Reduces the amount of data processed, reduces the amount of storage required.",
      "Increases query speed, makes queries simpler.",
      "Reduces the amount of storage required, increases query speed.",
      "Reduces the amount of data processed, increases query speed."
    ],
    "ans": 1,
    "explain": "Correct answer is B as Denormalization help increase query speed without the need to join tables with simpler queries.\n\nRefer GCP documentation - BigQuery Managing Data: - https://cloud.google.com/solutions/bigquery-data-warehouse#managing_data;\n\nOptions A, C & D are wrong as Denormalization requires more storage, and also increased the amount of data processed.",
    "concept": "BigQuery"
  },
  {
    "q": "You are responsible for security and access control to a BigQuery dataset hosted within a project. Multiple users from multiple teams need to have access to the different tables within the dataset. How can the access be control?",
    "opts": [
      "Create Authorized views for tables in a separate project and grant access to the teams",
      "Create Authorized views for tables in a same project and grant access to the teams",
      "Create Materialized views for tables in a separate project and grant access to the teams",
      "Create Materialized views for tables in a same project and grant access to the teams"
    ],
    "ans": 0,
    "explain": "Correct answer is A as the controlled access can be provided using Authorized views created in a separate project.\n\nBigQuery is a petabyte-scale analytics data warehouse that you can use to run SQL queries over vast amounts of data in near realtime.\n\nGiving a view access to a dataset is also known as creating an authorized view in BigQuery. An authorized view allows you to share query results with particular users and groups without giving them access to the underlying tables. You can also use the view's SQL query to restrict the columns (fields) the users are able to query.\n\nWhen you create the view, it must be created in a dataset separate from the source data queried by the view. Because you can assign access controls only at the dataset level, if the view is created in the same dataset as the source data, your data analysts would have access to both the view and the data.\n\nOption B is wrong as Authorized views should be created in a separate project. If they are created in the same project, the users would have access to the underlying tables as well.\n\nOptions C & D are wrong as only logical views can be created and not materialized.",
    "concept": "BigQuery"
  }
]